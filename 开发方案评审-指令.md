# 开发方案评审指令

> 把本文件连同下方材料清单交给评审 agent。它是**给评审者的指令**，不是被评审的内容。

---

## 1. 你的任务

评审《AI体检报告分析-开发方案V1.md》，判断标准只有一条：

> **一个不了解前因后果的后端开发，只看这套文档，能不能写出正确、安全、可联调的代码？**

不能，就指出具体是哪一段挡住了他，以及他会写错成什么样。

你不是在评价文档写得好不好看，也不是在补充你认为应该有的最佳实践。
**你在找的是「照着做会出错」的地方。**

## 2. 材料

| 文件 | 角色 |
|---|---|
| `AI体检报告分析-开发方案V1.md` | **主要被评审对象**（1907 行） |
| `AGENTS.md` | 工程约束，优先级最高 |
| `AI体检报告分析-精简设计方案V1.md` | 产品与架构行为的上游依据 |
| `体检报告分析需求.md` | 原始需求 |
| `schema/llm_a_output.schema.json` | LLM-A 正式契约 |
| `schema/llm_b_output.schema.json` | LLM-B 正式契约 |
| `prompt/llm_a_extract.md` | LLM-A 提示词 |
| `prompt/llm_b_dish_tag.md` | LLM-B 提示词 |
| `constants/README.md` | 常量结构定义 |
| `constants/内容常量草案.md` | 常量内容草案 |
| `allergen_display_split.csv` | 过敏原词表基线 |
| `AI体检报告分析-精简设计方案V1-开发评审.md` | **上一轮评审意见**，本版声称已全部处理 |

`AI体检报告分析与菜品推荐-完整技术方案V1_7_1.md` 是**已废弃的旧方案**，仅供历史追溯。
**不要拿它当判据**，它描述的是一套被取代的架构（任务状态在 Redis、双 Redis 实例、
`diet_advice_cache` 表、LLM-C、Lua 入队、模型重试）。

## 3. 不要重开的决策

以下是产品/技术负责人已经明确拍板的。你可以指出它们的**实现是否自洽**，
但**不要再论证它们该不该这么定**——上一轮评审已经提过一遍，重复提没有价值。

```
零重试            不重试模型调用、不重投队列消息、无 outbox、无 DLQ
新任务重解析      「重新解析」= 同批 fileIds 重调 analyze，产生新 taskId
无提示文字        四个模块只出内容、来源、空态句、底部声明，不出任何提示/说明/警示
静默降级          三种降级隐藏对应模块，不告诉用户原因
简单空态          健康问题为空即用需求原文「各项指标均在正常范围内」，不区分完整性
第四态 ABNORMAL   状态标签新增橙色第四态，阳性/阴性由模型判断
模型判断状态      LLM-A 判定非方向性结论是否属于异常
不做缺页判定      需求 §3-9 的「体检报告不完整」V1 不实现
同一人限制        多文件姓名/性别不一致直接失败，不支持代家人分析
过敏隐藏正面标签  过敏拒绝的菜不下发任何推荐标签
OCR 喂 LLM-A      OCR 文本带 segmentId 送模型
硬编码建议内容    无内容管理后台、无生成模型、无版本激活
占位符            S3/Dify/OCR/userId/菜品查询 五处只写接口 + TODO
不写配置类        不写任何中间件或数据源配置类
不管前端          只评审后端
```

## 4. 重点检查区

按「作者自评置信度从低到高」排列。**前四项最可能有问题，请优先投入时间。**

### 4.1 表格 Segment 的 Y 坐标聚类（§6.3）—— 置信度最低

作者为了解决「指标五字段必须在同一 segment」与「PDFBox 识别不了表格单元格」的矛盾，
自行设计了「按 Y 坐标聚类成逻辑行」。**这个算法未经任何真实报告验证。**

请具体检查：

- PDFBox 的 `TextPosition` 能否稳定提供所需坐标？该怎么拿？
- 「行高容差 = 该页平均字号 × 0.6」这个规则在**上下标、跨行单元格、多栏排版**下会怎样？
- 体检报告常见的**双栏布局**（左右各一个指标表）会不会把两栏的字拼进同一行？
- 跨页续表的表头行成为独立 segment 后，模型能否正确关联？
- DOCX 的 `<w:tr>` 一行一 segment，与 PDF 的坐标聚类，产出的 segment 粒度是否真的可比？
- OCR 识别块的坐标聚类，与 PDF 的，阈值能不能共用？

**如果这个设计不成立，模块一（健康指标）整个做不出来。** 请给出替代方案而不只是指出问题。

### 4.2 MySQL/Redis 结果可见性协议（§4.2）—— 请当作攻击目标

作者声称「预写 Redis → CAS 置 SUCCEEDED + result_visible」这个协议关闭了
「用户删除后结果重新出现」的竞态。**请尝试证伪。**

具体请构造并检查这些时序：

- Worker 预写 → 用户 DELETE（删了 Redis、打了 deleted_at）→ Worker CAS 失败 → Worker 执行 DEL，但**此时 Worker 进程崩溃在 DEL 之前**
- 用户 DELETE 与 Worker CAS **在同一毫秒**，MySQL 行锁下谁先谁后
- `deleted_at` 打上后、`DEL result` 之前，GET 请求进来
- Redis 主从切换导致 `DEL` 丢失
- 同一个 taskId 被两个 Worker 同时处理（Stream 重复投递 + CAS 都成功？）

### 4.3 零重试的连锁后果 —— 作者可能没追完

零重试是本轮才定的，它影响了很多处。请检查这些地方是否都自洽：

- `QueuedTimeoutSweepJob` 5 分钟超时（§8.5）——如果队列正常积压超过 5 分钟呢？会不会把排队中的正常任务误杀？
- LLM-A 任一批失败即整任务失败（§7.2）——4 批并发时，第 1 批失败而其余 3 批还在跑，谁负责置终态？会不会重复置？
- `XADD` 在事务内（§5.2）——Spring 事务里调 Redis，如果 Redis 阻塞 30 秒，MySQL 事务和连接池会怎样？
- 心跳独立线程（§7.2）——主流程抛异常后，心跳线程会不会泄漏？

### 4.4 数字与枚举的全局一致性 —— 改过多轮，很可能有残留

过敏原枚举从 12 组改成 11 组、打标维度从 21 改成 20、正式枚举从 35 改成 34，
涉及 5 个文件。**请逐一核对**，把对不上的列出来：

```
开发方案  §9.1 维度表 / §10 常量表 / §11 测试清单
设计方案  §7.2 枚举清单 / §8.2 维度表 / §8.3 成本估算
schema/llm_a_output.schema.json   allergenKey 的 enum
schema/llm_b_output.schema.json   enumKey 的 enum 与 allOf 条件
prompt/llm_a_extract.md           枚举表
prompt/llm_b_dish_tag.md          维度数量描述
constants/内容常量草案.md          §1 表格与 §7 工程侧常量
allergen_display_split.csv        实际词条
```

### 4.5 MyBatis-Plus 与 Java 8 的 API 正确性

请核实全文的 API 用法是否真实存在且签名正确：

> `@TableField(insertStrategy = FieldStrategy.NEVER, updateStrategy = FieldStrategy.NEVER)`
> 已经过确认，是正确写法，**不需要再核**。

其余请逐项核实：
- 是否出现 Java 8 不支持的语法（`var`、`List.of`、`Optional.isEmpty`、`String.isBlank`、text block）
- `StreamRecords` / `PendingMessages` / `xAck` 等 Spring Data Redis 2.7.x 的 API 签名是否正确
- `@XxlJob` 注解与 xxl-job-core 版本的匹配
- `ZipSecureFile.setMinInflateRatio` / `IOUtils.setByteArrayMaxOverride` 在 POI 4.1.x / 5.2.x 的可用性
- `ImageIO.getImageReaders` 两阶段解码的写法是否真能避免全量解码

### 4.6 上一轮评审的 16 条是否真的关闭

`AI体检报告分析-精简设计方案V1-开发评审.md` 的 §3.1~§3.16、§4、§5、§6。
**逐条核对本版是否真正解决**，不是「提到了」而是「给出了可实现的规则」。

特别留意作者**没有照搬**评审建议的两处，判断替代方案是否成立：

- 评审 3.7 要求投递恢复（outbox 或补投），作者改成「QUEUED 超时置 FAILED」
- 评审 3.12 要求逐条证据覆盖，作者实现了，但只对「含阳性标记的过敏章节 segment」做，
  漏掉「过敏章节里没有阳性标记但模型漏抽了整段」的情况——这是否是个缺口？

## 5. 常规检查维度

在上述重点之外，按以下维度扫一遍：

| 维度 | 关注点 |
|---|---|
| 需求符合性 | 逐条对照 `体检报告分析需求.md`，找出未实现或偏离而未在 §13 声明的 |
| 安全 | 过敏链路是否有 fail-open 路径；健康数据是否有意外持久化路径；日志白名单是否有漏 |
| 契约完整性 | 开发能否只看文档写出前后端联调所需的全部 DTO 与错误码 |
| 状态机 | 是否存在无法到达或无法退出的状态；并发下是否有非法迁移 |
| 事务边界 | 跨 MySQL/Redis/S3 的操作，失败补偿是否定义清楚 |
| 可测性 | §11 的测试清单是否每条都能写成确定性用例；有没有需要真实样本才能测但被列为单测的 |
| 内部矛盾 | 同一规则在不同章节的表述是否一致 |

## 6. 输出格式

```markdown
## 结论
[能否作为开发基线] + 三句话理由

## P0 必须修复
### 问题标题
**位置** 文件 §章节 / 行号
**问题** 客观描述，引用原文
**后果** 照现在写会产生什么具体错误。给出触发场景或输入
**建议** 可实现的修法，不是"建议考虑"

## P1 应当修复
（同上格式）

## P2 建议
（可简写）

## 上一轮评审关闭情况
| 编号 | 主题 | 本版是否解决 | 依据 |

## 数字与枚举一致性核对
| 位置 | 期望值 | 实际值 | 是否一致 |
```

## 7. 什么不算发现

以下内容**不要写进报告**，它们会稀释真正的问题：

- 复述文档已有内容
- 「建议增加单元测试」「建议完善注释」「建议加监控」这类无位置、无具体缺陷的泛化建议
- 重开第 3 节列出的已定决策
- 拿废弃的 `V1_7_1.md` 当判据
- 指出「常量内容为空」——这是已知状态，文档里已声明待医务填写
- 指出「五个占位符未实现」——这是刻意设计
- 指出「没有前端代码」——不在范围内
- 建议引入新框架、新中间件、新数据库
- 风格偏好（命名习惯、目录层级、注释语言），除非违反 `AGENTS.md` 明文

## 8. 态度要求

**宁可少报，不可虚报。** 一条能复现的具体缺陷，价值高于二十条「建议考虑」。

如果你检查了某个重点区域后**认为它没问题**，明确说出来并给出你的判断依据
——「§4.2 的可见性协议我构造了 5 个时序，都没能证伪，理由是 X」比沉默有价值得多。

如果你**不确定**（例如没法验证某个第三方 API 的真实签名），标注为「需实测确认」，
不要猜一个答案写成结论。
