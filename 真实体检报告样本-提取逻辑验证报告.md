# 真实体检报告样本—提取逻辑验证报告

> 验证对象：用户指定的两份真实 PDF 体检报告  
> 验证日期：2026-08-24  
> **唯一开发评审基准：`AI体检报告分析-开发方案V1.md`**  
> 同步核对：`AGENTS.md`、`prompt/extraction.md`、`schema/extraction_output.schema.json`  
> 隐私说明：本报告不记录姓名、联系方式、检查号、具体检验值或其他可识别个人的信息。

## 1. 基准纠正说明

`AI体检报告分析-开发方案V1.md` 在 §0.1 已明确规定文档优先级，并写明：

> 旧文档 `AI体检报告分析与菜品推荐-完整技术方案V1_7_1.md` 仅供历史追溯，不再是任何判据。

因此，本报告不使用完整技术方案 V1.7.1 中的 DOC-VL、页面角色、单元格级交叉校验、复杂坐标体系等设计作为开发要求。

本次按开发方案 V1 的实际链路评审：

```text
Java 逐页判定 NATIVE/OCR
  → 产生带 segmentId 的逻辑行
  → 文本 segment + 页面图像一起给 LLM-A
  → LLM-A 返回局部抽取 + segmentId
  → Java 做 Schema、segmentId、简单包含性、去重排序和安全兜底
```

## 2. 总结论

**开发方案 V1 能覆盖两份报告的基本文件解析和大部分抽取链路，但按当前 Prompt/Schema 直接开发，仍会在这两份真实报告上出现明确的漏抽和误抽风险。**

当前结论为 **NO-GO**，但不需要把 Java 复杂化。修改应集中在 OCR/LLM-A 契约上，Java 仍只做简单、确定性判断。

必须修复的核心问题：

1. 已实测 OCR 能正确识别样本 B 的异常箭头，但当前 `NATIVE/OCR` 二选一路由不会对该原生文本页调用 OCR，仍需修正路由；
2. 样本 A 大量正常指标的明确结论在分组“小结”中，当前 Schema 不支持“数据行 segment + 小结 segment”两段证据；
3. Prompt 没有全局硬性规定“医学影像/波形本身必须忽略”；
4. 样本 B 同一批次同时包含真实检查、科普、推广、二维码和心电图，当前 `batchStatus` 和字段规则不足以防止科普被误抽成个人结论；
5. 当前 Prompt 的饮食建议抽取范围会漏掉样本 B 专科页中明确针对用户的饮食建议。

## 3. 两份报告在开发方案 V1 下的实际路由

### 3.1 样本 A：18 个 PDF 物理页

按 §6.4 的“字符数至少 50 且非空白占比至少 30%”逐页规则：

- 第 1–15 页：有足够原生文本，走 `NATIVE`；
- 第 16 页：原生文本只有少量页脚，走 `OCR`；
- 第 17 页：正文是嵌入式文字报告图片，原生文本只有页脚，走 `OCR`；
- 第 18 页：有足够原生文本，走 `NATIVE`。

这说明开发方案 V1 已经正确覆盖“原生 PDF + 后部图片页”的混合报告，不需要 Java 再增加页眉页脚语义识别或正文覆盖率算法。

对图片页的正确处理是：

- 纯波形/医学影像页：OCR 可识别其中文字，但 LLM-A 不得根据波形或影像生成诊断；
- 图片式文字检验报告：OCR 识别表格和书面结论，LLM-A 可抽取；
- 附件项目核对页：不产生新的指标或健康问题。

### 3.2 样本 B：23 个 PDF 物理页

该 PDF 绝大多数页面（包括图文页和最后的旋转附件）都有超过阈值的原生文本，因此按当前规则主要走 `NATIVE`。虽然 OCR 已实测能识别图形异常箭头，但现有二选一路由在这些页上不会调用 OCR。

这条路径会暴露三个真实问题：

1. 文本层中有 78 种 CJK/康熙部首污染字符，必须经过 `TextNormalizer`；
2. 部分表格的红色异常箭头可见于页面图像，但原生文本层中没有对应字符；
3. 后部批次同时有真实检查页和非个人化科普/推广页。

## 4. 已满足的部分

| 能力 | 样本表现 | 结论 |
|---|---|---|
| 格式、大小、页数 | 两份 PDF 都小于 20MB、页数小于 30 | 通过 |
| PDF 逐页文本层判定 | 样本 A 可同时产生 `NATIVE` 与 `OCR` segment | 通过 |
| 图片式文字报告 | 样本 A 的嵌入式检验页会因原生字数不足自动走 OCR | 通过 |
| 原始顺序 | `segment.seq + itemIndex` 可保存页内/文件内顺序 | 基本通过 |
| 报告自带总览 | 样本 B 有自带总指标数与异常项数 | `reportOverview` 可支持 |
| 影像旁边的书面结论 | 两份都有检查所见/诊断意见文字 | `textualFindings` 可支持 |
| 多文件同一性 | 两份分别单独上传时不触发冲突 | 通过 |

## 5. P0 问题

### P0-1 OCR 已能识别图形异常箭头，但当前路由不会在 `NATIVE` 页调用 OCR

已有实测结果确认：**PaddleOCR 可以正确识别样本 B 表格中的图形异常箭头。** 因此箭头识别能力不再是问题，也不需要 Java 判断图形。

剩余问题是开发方案 §6.4 的二选一路由：样本 B 的表格页会被判为 `NATIVE`，于是该页不调用 OCR。LLM-A 虽然同时看到页面图像，可以读出箭头，但原生文本 segment 中没有对应字符。

现有 §7.5 要求指标的五个字段都必须是同一 `NATIVE` segment 的严格子串，因此：

```text
LLM-A 从图上正确读到 conclusionText = "↑"
→ 原生 segment 不含 "↑"
→ 五字段包含性失败
→ 该指标整条丢弃
```

这是路由契约问题，不是 OCR 能力问题。

**简化修正建议：将逐页 `NATIVE/OCR` 二选一改为 OCR 全页执行，原生文本可用时同时保留。**

```text
每个 PDF 页都渲染并调用 OCR
  → 产生 OCR segment

原生文本达标的页
  → 同时产生 NATIVE segment

LLM-A 同时看页面图像、NATIVE segment 和 OCR segment
  → 带异常箭头的指标返回字段齐全的 OCR segmentId
```

Java 继续使用已有逻辑：

- segment 是 `NATIVE` 则严格子串校验；
- segment 是 `OCR` 则使用 OCR 档包含性校验；
- Java 不识别箭头、不检测表格页、不增加 `conclusionSource`。

这会增加 OCR 调用量，但 V1 实现最简单，也符合“OCR 负责看清、LLM-A 负责看懂、Java 只做简单校验”的职责边界。

### P0-2 样本 A 的分组小结与数据行分属两个 segment，当前 Schema 无法表达

样本 A 的典型结构是：

```text
数据行 segment：指标名 + 数值 + 单位 + 参考范围，提示列为空
小结 segment：“小结：未见异常”或点名某些异常项
```

当前 Prompt 要求“有数值 + 有结论”才进入 `indicators`，又要求所有字段都可在同一 `segmentId` 中找到。因此大量分组小结已明确判为正常的指标无法进入模块一。

样本 A 又没有自带 `reportOverview`，§8.1 会用最终卡片自行统计总览，可能将一份大部分正常的报告显示为“卡片全是异常”。

**不要让 Java 理解小结。** 由 LLM-A 判断小结对哪些指标生效，Schema 只增加第二个证据定位：

```json
{
  "segmentId": "指标数据行",
  "conclusionSegmentId": "分组小结行",
  "conclusionText": "小结中的报告结论"
}
```

Java 只检查：

- 指标四个数据字段出现在 `segmentId`；
- `conclusionText` 出现在 `conclusionSegmentId`；
- 两个 segment 都存在且属于同一文件。

是否属于同组、小结是否覆盖本指标，由 LLM-A 负责，Java 不做医学/文档语义推理。

### P0-3 Prompt 没有明确禁止根据医学影像或波形做结论

两份报告包含超声图、CT 图、脑血流波形和心电图波形。这些页面图像会与 segment 一起送给 LLM-A。

当前 Prompt 只写了“不生成、不推断”的总原则，但没有把医学影像这一高风险边界写死。

应在 System Prompt 增加硬规则：

```text
禁止根据超声图、CT/MR 影像、X 光图、心电图、脑血流图或其他医学影像/波形本身产生任何诊断或结论。
只能抽取报告上明确印刷的“检查所见、诊断意见、初步意见、小结”等文字。
图片本身就是文字/表格报告的，可抽取 OCR 识别到的书面内容。
```

这条由 LLM-A 执行，Java 不做影像分类。

### P0-4 批次内混有真实检查和非个人化内容时，`batchStatus` 无法防误抽

样本 B 的第三批（按每批最多 8 页）同时包含：

- 真实专科检查页；
- 产品/疫苗推广页；
- 健康咨询服务说明；
- 二维码页；
- 医学名词科普；
- 旋转心电图附件。

整批必须返回 `OK`，因为其中确实有真实检查内容。但 `OK` 不能防止模型把后续科普文章中的疾病名或建议当成用户结论。

不需要 Java 增加页面角色算法。在 LLM-A Prompt 中增加全字段排除规则：

```text
封面、体检须知、广告、产品/疫苗介绍、通用医学科普、咨询服务说明、二维码页，
不得向 indicators、textualFindings、summaryConclusions、allergens、
nutritionSupplements 或 dietRequirements 输出任何条目。
它们与真实报告页处于同一批时，batchStatus 仍为 OK，但必须逐页忽略。
```

## 6. P1 问题

### P1-1 当前 Prompt 会漏掉样本 B 专科页中的明确饮食建议

`prompt/extraction.md` 将 `nutritionSupplements / dietRequirements` 的抽取范围限定为“总检结论 + 医生建议章节”，并要求各科小结的顺带提及不抽。

样本 B 的专科检查页中有针对本次检查结果的明确饮食建议，按当前 Prompt 容易被排除。

需要明确产品口径：

- 如果产品只接受“总检/专家建议”，则当前 Prompt 可保留，但该专科建议就是明确不支持；
- 如果要提取报告中所有针对本用户的明确饮食建议，则应改为由 LLM-A 判断“个人化建议 vs 科普/临时检查准备”，Java 不判断。

### P1-2 OCR 的“按 Y 坐标聚类成行”不应由 Java 承担

开发方案 §6.3 要求 Java 对 PDF 字符和 OCR block 做 Y 坐标聚类，其中 OCR 还写了“规则同 PDF”。

根据当前已确认的职责边界，Java 不应重建 OCR 阅读顺序或表格行：

- PaddleOCR 应直接返回逻辑行/文本块及其顺序；
- 如 OCR 服务能返回 Markdown/HTML 表格，直接将结构化行包装为 segment；
- Java 只负责为 OCR 结果分配 `segmentId`，不做栏、行、表格的语义恢复。

PDF 原生文本如果仍保留，可以使用 PDFBox 自身的行输出做辅助 segment；表格是否对齐交给页面图像 + LLM-A，不在 Java 中写通用表格算法。

### P1-3 部首映射方向正确，但完整表未固化

样本 B 在本地提取中检出 78 种 CJK/康熙部首字符。NFKC 可以处理康熙部首，但 CJK 部首补充区仍需要手工映射。

开发方案和精简设计方案只写了“约 30 条”，没有一份可直接开发的完整常量。

需固化：

```text
RADICAL_FIX 完整映射表
样本 B 全文规范化后部首区残留数 = 0
```

这只是字符替换表，属于 Java 可以承担的简单确定性处理。

### P1-4 旋转心电图页需要回归测试，不需要 Java 医学理解

样本 B 最后一页带 PDF `/Rotate=270`。PDFBox 渲染时应正常应用该页面旋转标记，因此 V1 不需要建立复杂 bbox 坐标体系。

只需固化两条验收：

1. 渲染后给 LLM-A 的页面方向正确；
2. 因该页没有明确印刷的诊断结论，LLM-A 不输出任何自行心电诊断。

## 7. Java、OCR 与 LLM-A 的修正职责边界

### 7.1 Java 只保留简单确定性工作

- 文件格式、大小、页数和解压安全校验；
- PDF 逐页渲染；
- 保留达标的原生文本，并对每个 PDF 页调用 OCR；
- 为文本行/OCR 逻辑块分配 `segmentId`；
- LLM-A JSON Schema 校验、segmentId 存在性校验；
- 字符规范化和简单子串包含性校验；
- 按 `fileIndex/page/seq/itemIndex` 排序和按明确 ID 去重；
- 姓名/性别简单一致性比较；
- 任务状态、超时、删除和数据生命周期。

### 7.2 OCR 负责看清和文档结构

- 图片文字识别；
- 输出逻辑行/文本块和阅读顺序；
- 如能力支持，输出表格 Markdown/HTML；
- 保留异常箭头、单位和参考范围；
- 输出置信度供 LLM-A 参考。

### 7.3 LLM-A 负责看懂

- 区分真实检查、针对用户的建议、科普和推广；
- 识别章节标题；
- 将分组小结与具体指标关联；
- 从页面图像读取图形化的报告结论标记；
- 抽取指标、文字结论、总检结论、过敏原和饮食建议；
- 严格忽略医学影像/波形本身，只读取书面文字结论；
- 返回准确的 `segmentId` 和可选 `conclusionSegmentId`；图形异常标记优先定位到字段齐全的 OCR segment。

## 8. 应固化的样本验收断言

### 8.1 样本 A

1. 所有页产生 OCR segment；第 1–15、18 页同时保留 NATIVE segment，第 16、17 页只使用 OCR segment；
2. 纯波形页输出 0 条模型自行诊断；
3. 图片式文字检验报告页能抽取书面检验结果；
4. 影像页只抽取报告明确印刷的检查所见/诊断意见；
5. 分组小结已明确判为正常的数值指标可通过 `conclusionSegmentId` 进入指标模块；
6. 附件核对页不产生新指标、健康问题或饮食建议；
7. 样本没有过敏原筛查表，不得把过敏相关疾病文字当成食物过敏原。

### 8.2 样本 B

1. `TextNormalizer` 处理后非标准部首残留数为 0；
2. 报告自带的总指标数/异常数正确进入 `reportOverview`；
3. OCR 能正确识别图形异常箭头，LLM-A 返回对应 OCR segmentId，指标通过 OCR 档包含性校验；
4. 异常概览和明细表的重复内容不生成重复健康问题；
5. 专科页中针对用户的明确饮食建议按最终产品口径决定是否抽取；
6. 推广、产品介绍、咨询项目说明、通用医学科普和二维码页对六个业务数组均输出 0 条；
7. 旋转心电图页渲染方向正确，且不根据波形输出诊断；
8. 样本没有过敏原筛查表，`allergens=[]`，不得因报告中出现过敏相关疾病名而产生过敏原条目。

## 9. 修改顺序

1. 修改开发方案 §6.4：所有 PDF 页都生成 OCR segment，原生文本达标时同时保留 NATIVE segment；
2. 修改 LLM-A Prompt：字段齐全的 OCR segment 可作为指标定位，不增加 `conclusionSource`；
3. 修改 LLM-A Schema：只为分组小结场景增加可选 `conclusionSegmentId`；
4. 修改 LLM-A Prompt：增加医学影像禁止推断、科普/推广全数组排除、分组小结关联规则；
5. 确认专科页饮食建议是否纳入产品范围，同步修改 Prompt；
6. 调整 §6.3：OCR 逻辑行/表格结构由 OCR 服务提供，Java 不做 Y 坐标表格恢复；
7. 固化完整 `RADICAL_FIX` 映射表；
8. 用本报告 §8 的断言对两份 PDF 整本跑端到端回归。

## 10. 验证边界

本次使用真实 PDF 完成了页数、原生文本量、图片层、旋转标记、抽取文本顺序与开发契约的静态回放评审。

当前仓库内没有可直接运行的 PaddleOCR、Dify LLM-A 和 Java 实现，因此本报告不等于端到端准确率测试。修改 Prompt/Schema 后，仍需整本运行并与脱敏人工标注逐条比对。
