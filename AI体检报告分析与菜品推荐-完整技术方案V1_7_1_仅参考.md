# AI 体检报告分析与菜品推荐 — 完整技术方案 V1.7.1

> 技术栈：Java 8 / Spring Boot 2.7.x / MyBatis-Plus / PDFBox / Apache POI / ofdrw / TinyPinyin /
> MySQL / Redis（Stream 队列 + 缓存） / xxl-job / Dify
>
> 生效日期：2026-08-23　状态：已确认
> **本文件是自包含的完整方案**，合并了 V1.6.4 基线与 V1.6.5 delta 的全部内容，并引入 V1.7 的
> 身份边界变更。V1.7 及更早版本自本版起仅用于历史追溯，**不再作为任何判据**。

---

## 版本说明

### V1.7.1 是一致性修正版，外加一处行为修正

与 V1.6.1 同性质（见 §19.2）：绝大部分改动是把文档内部互相矛盾、或与数据真源不一致的地方拉齐，
**没有任何产品决策、架构或接口语义的变更**。

**例外只有一处，但它改变实际行为，必须单独知会：** §4.4b 与 §4.5 的校验门槛由
`mode == IMAGE` 改为 `textLayerSource == NONE`（§19.V1.7.1 第 35 条）。
V1.7 的写法会让**拍照件在校验入口被直接短路** —— 而 §3.1 规定 `image/*` 整份走 IMAGE，
于是拍照场景下 DOC-VL 建立的文本层根本不会被使用，§3.11「关闭拍照缺口」的结论不成立，
幻觉引用（§1.1 失败 2）在预期的主流形态上无人把守。修正后拍照件正常参与校验。
**已按 V1.7 开工的实现无需返工**，但下列三处必须按本版核对，因为 V1.7 的原文会导向错误实现：

| # | V1.7 的问题 | 后果 | 本版 |
|---|---|---|---|
| 1 | §15.4 的单测注释要求"未命中的未知过敏原留在推荐列表带横幅" | 与 §8.4 的结论完全相反。照此写出的单测会把 fail-open **测成通过** | §8.4 口径：转入「过敏成分待确认」 |
| 2 | §8.4 的 fail-safe 伪代码只 `setUnverifiedAllergen`，从不写入 `allergenUncertain` | 照抄即为 fail-open：该菜仍会走到 `merge()` 的 `RECOMMENDED` 分支 | 补入集合，并在 §8.6 `merge()` 增加第三列表分支 |
| 3 | 证据用 `fileSeq`（1 起）、预览接口用 `fileIndex`（0 起），全文无换算 | 差一错误 = **点开原图看到另一份报告的页**，而本系统在外部鉴权层之外无第二道防线 | 全案统一 `fileIndex`（0 起） |

**过敏关键词族本版一个字都没动。** §7.4 的示例此前与 `docs/allergen_display_split.csv`
和仓库常量不一致（示例多出 `蛋`、`沙拉酱`、`焗`、`芝士焗`），本版把**示例**改为逐字照抄 CSV。
词表本身的增删属安全变更，须单独走 `OPEN_QUESTIONS.md` H.2 的流程，
其中「单字 `蛋` 不在词表内」这一覆盖边界已显式写入附录能力边界。

改动全清单见 §19.V1.7.1。

---

## V1.7 版本说明（内容继续有效）

### 本版相对 V1.6.5 的两项实质变更

1. 系统不再持有任何用户账号或外部身份标识。
2. 上传阶段只产生 `fileId`；调用“体检报告总结”接口时才提交有序 `fileIds`、创建 `taskId`
   并关联文件。

**身份边界变更（V1.7 原文，V1.7.1 已部分回退，见下表）：** 访问鉴权整体移出本系统边界，
由外部鉴权层承担；本系统**不存储、不接收、不返回任何用户账号或外部身份标识**，包括原 `ownerId`。体检报告、预览图和分析结果仍是敏感健康数据，
继续执行本方案规定的最小化、隔离和 TTL 规则，不得把“无用户标识”误写成“无敏感数据”。

具体废止项：

> ⚠️ **下表是 V1.7 的历史记录。V1.7.1 已定向回退其中四项** —— 归属校验重新回到本系统内部，
> 只是字段名由 `ownerId` 改为 `userId`。当前口径以 §9.1.2、§9.1.1a、§13.1.2 与
> `OPEN_QUESTIONS.md` I 为准，不要照本表实现。

| 原设计 | V1.7 | V1.7.1 |
|---|---|---|
| `task:{taskId}` Hash 中的 `ownerId` Field | **删除** | **恢复**，改名 `userId`（§9.1.2） |
| `health_report_file.owner_id` 列 | **删除** | **恢复**，改名 `user_id`，非空（§9.1.1a） |
| 原子入队 Lua 写入 `ownerId` | **删除该 ARGV 与 HSET 字段** | **恢复**，ARGV[5] = `userId`（§11.1） |
| 确认接口 Lua 校验 `ownerId` | **删除该分支** | 接口本身已随 `NEED_CONFIRM` 取消（§4.0a），不恢复 |
| §9.1.2「`ownerId`：taskId 难猜 ≠ 权限校验」 | **整节废止**，改为 §13.1.2 对外部鉴权层的强制要求 | 该节以 `userId` 口径重写 |
| §13.1.2 中的「结果查询接口校验 `ownerId`」 | **废止**，改为外部层职责 | **恢复**，四类端点全部校验归属（§13.1.2） |
| 应用内 `Principal` / 认证机制 | **不实现**，接口不接收身份参数 | 认证仍不实现，但**接口接收已认证的 `userId`**（当前为占位符） |

**未变更项：** 除上述上传建任务时机外，其余文件校验、解析、LLM-A、身份一致性校验、证据回溯、过敏硬兜底、任务状态机、
TTL、离线预热与激活、在线维度级判定、四模块组装、数据生命周期规则**全部继续有效**。

### 这项变更的安全后果 —— 必须由外部层兜住

V1.6.4 §9.1.2 曾明确写过一句判断：**「UUID 难猜是混淆，不是鉴权」**。本版移除 `ownerId` 之后，
本系统内部**不再具备任何自验能力** —— 一个持有 `taskId` 的请求，系统无法区分它来自报告的上传者
还是任意第三方。

因此下列保证**从本系统转移到外部鉴权层**，它们不是建议，是本方案成立的前提（详见 §13.1.2）：

1. 首次逐份上传只返回 `fileId`，不创建 `taskId`，也不建立用户或任务关联；真正调用“体检报告总结”
   接口时提交有序 `fileIds`，本系统此时才创建 `taskId` 并关联文件。外部层可从该响应开始维护
   「`taskId` ↔ 用户」绑定，并在后续请求到达本系统之前完成校验。
2. 绑定必须覆盖创建任务后的分析、状态、结果、确认、原图和清理端点；上传端点没有 `taskId`，
   不属于任务归属校验范围。
3. `taskId` 允许作为技术标识进入应用日志以便排障，但不得与患者姓名、报告/OCR/证据文本、
   原始过敏或医嘱文本、模型完整请求响应共同记录，也不得出现在 URL 查询串或分享链接中。

> **本方案不对"外部层是否真的做到了"提供任何补偿机制。** 这是本版为换取“系统不持有用户账号或外部身份标识”
> 所付出的确定代价，写在这里是为了让它成为一个被知情的决定，而不是一个被遗忘的缺口。

### 相对 V1.6.5 的收益

移除 `ownerId` 后，本系统**在任何存储介质中都不再存在用户账号或外部身份标识**：

- MySQL 两张表：`diet_advice_cache` 是跨用户复用的公共内容；`health_report_file` 只剩
  S3 定位、任务关联、文件技术元数据与过期时间，不含用户账号或外部身份字段。
- Redis `task:{taskId}`：只剩状态机字段与 2 小时后自动消失的分析结果。
- 日志白名单（§13.1.1）**再减一项** —— 原先 `ownerId` 属于允许记录的技术字段，现在它不存在。

结合 §13.1「原文件成功即删、预览图与结果 2 小时失效」，本系统对个人信息的留存面收敛为
**「仅在 2 小时窗口内、且不含任何身份标识的报告分析结果」**。

---

## 0. 设计原则

> **能程序判定的，不交给模型；程序无法可靠判定的，也不要伪装成已经判定。**
>
> **原文即事实。** 展示层严格复刻报告原文，模型负责"定位与分类"，不负责"生成与推断"。
>
> **按风险分层，不按环节一刀切。** 安全红线用确定性规则兜底；语义判断交给模型；
> 模型判断过的东西沉淀成缓存，不要每次重赌一遍。

推论：

1. `UNKNOWN` / `UNCERTAIN` / `CONFLICT` / `NOT_SUPPORTED` / `BLOCKED` 是**正常业务状态**，不是异常分支。
2. **`UNKNOWN ≠ NONE`。** "模型没告诉我有没有过敏原"和"报告说没有过敏原"是两件事，
   系统在任何一层都不得把前者折叠成后者。
3. 数值的语义依赖二维版面，一维文本抽取会丢失行列关系，而模型**无法察觉自己拿错了**。
4. 缺数据不能静默放行 —— 有硬约束时，`UNKNOWN` 按"不安全"处理。
5. 凡是要展示为"报告原文"的字段，必须能回溯到解析产物中的真实片段（§4.5）。回溯不上就不是原文。
6. **不确定性只在写入时消费一次。** 同一道菜在同一条建议下的判定，第一次由模型给出，
   之后由缓存给出 —— 模型的波动只影响一次写入，不影响每次读取。
7. **安全判定必须发生在归一化之前。** 归一化会把无法识别的输入折叠成 `OTHER`，
   而 `OTHER` 恰恰是最需要被安全规则拦住的那一类。

### 阅读约定：代码块是伪代码

全文的 Java 代码块用于**表达判定顺序与失败方向**，为保持判定链的对齐可读性，
大量使用了单行 `if (...) return X;` 的紧凑写法。**它们不是可直接抄进仓库的源码。**

落地时必须按 `AGENTS.md` §8 的工程约定改写，其中至少包括：

- **所有控制流语句体一律加花括号**，包括单语句的 `if`/`else`/`for`/`while`；
- 不得使用 Java 8 以上的语法与 API（`var`、records、`Optional.isEmpty()`、
  `Map.of`、`Stream.toList()`、`String.isBlank()` 等）；
- 补齐中文类/方法注释、`@Slf4j` 日志与 Lombok 访问器。

> 这条写在最前面，是因为本文档在实践中确实被当成抄写模板用。
> 判定**顺序**和**每个分支的失败方向**必须逐字照搬，**书写形式**不要照搬。

---

## 1. 核心约束与不可接受的失败

### 1.1 五条不可接受的失败

任何一条发生，即视为线上事故，需回归验证：

| # | 失败 | 守护机制 |
|---|---|---|
| 1 | 静默输出错误的化验值 | §4.4a 结论/数值交叉校验、§4.4b 行级联合校验 |
| 2 | 引用了报告中不存在的原文（幻觉引用） | §4.5 原文回溯校验，回溯不上整条丢弃 |
| 3 | **含过敏原、或过敏核验未完成的菜品出现在推荐列表** | §8.3 Java 硬兜底 + §8.4 未知过敏原转第三列表 + §8.6 强制改判，模型不可推翻 |
| 4 | **把"模型没返回"解释成"报告里没有"** | §4.0 严格 Schema，缺字段 ≠ 空数组 |
| 5 | **把不属于同一个人的多份文件合成一份健康画像** | §4.0a 同一性校验，不一致直接失败，不自动合并 |

### 1.2 安全不对称

过敏是全案唯一的安全红线，误判代价高度不对称：

```
漏标（含过敏原的菜进了推荐列表）→ 可能造成严重过敏反应
误标（不含过敏原的菜进了不推荐列表）→ 用户少一个选择
```

因此过敏是**唯一不交给模型的维度**，由 Java 关键词族硬匹配，且一律偏向高召回：

| 项 | 其他维度（LLM） | 过敏维度（Java 硬兜底） |
|---|---|---|
| 判定方式 | 模型语义判断 | 关键词族字符串匹配 |
| 重量占比阈值 | 由模型按语义把握 | **无阈值，微量即命中** |
| 匹配范围 | 主料为主 | **菜名 + 主料 + 配料全文** |
| 关键词族未收录的过敏原 | — | **不得判为安全**（§8.4） |
| 报告未做过敏原筛查 | — | **无兜底可用**，见下方能力缺口 |
| 配料表缺失 | 按未命中处理 | **不得判为安全**，单独标注（§8.9） |
| 模型能否推翻 | — | **不能** |

**必须向业务方讲清的能力缺口：过敏保护完全依赖报告。**

产品确认不做"用户自填过敏原"，因此：

```
报告做了过敏原筛查  → Layer 1 正常运行
报告没做（NOT_SCREENED，这是体检报告的常态）→ 系统对用户过敏情况一无所知
                                            → 且用户没有任何补救途径
```

系统在这种情况下**不会犯错**（它从未声称核验过过敏），但也**提供不了保护**。
唯一的兑现方式是 §4.0.3 的模块级提示：
「本次报告未包含过敏原检查，推荐结果未考虑过敏因素。如有已知过敏，请自行确认菜品配料，
并向食堂工作人员确认」。

**文案中不得出现"请在设置中补充"之类的引导** —— 本版没有这个入口，
指向一个不存在的功能比不提示更糟。

> 这条提示不能省，也不能弱化措辞。它是这个产品决策唯一的安全出口。

### 1.3 所有词表与阈值硬编码在代码里，不做配置化

**全案不引入任何运行时可改的规则配置**（不进数据库、不进 Nacos/Apollo、不进配置文件）。
归一化词表、别名表、单位白名单、过敏关键词族、安全黑名单、阈值常量，
一律写成 `static final` 常量或 `enum`，改动走发版。

```java
// 集中在少数几个常量类中，便于 review 与单测覆盖
ConclusionLabelWords     结论归一化词表（§4.3）
SectionSummaryMatchWords 小结点名判定的松紧阈值（§4.1，断言严格 / 否定宽松）
IndicatorAliasWords      指标别名与单位白名单（§4.6）
AdviceKeyAliasWords      建议键别名（§7.2）
SafetyPatterns           Tier-0 安全黑名单（§7.2）
AllergenKeywords         过敏关键词族（§7.4）
AllergenSectionWords     过敏原检查章节名词表（§4.0.1a，未命中即判未筛查，方向保守）
IdentityMatchConstants   姓名容错阈值（§4.0a，**唯一方向放松的常量**，需标注集校准）
DishRuleConstants        调味料表、重量阈值、批大小（§8）
DisclaimerConstants      四个声明 + 全部空态文案
```

**这么做是安全的，前提是所有词表的"未命中路径"都必须保守**，且这一点已在各处落实：

| 词表 | 未命中时 | 后果 |
|---|---|---|
| 结论归一化 | `UNRECOGNIZED` → **计入异常**，原样展示报告结论 | 多展示一条问题，不会漏报 |
| 单位白名单 | `UNVERIFIED` → 放弃数值校验 | 少一道校验，展示不受影响 |
| 指标别名 | `code = UNKNOWN` → 仍完整展示 | 仅影响去重与单位校验 |
| 建议键别名 | `OTHER` → 只展示原文与未收录提示，只计数 | 不落库、不调用 LLM-B、不参与菜品匹配 |
| **过敏关键词族** | **走 §8.4 fail-safe，不判为安全** | 保守 |
| **安全黑名单** | 叠加模型 `safetyFlag` 或运算兜底 | 双保险 |

**因此发版滞后不会产生错误结果，只会让结果更保守。** 这是"词表可以硬编码"的全部依据 ——
如果某张词表未命中时会走向"更宽松"，那张表就不能硬编码，必须改设计。

**词表的更新闭环靠脱敏计数而非配置：** 各处只记录
`unrecognizedConclusionCount` / `adviceKeyMissCount` / `unknownAllergenCount`，不记原文。
计数升高时由具备权限的人员抽查真实报告定位待补词条，随下一次发版补进常量；
仅凭计数不能生成原文 Top N。这比"线上随手改配置"更可控 ——
词表变更会经过代码 review、单元测试和灰度，而配置变更通常什么都不经过。

### 1.4 三个"不做"（需求 §7-5 的硬约束）

1. **不从指标异常推导饮食建议。** 甘油三酯偏高 ≠ 低脂饮食，只有报告明文写了才生成。
2. **不合并同向建议。** 「低脂饮食」与「控制体重」各自成卡片，各自引用各自原文。
3. **不在饮食建议中引用指标数据。** 不出现「因您的甘油三酯 2.8」这类表述。

> 守法方式是**不给模型关联所需的数据**：LLM-B 的输入只有 `adviceKey`，
> LLM-C 的输入只有菜品与单个建议内容，两者结构上都看不到指标。不靠提示词嘱咐。

### 1.5 需求边界的直接后果（需业务方知情）

一份"甘油三酯 3.5↑↑、但总检结论只写了'建议复查'"的报告，**不会产生任何饮食建议和菜品推荐**，
只展示健康指标与健康问题。这是需求 §7-5 的明确选择，建议上线前用样本估算该类报告占比（§16 #11）。

---

## 2. 整体链路

系统有两条链路：**在线链路**（用户请求触发）与**离线链路**（每日预计算 + 内容校验），
两者通过缓存表解耦。稳态下在线只有一次模型调用。

### 2.1 产品视角：三步

```
① 用户逐份上传体检报告，每份只返回 fileId；点击“分析体检报告”时提交有序 fileIds
        │
        ▼
② AI 分析（文件解析 → 结构化抽取 → Java 校验 → 饮食建议 → 菜品匹配）
        │
        ▼
③ 返回四模块分析结果
```

**技术上是异步的（每次上传只返回 `fileId`，不创建 `taskId`；点击分析时提交 1～5 个有序
`fileIds`，后端才创建并返回 `taskId`、关联文件并入队，前端随后轮询结果），
但产品语义仍是这三步。**

不要为了"看起来同步"改成一次 HTTP 请求等到底：LLM-A 单次超时就是 120~180s，
Worker 总执行上限 15 分钟，同步 HTTP 会撞上 Nginx 超时、网关超时、
客户端切后台、移动网络断连、负载均衡断开 —— 每一条都会让用户白等一场。

下面 §2.2~§2.4 是第二层的技术实现视图。

### 2.2 技术视角：在线链路

```
App
 │  逐份 upload（每次一个文件）
 ▼
Java API
 ├─ MIME(magic number) / 单文件大小校验
 ├─ 存 S3 私有 Bucket
 ├─ 每份文件创建一条尚未关联任务的文件记录并设置初始 expireAt
 └─ 只返回 fileId；不创建 taskId、不解析、不写 Stream
 │
 │  用户点击“分析体检报告”，提交 1～5 个有序 fileIds
 ▼
Java API
 ├─ 校验全部 fileId 存在、未过期且尚未关联其他 taskId
 ├─ 校验累计大小与队列深度
 ├─ 创建 taskId，并在数据库事务内按提交顺序把全部文件关联到该 taskId
 ├─ 延长该 taskId 下文件记录的 expireAt
 └─ Lua 原子：写 task 状态 + XADD q:analysis      ──▶ 返回 taskId
                    │
                    ▼
                 Worker（终态去重 → Redis 任务锁 → Semaphore 限流）
                    ├─ ① 文件解析：逐页渲染 + 建立文本层（§3.2）
                    │      ├─ DOC-VL 解析（全部页面，含拍照）→ HTML/Markdown
                    │      └─ 原生文本层（仅电子版）→ 两者并存互校
                    │
                    ├─ ② LLM-A 结构化抽取（分批，≤8页/批）
                    │      九个顶层字段严格 Schema：reportMeta / allergenSectionPresent /
                    │      reportOverview / sections / indicators / textualFindings /
                    │      summaryConclusions / allergens / doctorAdvices
                    │      缺字段 ≠ 空数组 → 重试 → 仍缺则按 §4.0 分级处理
                    │      当前页图像写入 S3 单页 PNG 预览后立即释放内存
                    │
                    ├─ ③ 同一性校验（§4.0a）
                    │      多文件姓名/性别一致性（只在识别出信息的文件之间比对）
                    │      姓名/性别不一致 → 直接 FAILED/IDENTITY_MISMATCH，不自动合并
                    │
                    ├─ ④ Java 校验层
                    │      准入过滤 → 结论归一化 → 数值交叉校验
                    │      → 原文回溯校验 → 多文件合并 → 全局 ID 分配
                    │
                    ├─ ⑤ Safety Normalize（§7.2）
                    │      raw adviceText ─▶ Tier-0 安全分类（关键词 OR 模型标记）
                    │                          命中 → BLOCKED，止步
                    │                     └─▶ Tier-1 adviceKey 归一化
                    │
                    ├─ ⑥ 组装【模块一 健康指标】【模块二 健康问题】   纯 Java 计算
                    │
                    ├─ ⑦ 组装【模块三 饮食建议】   **只读，零模型调用**
                    │      查 diet_advice_cache（仅 ACTIVE 可用）
                    │        命中 ─▶ 直接用
                    │        正式枚举无活动版本 ─▶ 仍展示该条报告原文与来源
                    │                              不触发、不排队、不等待 LLM-B（§7.3）
                    │        OTHER ─▶ 不查库、不生成、不校验，只展示原文与未收录提示
                    │
                    └─ ⑧ 组装【模块四 菜品推荐】   **只读，零模型调用、零缓存写入**
                           Layer1  Java 过敏硬兜底（关键词族 + 未知过敏原 fail-safe）
                                     对**全部在架菜品**运行，含预热窗口后新增的菜品
                           Layer2  查 Redis 打标缓存（Field 含 adviceContentHash）
                                     **按维度做有无判定**（§8.2）：
                                       零有效标签 ─▶ 跳过整个该维度
                                       有标签     ─▶ 该维度照常参与，未打标的菜不获推荐
                           Layer3  Java 合并裁决 + 七条校验 + 主料校验 + 拼音排序
 │
 ├─ 轮询 taskId（递增间隔）──▶ 返回四模块聚合结果
```

“体检报告总结”接口契约：

```http
POST /api/health-report/analyze
Content-Type: application/json

{"fileIds":["file-a","file-b"]}
```

`fileIds` 顺序就是稳定的 `fileIndex` 顺序。请求成功后返回新创建的 `taskId`；同一个 `fileId`
只能成功关联一个 `taskId`，重复或并发提交返回明确冲突，不得静默复用或复制文件关联。

### 2.3 离线链路

```
上线/发版初始化（幂等入口，由部署侧接入调度）
   └─ **全部正式非过敏 adviceKey**（排除 OTHER 与 ALLERGEN，不按活跃度裁剪）
        ─▶ LLM-B 生成并自检 ─▶ Java 硬校验 ─▶ 保存非活动版本
        ─▶ LLM-C 全量预热 ─▶ **该维度至少一条有效标签** ─▶ 原子切换 ACTIVE

每日凌晨预热（每天一次，窗口之后不补算）
   ├─ 取**当日**在架菜品，计算 dishVersion
   ├─ 取**全部 ACTIVE 正式内容版本**（不做活跃度筛选）
   ├─ diff 出缺失的 (dishVersion, adviceKey:contentHash) 组合
   ├─ 分批调用 LLM-C（40 道/批），只写通过 Java 校验的标签
   └─ 上报 dish_tag_target_total / dish_tag_written_total 两个计数；不等即告警（§9.1a）

> **不存在覆盖率健康检查任务。** V1.6.4 的 `dishTagHealthCheckJob` 与其比值门槛已废止（§9.1a）。
> 预热窗口之后新增或变更的菜品没有标签，按 §8.2 自然不获推荐，**不触发任何盘中补算**。
```

### 2.4 三类模型调用

| 调用 | 时机 | 输入 | 缓存键 | 失败影响 |
|---|---|---|---|---|
| **LLM-A** 结构化抽取 | 在线，每 task 必调 | 报告图 + 文本层 | 按文件 hash | **task 失败**（唯一不可降级） |
| **LLM-B** 建议内容生成 | **纯离线**，在线链路禁止调用与排队 | `adviceKey` | §9.2 | 无 ACTIVE 内容时，模块三仍展示报告原文与来源 |
| **LLM-C** 菜品维度打标 | **纯离线**，在线链路禁止调用与等待 | 菜品批 + 建议内容 | §9.2 | 该维度零有效标签则**整维度跳过**，不伪造 `NEUTRAL` |

**LLM-B/C 的输入都不含任何用户数据** —— 这是缓存可跨用户复用的前提，也消除了这两步的 PII 面。
本版进一步要求：**LLM-B、LLM-C 只允许由上线初始化、发版初始化或每日离线预热任务调用**，
在线报告分析链路对二者的调用次数必须为 0，对标签缓存的写入次数也必须为 0（§8.2、§15）。

---

## 3. 阶段一：文件解析

### 3.0 字符规范化：全局唯一入口

**这是全案唯一一处"必须在所有文本进入比对之前执行"的处理，不属于任何单一格式的解析器。**

#### 3.0.1 问题

某连锁体检机构的真实 PDF（22 页，电子版）文本层中检出 **895 个非标准汉字、78 种**：

```
未⻅明显异常     ⻅ = U+2EC5（CJK部首补充）   ≠  见 = U+89C1
饮⻝             ⻝ = U+2EDD                  ≠  食 = U+98DF
⾎常规           ⾎ = U+2F8E（康熙部首）      ≠  血 = U+8840
```

**视觉上完全一致，渲染毫无问题，人工抽检发现不了。** 成因是 PDF 生成器构建
ToUnicode CMap 时按字形反查码点，而部首区与汉字区共用同一字形，反查取到了码点更小的部首区。
只有"本身可独立成字的部首"受影响（见/食/血/女/大/心/白/目/门/龙/齿…），
非部首字（明/异/常/脂）完全正常 —— 这个分布特征可作为快速识别依据。

**实测该报告上现有代码的匹配结果：**

| 关键词 | 未规范化时命中 | 实际出现 |
|---|---|---|
| `未见明显异常` | **0 次** | 34 次 |
| `饮食` | **0 次** | 7 次 |
| `血常规` | **0 次** | 1 次 |

**三条连锁失效，最后一条是安全红线：**

1. §4.1 的分组小结判定全线失效 → 该报告退回"仅 3 项带 ↑ 的指标"，异常占比又变 100%
2. §7.2 的饮食要求识别失效 → 抓不到「低脂、低糖、低胆固醇饮食」→ 不产生任何菜品推荐
3. **§8.3 的过敏关键词族失效** → 配料中的 `⻥` 匹配不上常量表的 `鱼` → **含鱼的菜进入推荐列表**

> §3.3 判定表里"PUA 字符 → MIXED"抓不到它 —— 这些是**合法 Unicode 字符**，
> 不是 PUA、不是 `�`，任何"文本层是否可信"的启发式检查都会判它正常。

**重要限定：部首污染只存在于「PDF/OFD 原生文本层」这一条路径。**
实测同一份报告经 DOC-VL（§3.11）识别后输出的是标准汉字 —— 它是识别产物，
不经过 ToUnicode 反查。因此启用 §3.2 的来源优先级后，
本节的爆炸半径大幅收窄，但**规范化层仍须保留**：原生文本层路径、菜品数据侧仍会遇到。

#### 3.0.2 实现：NFKC 不够，必须补部首映射表

```java
public final class TextNormalizer {
    // CJK部首补充 U+2E80-2EFF：NFKC 无对应映射，必须硬编码（§1.3）
    private static final Map<Character, Character> RADICAL_FIX;
    static {
        Map<Character, Character> values = new HashMap<Character, Character>();
        values.put('⻅', '见'); values.put('⻆', '角'); values.put('⻓', '长');
        values.put('⻔', '门'); values.put('⻘', '青'); values.put('⻙', '韦');
        values.put('⻛', '风'); values.put('⻝', '食'); values.put('⻣', '骨');
        values.put('⻩', '黄'); values.put('⻬', '齐'); values.put('⻮', '齿');
        values.put('⻰', '龙'); values.put('⻥', '鱼'); values.put('⻢', '马');
        values.put('⻦', '鸟'); values.put('⻋', '车'); values.put('⻉', '贝');
        values.put('⻄', '西'); values.put('⻈', '讠');
        /* 完整表约 30 条，覆盖 U+2E80-2EFF 全区 */
        RADICAL_FIX = Collections.unmodifiableMap(values);
    }

    public static String normalize(String raw) {
        if (raw == null) { return ""; }
        // ★ 顺序不可换：反 LaTeX 与 HTML 实体反转义必须在 NFKC 之前（§3.0.2a）。
        //   漏掉这两步，DOC-VL 路径的 evidence 会 100% 判成幻觉丢弃 —— 见 §4.5 的说明。
        String s = unescapeHtml(deLatex(raw));
        s = Normalizer.normalize(s, Normalizer.Form.NFKC);            // 康熙部首 U+2F00-2FDF
        StringBuilder sb = new StringBuilder(s.length());
        for (char c : s.toCharArray()) {
            Character mapped = RADICAL_FIX.get(Character.valueOf(c));
            sb.append(mapped == null ? c : mapped.charValue());
        }
        return sb.toString()
                 .replaceAll("[\u200B-\u200D\uFEFF\u00AD\u2060]", "")  // 零宽/软连字符
                 .replaceAll("\\s+", "");
    }
}
```

**NFKC 只能修一半。** 实测：康熙部首区（U+2F00-2FDF，739 个）NFKC 可还原；
**CJK 部首补充区（U+2E80-2EFF，156 个）NFKC 无对应映射**，`⻅` 规范化后仍是 `⻅`，必须查表。

加上补充表后实测：全文 34 处「未见明显异常」全部命中，残留非标准部首字符 **归零**。

#### 3.0.2a 反 LaTeX 归一化（DOC-VL 路径专属）

文档解析模型会把带括号的数值单位识别为数学公式。实测同一份报告出现两种形态：

```
原文  偏高(21.9μmol/L)      → 输出  偏高 $ (21.9\ \mu\text{mol/L}) $
原文  偏高(489μmol/L)       → 输出  偏高( $ 489 \mu mol/L $)     ← 括号跑到 $ 外面
原文  小结：尿酸（UA）偏高(489μmol/L)
      → 输出  小结：尿酸（UA）偏高 $ 489\mu mol/L $
```

**不处理的后果：** §4.5 原文回溯必然失败 —— 模型看图输出的 `evidenceText` 是
`(489μmol/L)`，与文本层的 `$ 489 \mu mol/L $` 比对不上，正确条目会被判成幻觉丢弃。

```java
// TextNormalizer 内部，由 normalize() 在 NFKC 之前调用（约 20 条映射，硬编码 §1.3）
// —— 它必须真正出现在 normalize() 的调用链上，而不只是"定义在同一个类里"。
private static String deLatex(String s) {
    s = s.replaceAll("\\\\text\\{([^}]*)\\}", "$1"); // \text{mol/L} → mol/L
    s = replaceGreekCommands(s);                         // 用 Pattern/Matcher 显式替换命令
    s = s.replaceAll("\\\\[,;: ]", "");           // \  \, \; 间隔符
    s = s.replace("$", "");                            // 剥定界符（可能不成对，逐个删）
    return s;
}
// replaceGreekCommands 使用 Java 8 的 Pattern / Matcher + appendReplacement；
// 不使用 Java 9 才增加的 Matcher.replaceAll(Function)。
// GREEK: mu→μ, alpha→α, beta→β, gamma→γ, delta→δ, micro→μ
```

**两个易错点：**

1. **`$` 可能不成对**（见上面第二例，`(` 在 `$` 外），因此逐个删除而不是配对剥壳
2. **`\mu mol` 中间的空格必须删**（`\mu` 与 `mol` 之间是 LaTeX 语法分隔符）。
   注意 `deLatex` 在 `normalize()` 内部、NFKC 之前执行，而 `normalize()` 末尾会统一
   `replaceAll("\\s+", "")` 去掉**全部**空白 —— 因此这一步不需要区分"语法空格"与"正常空格"，
   两者最终都会消失。**规范化结果只用于比对，不用于展示**：展示层始终用未规范化的报告原文

同路径还需 HTML 实体反转义：`&gt;3.0` → `>3.0`、`&amp;` → `&`、`&lt;` → `<`，
实现为 `unescapeHtml(String)`，同样由 `normalize()` 在 NFKC 之前调用（见 §3.0.2 主流程）。

#### 3.0.3 三个必须覆盖的位置

按"文本从哪来"分，风险取决于**有没有字形反查这一步**：

| 文本来源 | 机制 | 部首污染 | LaTeX 化 |
|---|---|---|---|
| PDF 原生文本层 | 字形 → ToUnicode 反查 | **高**，已确认 | 无 |
| OFD 原生文本层 | 同上（ofdrw 转 PDF 后走同一路径） | **高，可能更高**（§3.8） | 无 |
| DOCX | Unicode 直接存于 XML，无反查 | 低，**但会继承**（PDF 另存为 Word） | 无 |
| **DOC-VL 输出（§3.11）** | 识别输出 | **无**（实测标准汉字） | **有**（§3.0.2a） |
| LLM-A 视觉输出 | 模型看图输出 | 无 | 无 |

**最后两行是好消息，却制造了本案最麻烦的情况 —— MIXED 页上两类文本会被拿来互相比对：**

```
同一页（MIXED）
  ├─ 渲染图 → LLM-A → evidenceText: "未见明显异常"   （标准汉字）
  └─ 文本层 → §4.5   → 页面文本:    "未⻅明显异常"    （部首字符）
                        ↓  contains() = false
                   EVIDENCE_NOT_FOUND → 整条丢弃 + 计入 hallucinationCount
```

**模型完全正确，却被判成幻觉。** 这类页面上每条 evidence 都会如此，
`hallucinationRatio` 瞬间冲到 100% 触发提示词回归告警 —— 而查提示词永远查不出根因。
§4.4b 行级校验同理，全部落 `UNCERTAIN`，用户看到满屏"识别存疑"角标。

**因此 `normalize()` 必须覆盖三个位置，缺一即有缺口：**

```
① 报告文本层    PDF / OFD / DOCX / OCR 输出
② 模型输出      LLM-A 的 evidenceText / conclusionText / adviceText / allergenText
③ 菜品数据      菜名与食材原文名（§8.3 的 haystack）
```

第 ③ 条常被忽略但同样是安全相关：食堂菜品数据若从 Excel/PDF 菜单导入，
同样可能带部首字符，`「清蒸⻥」.contains("鱼")` 为 false → **鱼过敏用户拿到含鱼的推荐**。

#### 3.0.4 用类型系统强制，而非靠自觉

```java
// 禁止在业务代码中直接比对裸 String
public final class NormalizedText {
    private final String value;
    private NormalizedText(String v) { this.value = v; }
    public static NormalizedText of(String raw) { return new NormalizedText(TextNormalizer.normalize(raw)); }
    public boolean contains(NormalizedText other) { return value.contains(other.value); }
    public boolean equalsText(NormalizedText other) { return value.equals(other.value); }
}
```

**ArchUnit 规则：** 校验层、词表匹配、过敏兜底、菜品匹配相关的类中，
`String.contains` / `String.equals` / `String.indexOf` **禁止直接调用**，
只允许通过 `NormalizedText`。漏掉一处会编译期或构建期失败，
而不是等到线上漏判过敏。

> 这与 §9.1.2 的 `updateTask()` 封装同性质：**只要允许某处裸操作，漏洞就会从那里回来。**
> 区别在于那里漏掉的后果是"永久 Key"，这里漏掉的后果是"含过敏原的菜进推荐列表"。

#### 3.0.5 监控

| 指标 | 含义 | 告警 |
|---|---|---|
| `nonStandardCharRatio` | 文本层中部首区/PUA/零宽字符占比 | > 1% 记录该报告来源，用于统计受影响机构 |
| `residualNonStandardCount` | **规范化后仍残留的非标准字符数** | **非 0 即告警**，说明映射表需补充 |

`residualNonStandardCount` 是这套机制唯一的自检手段 —— 映射表不可能一次穷举，
必须靠线上真实数据持续补齐。补齐走发版（§1.3）。

### 3.1 文件级路由

```
magic number 探测（不信扩展名）
  ├─ image/*     → 整份 IMAGE
  ├─ PDF         → 逐页判定
  ├─ DOC/DOCX    → 结构化抽取
  └─ OFD         → 转 PDF 后逐页判定
```

支持格式：PDF ≤20MB、JPG/JPEG ≤10MB、PNG ≤10MB、OFD ≤20MB、DOC/DOCX ≤20MB，单次最多 5 个文件，**建议增加总量上限 60MB**（网关同步配置）。

> **`PageMode` 只决定"发什么给 LLM-A"，不决定"Java 校验层有没有东西可校验"。**
> 这两件事在 V1.6 引入 DOC-VL（§3.11）之后就分家了，必须分别建模：
>
> | 概念 | 取值 | 何时确定 | 用途 |
> |---|---|---|---|
> | `PageMode` | TEXT / IMAGE / MIXED | 路由阶段，DOC-VL 之前 | 决定发给 LLM-A 的形态（纯文本 / 纯图 / 图+**原生**文本层） |
> | `TextLayerSource` | DOC_VL / NATIVE / BOTH / **NONE** | DOC-VL 执行并过质量门槛之后 | 决定 §4.4b、§4.5 用什么做校验 |
>
> **拍照件的正确状态是 `mode=IMAGE` 且 `textLayerSource=DOC_VL`** —— 只发图给模型是对的
> （§3.11.4① 红线：DOC-VL 输出绝不进 LLM-A 的 prompt），但校验层此时**有**文本层可用。
> 若把校验门槛挂在 `mode == IMAGE` 上，拍照件会在校验入口被直接短路，
> §3.11 为拍照场景建立文本层的全部价值当场归零 —— 详见 §4.4b、§4.5 的门槛写法。

> **HEIC/HEIF 未列入需求支持格式。** iOS 相机默认输出 HEIC，按现有校验规则 iPhone 用户拍的报告会被直接拦下，**上线第一天就会爆**。
> **决策：由前端转码**（Web 用 `heic2any`，原生用系统 API）。后端不引入 `libheif`/ImageMagick 等 Native 依赖，避免部署复杂度。

### 3.1a 多文件语义

**明确定义为"同一个人的多个报告附件"**（分页照片、多份报告）。

同一个人在不同时间、不同机构做的检查**允许一起提交**；系统只拦"不同人"，
不拦"不同次体检"（§4.0a，2026-08-23 决策）。

Worker 将多文件打平、按文件选择顺序 + 页码排序，**合并生成一个 Task、输出一份汇总报告**。严禁拆成多个独立推荐结果。

合并前必须先通过 §4.0a 的同一性校验，合并规则见 §4.7。

### 3.2 文本层来源优先级

**每一页都必须有文本层。** 来源按以下优先级选取，不再区分"电子版有、拍照没有"：

```
① DOC-VL 输出（§3.11）        全部页面适用，含拍照
     ├─ 表格 → HTML，行列关系结构化
     ├─ 段落 → Markdown，条目编号保留
     └─ 质量不达标 → 降级到 ②
②  PDF/OFD 原生文本层           仅电子版
     ├─ 字符权威（原始 codepoint）
     └─ 但可能缺 ↑↓（矢量图形）、可能部首污染（§3.0）
③  无文本层 → textLayerSource = NONE   ①② 都不可用时
```

选取结果记为 `PageResult.textLayerSource`，取值 `DOC_VL` / `NATIVE` / `BOTH` / `NONE`。
**它与 `PageMode` 相互独立**（§3.1）：拍照件是 `mode=IMAGE` + `textLayerSource=DOC_VL`，
电子版 MIXED 页通常是 `mode=MIXED` + `textLayerSource=BOTH`。
**只有 `NONE` 才意味着校验层无米下锅**，此时 §4.4b 落 `IMAGE_ONLY`、§4.5 落 `EVIDENCE_UNVERIFIABLE`。

**①② 不是互斥的，电子版页面应两者都取：**

| 来源 | 强项 | 弱项 |
|---|---|---|
| DOC-VL | 行列结构、`↑↓` 图形标记、阅读顺序、无部首污染 | 识别产物，可能改写；LaTeX 化 |
| 原生文本层 | 字符权威，不会改写 | 顺序可能错乱；拿不到图形标记；可能部首污染 |

两者**互为第三来源**：`CONSISTENT` 需要 LLM-A 与至少一个文本层来源一致；
若两个文本层来源互相矛盾，该项标 `UNCERTAIN`。

> 实测某报告 `↑` 在原生文本层出现 0 次、在 DOC-VL 输出中 6/6 正确；
> 另一份报告原生文本层完好而 DOC-VL 在跨页续表处出错（§3.11.5）。
> **两者的失败模式不重叠，这正是同时取用的理由。**

### 3.3 PDF 页面级判定表

默认 MIXED，只有明确证据才降级：

| 优先级 | 条件 | 输出 |
|---|---|---|
| 1 | 页面无有意义文本（有效字符 ≈ 0） | IMAGE |
| 2 | 表格化医学数据风险（§3.4） | **MIXED** |
| 3 | 疑似 OCR overlay：`imageAreaRatio > 0.5 && textLength > 100` | **MIXED** |
| 4 | 文本层不可信：字体缺 ToUnicode CMap、`�`、PUA 字符 | **MIXED** |
| 5 | 大面积图像 + 存在检查所见/结论文字 | **MIXED** |
| 6 | 文本充分、质量正常、无版面风险 | TEXT |
| 7 | 其余全部 | **MIXED** |

> 原第 6 行「大面积图像 + 确认无有效正文 → IMAGE」已删除：该情形已被第 1 行
> （页面无有意义文本）完全覆盖，保留会让人以为代码里还有一条独立分支。

```java
if (noMeaningfulText(page))             return IMAGE;
if (hasStructuredMedicalDataRisk(page)) return MIXED;
if (isLikelyOcrOverlay(page))           return MIXED;
if (!isTextLayerReliable(page))         return MIXED;
if (hasLargeMedicalImage(page))         return MIXED;
if (isClearlyNarrativeTextPage(page))   return TEXT;
return MIXED;   // 初始值，不是兜底值
```

**IMAGE 是极窄分支** —— 超声报告即使只有"检查结论：轻度脂肪肝"几十个字，也走 MIXED。而这几十个字正是需求 §6 健康问题模块的核心来源，绝不能丢。

**记录 `reason` 而非仅 `mode`：** `PageDecision` 保存命中的规则枚举，否则线上只知道 MIXED 率 80%，不知道是哪条规则打的，无法调阈值。

### 3.4 表格化医学数据风险

**主判据：X 坐标锚点重复。** 重写 `PDFTextStripper.writeString(String, List<TextPosition>)` 取字符坐标，检测连续多行是否聚集在少数固定 X 区间。

```
x≈50       x≈220   x≈330     x≈450
白细胞      12.3    10^9/L    4.0-10.0
血红蛋白    98      g/L       130-175
```

它判断的正是风险本身 —— 不是"这页有医学数据"，而是"这页语义依赖二维排版，一维抽取会丢"。不依赖医学词表，不受定量/定性影响，成本近乎为零。

**辅助召回（词表）**：参考区间模式、单位（`mmol/L` `g/L` `10^9/L` `U/L` `μmol/L`）、定性结果（`阴性` `阳性` `+` `++`）、异常标识（`↑` `↓` `H` `L`）、表头词（`项目` `结果` `参考值` `单位` `提示`）。

### 3.5 文本层结构化（X 分栏 → Y 聚类）—— 原生文本层专用兜底

> **适用范围收窄。** 启用 §3.2 的来源优先级后，表格区域的行列关系由 DOC-VL 的 HTML
> 直接给出，本节的坐标聚类**不再是主路径**。它现在只用于两种情况：
> ① DOC-VL 质量不达标、退回原生文本层时；② 非表格区域的块内行切分。
> 双栏键值对（`发育情况|良好|心率|70`）已由 DOC-VL 正确保持列结构，实测通过。

**这是 §4.4b 行级校验与 §4.5 原文回溯的唯一数据来源。**

不能只按 Y 坐标聚类。体检报告双栏排版极常见：

```
血尿酸    480   208-428  │  甘油三酯   1.2   0-1.7
血糖      5.1   3.9-6.1  │  总胆固醇   4.5   0-5.2
```

纯 Y 聚类会把左右两栏合并成一个字符串，导致 `血尿酸` 与 `1.2` 落在"同一行" —— **往假一致方向失效，正是要防的**。

```java
// 1. 按 X 坐标间隙分栏：统计字符 X 分布，找出显著空白带
List<ColumnBand> bands = detectColumnBands(positions, GAP_THRESHOLD);
// 2. 栏内按 Y 聚类成行
List<TextLine> lines = bands.stream()
        .flatMap(b -> clusterByY(b))
        .collect(Collectors.toList());
```

`GAP_THRESHOLD` 建议取页宽的 5%，需用标注集校准。单栏报告自然只得到一个 band，逻辑兼容。

产出 `List<TextLine>{ text, x0, x1, y, bandIndex, lineIndex }`，**只存在于 Worker 单次执行的内存中，
不落任何存储**（§9.1.4 删除了 `report_parse_result` 表，理由是文本层接近报告全文、是全案最敏感的数据；
§10.3、§13.1 同口径）。

`TextLine` 与 §3.11.6 的 `Block` 共同实现 **`TextUnit`**（有界文本单元：`normalized()`、
`getBandIndex()`、`mergeWith()`）。§4.4b 行级校验与 §4.5 原文回溯**都以 `TextUnit` 为比对单元**，
不使用整页拼接串 —— 口径统一在这里，两处校验才不会一严一松。

**章节标题行捕获。** 需求 §5-4 要求"分组标签使用报告原文的章节名称"，因此解析层需要标记候选标题行：

```java
boolean isSectionHeaderCandidate(TextLine l) {
    return l.isBoldOrLargerFont()                    // 字号/字重高于正文
        || l.occupiesFullWidth()                     // 独占整行
        || SECTION_PATTERN.matcher(l.text).find();   // 血常规/血脂/肝功能/生化/尿常规…
}
```

标题最终由 LLM-A 确认（模型看图更准），但候选行随文本层一起投喂，可显著提高章节名的原文一致性。**不要让程序独立决定章节名** —— 版面千差万别，程序判定的假阳性会直接污染分组标签。

### 3.6 图片真实面积

**不能用 `PDImageXObject` 的 width/height** —— 那是像素尺寸，不是版面占位。4000×3000 的图可能只画成 2cm 的 LOGO。

必须继承 `PDFStreamEngine`，从内容流取 CTM，面积 = CTM 行列式：

```java
class ImageAreaEngine extends PDFStreamEngine {
    @Override protected void showForm(PDFormXObject form) { /* 必须递归 */ }
    // drawImage 时读 getGraphicsState().getCurrentTransformationMatrix()
}
```

图片**数量**没有判断价值（扫描仪常把一页切成 N 个条带），只有**面积**有。同理 `resources.getXObjectNames()` 只见顶层，嵌在 `PDFormXObject` 内的会漏，必须递归。

### 3.7 DOCX

**唯一一类文本抽取比转图更可靠的格式** —— `XWPFTable` 是真实结构化对象，行列关系是读出来的。

- 表格 → 遍历 `getRows()`/`getTableCells()` → **Markdown 表格** → TEXT
- 段落 → TEXT
- 段落样式为 `Heading*` 或加粗独立段 → 标记为章节标题候选，透传给 LLM-A
- `getAllPictures()` 非空且有效文本 < 100 → 图片抽出走 IMAGE/MIXED（用户把截图贴进 Word）

**两个禁忌：**
1. 不要用 `XWPFWordExtractor.getText()` —— 会把表格拍扁，浪费 DOCX 唯一的结构优势
2. 必须按 `BodyElement` 原始顺序遍历，不要"先取所有段落再取所有表格" —— 需求 §5-4 要求"同一分组内的指标按报告原始顺序排列"，顺序本身是需求的一部分

判断嵌入图片时需排除页眉 LOGO：结合图片尺寸、显示位置、是否重复出现。

### 3.8 OFD

```
OFD ─ ofdrw-converter 转 PDF ─ 成功 → 复用 §3.3
                             └ 失败 ─ OFD 直接渲染 PNG ─ 成功 → IMAGE
                                                        └ 失败 → DOCUMENT_PARSE_FAILED
```

**必须准备两条渲染路径** —— 转 PDF 已失败就没有 PDF 可渲染，"转图兜底"逻辑不成立。

失败提示：「该文件暂时无法可靠解析，建议改用手机拍摄或截图后重新上传」。

> **不要写成「请重新上传 PDF/JPG/PNG 格式」。** OFD 是医院开具的电子报告，
> 用户手上通常**只有**这一份，他没有 PDF 可换 —— 指向一个他做不到的动作等于让他放弃。
> 拍照/截图是他真正可执行的路径，而 §3.11 已使拍照件同样具备文本层。

> **评审前行动项（未关闭）：** 找 3–5 家不同医院真实 OFD 跑 ofdrw 转换。不同 HIS 厂商差异极大（内嵌签章、自定义字体）。通过率不足就在需求阶段砍掉或降级。

### 3.9 MIXED 的发送方式

同一条 message 内：`[image block]` 150 DPI 渲染图 + `[text block]` 提示词 + 文本层（含章节标题候选标记）。

**提示词分三版**，router 把 `PageMode` 与 `textLayerSource` 一并传下去：

**原生电子 PDF 页**（文本层是原始 codepoint，字符准确，错的只是顺序）：

```
本页同时提供页面图像和 PDF 文本层抽取结果。
页面图像用于确认：表格行列关系、项目与结果的对应、单位与参考区间的对应、
↑/↓/H/L 标识、章节标题归属、页面布局。
文本层用于辅助确认具体文字与数值。
注意：文本层可能因坐标排版产生顺序错乱；图像也可能因分辨率导致字符识别误差。
对于项目名称、检测值、单位、参考区间、报告结论等关键数据：
若两者冲突或无法可靠对应，不得自行选择其一或猜测，
必须标记为"识别存疑，需要核对原报告"。
```

**OCR overlay 页**（文本层本身是识别产物，无字符权威性）：

```
本页文本层来自自动识别（OCR），可靠性低于页面图像，仅作参考。
以页面图像为准。文本层与图像不一致时，一律以图像为准并标记存疑。
```

**DOC-VL 结构文本页**（识别产物，但结构化程度高于原生文本层）：

```
本页同时提供页面图像和文档解析结果（表格为 HTML，段落为 Markdown）。
解析结果的行列关系通常可靠，可用于确认项目与数值的对应。
但它是识别产物，可能出现：跨页续表把表头文字混入数据列、
数值单位被识别为数学公式（$...$）、个别字符识别误差。
以页面图像为准。两者冲突时以图像为准并标记存疑。
特别注意：若某个数据单元格的内容与表头行文字相同（如"项目名称"出现在数据行），
那是解析错误，请以图像为准判断该列的真实内容。
```

> 不要用一版提示词覆盖三种情况 —— 那等于把可信度不同的来源抬到同等地位。
> 最后一句针对 §3.11.5 的已知缺陷，是靠视觉纠正结构错误的关键。

### 3.10 工程要点

| 项 | 说明 |
|---|---|
| **JBIG2 解码** | 医院扫描件常用，PDFBox 默认渲染直接抛异常，必须引 `jbig2-imageio`；CMYK 需 `jai-imageio-core`。**本地用电子版测试发现不了，线上必炸** |
| **Headless** | `-Djava.awt.headless=true`，否则 `PDFRenderer` 无 X server 启动即挂 |
| **中文字体** | 容器缺字体渲染成方块，装 `fonts-wqy-zhenhei` |
| **OOM 防护** | 见 §11.5，限流值按内存算不按 CPU 算 |
| **加密 PDF** | 捕获 `InvalidPasswordException`，空密码直接通过 |
| **DPI** | 默认 150。不要承诺"降到 110 省一半 token"（视觉模型多为分块编码，非像素线性）。成本优化优先做裁白边、裁页眉页脚 |
| **LibreOffice** | 若引入需固定大小进程池串行调度。**能用 POI + ofdrw 覆盖就别引入** |

---

---

### 3.11 DOC-VL 文档解析：主链路组件

**这一节在此前版本是"P0 前置 spike"。用两份真实报告共 6 页实测后，本版升为主链路组件。**

#### 3.11.1 它解决的问题

拍照上传时页面无文本层，§4.4b 行级校验与 §4.5 原文回溯**双双空转**：
`value` / `conclusionText` / `evidenceText` 全部来自同一次视觉模型调用，
§1.1 定义的失败 1（错读化验值）与失败 2（幻觉引用）**在拍照场景几乎无人把守**
—— 只剩 §4.4a 的结论/数值交叉校验，而它抓不到"数值和结论被一起读错"。

而拍照很可能是食堂场景的主流形态。**这是挂了多个版本的一处真实缺口。**

电子版同样有问题：某连锁机构的 PDF 原生文本层里 `↑` 出现 **0 次**（红色箭头是矢量图形），
且含 895 个部首污染字符（§3.0）。原生文本层并不总是可靠。

#### 3.11.2 选型：文档解析模型，不是传统 OCR，也不是通用 VLM

三层可选方案的定位：

| 层 | 代表 | 输出 | 在本方案中的定位 |
|---|---|---|---|
| ① 传统 OCR | PP-OCRv5、Tesseract | 字符 + 坐标 | 可用，但拿不到表格结构 |
| ② **文档解析模型** | **PaddleOCR-VL、PP-StructureV3** | **HTML 表格 / Markdown + 块级坐标** | **本方案选用** |
| ③ 通用 VLM | Qwen3-VL 等 | 任意结构化结果 | 这是 LLM-A，不是这里 |

**选 ② 而非 ①**：表格 HTML 比"字符 + 坐标"更直接 —— 行列关系是给出的，不是猜出来的（§4.4b）。

**不能用 ③ 顶替**：那样两个来源同源，校验层在逻辑上消失。

**误差不相关性成立**：LLM-A 用通用 VLM（如 Qwen3-VL 系），DOC-VL 用 PaddleOCR-VL
（NaViT 编码器 + ERNIE-4.5-0.3B），不同厂商、不同架构、不同训练数据。
比原先设想的"通用 VLM + 传统 OCR"更强 —— 两边都懂版面，但错法不同。

#### 3.11.3 实测结论（两份报告 6 页）

| # | 判据 | 结果 |
|---|---|---|
| 1 | `↑` 与指标行的绑定 | **通过 6/6**，与报告小结逐一吻合，正常项提示列干净留空 |
| 2 | 表名标注 | 通过，`colspan` 标出「血脂4项」等；**位置不统一**，不可假设总在 colspan 行 |
| 3 | 小结与表格的关联 | 通过。三种形态：表外段落、表内单元格、`rowspan` 切碎 |
| 4 | 双栏键值对不串行 | **通过**，`发育情况\|良好\|心率\|70` 正确保持 4 列 |
| 5 | 跨页续表 | **有缺陷**，见 §3.11.5 |
| 6 | 部首字符污染 | **未出现**，输出标准汉字 |
| 7 | 综述建议条目编号 | **通过 9/9**，`1、`~`9、` 完整保留 |
| 8 | 饮食医嘱完整性 | **通过**，「请您戒烟忌酒，低脂、低糖饮食，控制食量」逐字无损 |
| 9 | 跨页断句 | **通过**，「平时 \| 应注意少进食高嘌呤的食物」可直接拼接 |

**三处比原生文本层更可靠**（不只是"不差"）：

```
① DR-70 行断裂        原生：项目名被数据行劈成两截    DOC-VL：完整一行
② 肝功小结顺序错乱     原生：「小结：」标签跑到末尾     DOC-VL：按视觉阅读顺序还原
③ CT 多行编号         原生：编号 2. 卡在行尾          DOC-VL：1./2./3. 正确分行
```

#### 3.11.4 三条不可违反的约束

**① DOC-VL 输出绝不进入 LLM-A 的 prompt。**

```
✅  图像 ──▶ LLM-A  ──▶ 抽取结果 ──┐
    图像 ──▶ DOC-VL ──▶ 结构文本 ─┴──▶ Java 比对（§4.4b / §4.5）

❌  图像 + DOC-VL 文本 ──▶ LLM-A ──▶ 抽取结果 ──▶ 与 DOC-VL 文本比对
```

喂进去，模型输出就不再独立，**交叉校验的独立性当场归零** ——
它会顺着 DOC-VL 的错误抄，然后两者"完全一致"。

> 与 §3.9 的区别：MIXED 页把 PDF **原生文本层**喂给模型是可以的，那是原始 codepoint、字符权威；
> DOC-VL 输出是识别产物，性质完全不同。

**② 它不能取代 LLM-A。**

DOC-VL 输出的是文档结构（HTML/Markdown），不是业务 JSON。
`sectionSummary`、`adviceKey` 归一化、`allergenSectionPresent`、`reportMeta`
这些需要语义理解与分类，是 LLM-A 的职责。
更根本的：**两者并存才有第二来源**，合并即失去校验。

**③ 质量不达标就置 `textLayerSource = NONE`，不要强行造文本层。**

一份糊掉的输出比没有更糟：太松产生**假 `CONSISTENT`**（把错误数据盖章为已核验），
太严把正确数据打成 `UNCERTAIN`（满屏存疑角标）。**两个方向都比现状差。**

```java
boolean usableAsTextLayer(DocVlResult r) {
    return r.getBlockCount() >= MIN_BLOCKS
        && r.getAvgConfidence() >= MIN_AVG_CONFIDENCE
        && r.getLowConfBlockRatio() <= MAX_LOWCONF_RATIO;
}
```

三个阈值为待定值，由 §15.1 标注集校准，**校准完成前不得上线**。

#### 3.11.5 已知缺陷：跨页续表的表头混入

实测跨页续表（肝功12项在第二页的续行）出现结构错误：

```html
<tr><td rowspan="2">总胆红素（T-BIL）</td><td rowspan="2">项目名称</td><td>检查结果</td>...
<tr><td>26.0</td><td>μmol/L</td><td>0.0～26.0</td>...
```

**「项目名称」是表头文字，被当成了数据列。** 语义上可救（去掉多余那列就对了），
但**程序无法自动判断该去掉哪一列**。

**缓解：这正是 §3.9 MIXED 模式存在的理由，现在有了实证。**
图 + 结构文本一起喂给 LLM-A，模型看图即知哪个是表头。
**不要走"只喂结构文本"的纯文本路径** —— 那种情况下模型可能输出 `value="项目名称"`。

**检测信号**（可程序判定，命中则该页强制 MIXED）：
表格数据行中出现与表头行相同的文字。

#### 3.11.6 部署与接口

```java
public interface DocVlClient {
    /** 返回结构化文档（HTML 表格 + 段落）；不达标时返回 empty，调用方置 textLayerSource=NONE */
    Optional<DocVlResult> parse(byte[] pageImage);
}
@Getter
@Setter
public final class DocVlResult {
    private String html;
    private List<Block> blocks = new ArrayList<Block>();
    private float avgConfidence;
}

@Getter
@Setter
public final class Block {
    private String type;
    private String text;
    private float x0;
    private float y0;
    private float x1;
    private float y1;
    private float confidence;
}
```

三条接口约束：

- **必须返回置信度**，§3.11.4 ③ 的质量门槛靠它判定
- **必须返回块级坐标**，非表格区域的行级校验退回坐标方案（§3.5）
- **同步调用 + 超时（建议 15s/页）+ 降级返回 empty**，DOC-VL 是校验增强，不能因它挂了让任务失败

**部署形态优先级：**

| 优先级 | 方案 | 合规影响 |
|---|---|---|
| ① | **行内已部署的模型服务**（如国产加速卡上的 PaddleOCR-VL） | **零** —— 数据不出内网 |
| ② | 自建（独立 Python 服务 + HTTP，不用 JNI/ONNX 塞进 JVM） | 零，但多一套运维 |
| ③ | 云厂商 OCR API | **不推荐** —— 又开一个敏感数据外发点，§13.2 的七个问题要问两遍 |

> 跳过 ③ 的理由是合规不对称：省下的开发时间远小于多一条外发链路的审批与长期风险。
> §13.1 已把自有侧压到“原文件成功即删、单页预览图 2 小时即删”，再加一个外部 OCR 就抵消掉了。

**版本提醒：** 带 bounding box 的 text spotting 是 PaddleOCR-VL 1.5 才加入的能力，
更早版本可能只有块级坐标。**按实际部署版本验证，不要照最新文档判断。**

#### 3.11.7 坐标粒度的实际影响

官方文档明确：PP-StructureV3 提供表格单元格坐标、文本坐标等细粒度信息，
**PaddleOCR-VL 系列不提供**。但这对本方案影响有限：

| 区域 | 校验方式 |
|---|---|
| **表格区** | 用 HTML 结构做**单元格级精确比对**（§4.4b），比坐标更直接，不需要细粒度坐标 |
| **非表格区**（综述建议等） | 用块级坐标 + 文本比对，满足 §4.5 原文回溯 |
| 双栏键值对 | HTML 已正确保持列结构，§3.5 的 X 分栏不再需要 |

若后续发现非表格区的校验精度不足，再申请加部署 PP-StructureV3（同一仓库，成本低）。

---

## 4. 阶段二：结构化抽取与校验

### 4.0 LLM-A 输出格式与严格 Schema

```json
{
  "reportMeta": {
    "patientName": "张三",
    "gender": "MALE",
    "age": 34,
    "examDate": "2026-08-01",
    "institution": "XX市人民医院体检中心"
  },
  "allergenSectionPresent": true,

  "reportOverview": {
    "totalCount": 136, "abnormalCount": 13, "majorAbnormalCount": 0,
    "evidenceText": "136项 总指标 0项 重要异常结果 13项 异常结果", "pageNo": 3
  },

  "sections": [
    {"sectionName": "血糖", "sectionOrder": 1, "pageNo": 6,
     "sectionSummary": "未见异常", "summaryEvidence": "小结： 未见异常"},
    {"sectionName": "血脂4项", "sectionOrder": 2, "pageNo": 6,
     "sectionSummary": "低密度脂蛋白胆固醇偏高(4.26mmol/L) 总胆固醇（TC）偏高(6.47mmol/L)",
     "summaryEvidence": "小结： 低密度脂蛋白胆固醇偏高(4.26mmol/L) 总胆固醇（TC）偏高(6.47mmol/L)"}
  ],

  "indicators": [{
    "rawName": "总胆固醇（TC）", "sectionName": "血脂4项", "orderInSection": 1,
    "value": "6.47", "unit": "mmol/L", "referenceRange": "0～5.72",
    "conclusionText": "↑", "hasConclusion": true,
    "evidenceText": "总胆固醇（TC） 6.47 mmol/L 0～5.72 ↑", "pageNo": 6
  }, {
    "rawName": "甘油三酯（TG）", "sectionName": "血脂4项", "orderInSection": 3,
    "value": "1.69", "unit": "mmol/L", "referenceRange": "0～1.70",
    "conclusionText": "", "hasConclusion": false,
    "evidenceText": "甘油三酯（TG） 1.69 mmol/L 0～1.70", "pageNo": 6
  }],

  "textualFindings": [{
    "findingName": "脂肪肝", "sectionName": "肝胆B超", "orderInSection": 1,
    "evidenceText": "肝胆B超：肝实质回声增粗增强，提示脂肪肝", "pageNo": 5
  }],

  "summaryConclusions": [{
    "itemNo": 3, "evidenceText": "超重，建议控制体重，低脂低盐饮食", "pageNo": 6
  }],

  "allergens": [{
    "allergenText": "虾蟹类", "sourceSection": "过敏原检查",
    "evidenceText": "虾蟹类 阳性(+)", "pageNo": 4
  }],

  "doctorAdvices": [{
    "adviceType": "NUTRITION_SUPPLEMENT",
    "adviceText": "建议补充铁剂",
    "adviceKeyCandidates": ["IRON"],
    "safetyFlag": null,
    "sourceSection": "总检结论", "itemNo": 5,
    "evidenceText": "血红蛋白偏低，建议补充铁剂，必要时血液科就诊", "pageNo": 6
  }, {
    "adviceType": "DIET_REQUIREMENT",
    "adviceText": "低脂、低糖、低胆固醇饮食",
    "adviceKeyCandidates": ["LOW_FAT", "LOW_SUGAR", "LOW_CHOLESTEROL"],
    "safetyFlag": null,
    "sourceSection": "专家建议与指导", "itemNo": 2,
    "evidenceText": "低脂、低糖、低胆固醇饮食，节制饮食，控制体重", "pageNo": 7
  }]
}
```

#### 4.0.1 九个顶层字段全部 required

```
required:
  reportMeta / reportOverview / allergenSectionPresent
  sections / indicators / textualFindings
  summaryConclusions / allergens / doctorAdvices
```

**三类字段的空值语义不同，必须分开定义**，否则模型会混用 `[]`、`null` 与"省略字段"：

| 类别 | 字段 | 无内容时 | 说明 |
|---|---|---|---|
| **数组类** | `sections` `indicators` `textualFindings` `summaryConclusions` `allergens` `doctorAdvices` | `[]` | 空数组 = 明确告知"报告里没有" |
| **对象类** | `reportMeta` `reportOverview` | `null` | 报告本身没有这部分信息（如公立医院报告普遍不印总览数字） |
| **布尔类** | `allergenSectionPresent` | — | **必须 true/false，不得为 null** —— §4.0.3 四态判定的直接依据 |

```
字段存在 + [] / null    = 模型明确告知"没有"
字段不存在              = MODEL_OUTPUT_INVALID
字段类型错误            = MODEL_OUTPUT_INVALID
布尔字段为 null         = MODEL_OUTPUT_INVALID
必填子字段缺失          = 该条目丢弃 + 计数
```

**"必填子字段"必须逐条列明，不能只写一句概括** —— 判错方向的两种后果都不小：
把可选字段当必填会**丢掉报告里真实存在的内容**（§7.8 称之为"对报告内容的虚假陈述"），
把必填字段当可选则会让下游拿到半条数据。

| 数组 | 必填子字段（缺一即丢弃该条目 + 计数） | 可选子字段 |
|---|---|---|
| `indicators` | `rawName` `value` `evidenceText` `pageNo` | `unit` `referenceRange` `conclusionText` `sectionName` `orderInSection` |
| `sections` | `sectionName` `pageNo` | `sectionOrder` `sectionSummary` `summaryEvidence`（无小结时为空串） |
| `textualFindings` | `findingName` `evidenceText` `pageNo` | `sectionName` `orderInSection` |
| `summaryConclusions` | `evidenceText` `pageNo` | `itemNo` |
| `allergens` | `allergenText` `evidenceText` `pageNo` | `sourceSection` |
| `doctorAdvices` | `adviceType` `adviceText` `evidenceText` `pageNo` | `adviceKeyCandidates` `safetyFlag` `sourceSection` `itemNo` |

> **`safetyFlag` 明确为可选**（2026-08-23 决策）。缺失按 `null` 处理，即"模型未给出第二意见"，
> 此时 §7.2 的 Tier-0 判定完全由 Java 关键词承担。把它当必填会因为一个辅助字段丢掉整条医嘱，
> 代价明显不成比例。但**这条退化必须可观测**：缺失时计 `safetyFlagMissingCount`，
> 比例升高说明提示词回归，需排查 —— 否则第二道保险会静默消失而无人知晓。

用 JSON Schema（`everit-org/json-schema` 或 Jackson `@JsonProperty(required=true)` + 显式校验器）
在反序列化后立即校验，**不要靠"字段为 null 就 new 一个空 List"这种写法** —— 那正是漏洞本身。

**数组区块必填是 fail-open 防线**：缺 `allergens` 却当成 `[]`，会导致 Layer 1 不运行，
含虾的菜直接进推荐列表。

**`allergenSectionPresent` 必填的理由不同，不要混为一谈。** 它缺失时若默认 `false`，
得到的是 `NOT_SCREENED` —— 只会多显示一条"本次报告未包含过敏原检查"的提示，
方向是保守的，不构成 fail-open（真正危险的是默认 `true`）。
它必填是因为：**一个连布尔字段都会漏输出的响应，整体可信度已经不足，应当重试而非继续使用。**

> 这个区别必须写清楚，否则后续会有人按"因为危险所以必填"的逻辑去推断别的字段该怎么处理，推错方向。

**`reportOverview` 的必填理由又是第三种。** 它既不是 fail-open 防线（缺失只是回退到自行统计），
也不涉及可信度判断 —— 它必填纯粹是为了**区分"报告没印"与"模型没输出"**：
前者填 `null`，后者是字段缺失。若允许省略，两种情况无法分辨，
而它们的处理方式完全不同（前者正常走 §5.4，后者应重试）。

#### 4.0.1a 跨批合并规则：九个字段一个都不能漏

LLM-A 按 ≤8 页/批调用（§11.4），一份 22 页报告就是 3 批，**每批都会返回完整的九个顶层字段**。
六个数组类字段的合并规则见 §4.7，但三个非数组字段此前没有定义合并方式 —— 这是必须补上的，
因为 `allergenSectionPresent` 是 §4.0.3 四态判定的**唯一**依据。

| 字段 | 跨批合并规则 |
|---|---|
| `reportMeta` | **取第一个非 `null`**（按页序）。姓名/性别/日期/机构等信息物理上只印在首页信息栏。若后续批次给出**不同的**非 `null` 值，保留第一个并记 `metaConflictCount` 告警，不 FAIL —— 跨**文件**的身份一致性由 §4.0a 独立负责 |
| `reportOverview` | **取第一个非 `null`**。总览数字只印在首页/概览页，后续批次必然是 `null`；若用"末值胜出"会被 `null` 冲掉，正好丢掉 §5.4.1 论证的"优先展示报告自带数字" |
| `allergenSectionPresent` | **OR 合并，但 `true` 需要佐证** —— 见下 |

**`allergenSectionPresent` 为什么不能用裸 OR。** 语义上"任一批看到该章节即为真"是对的，
但两个方向的代价不对称：

```
误判为 true  → SCREENED_NONE → 不提示 → 用户以为系统核验过过敏，实际没有
误判为 false → NOT_SCREENED  → 多一条「本次报告未包含过敏原检查」提示
```

§1.2 称这条提示是「这个产品决策**唯一的安全出口**」，明确要求不能省、不能弱化措辞。
因此 `true` 这个方向必须有佐证，`false` 方向不需要：

```java
boolean mergeAllergenSectionPresent(List<BatchResult> batches) {
    // 有任何过敏原条目 → 章节必然存在，条目自带 evidenceText，无需额外佐证
    if (batches.stream().anyMatch(b -> !b.getAllergens().isEmpty())) { return true; }

    for (BatchResult b : batches) {
        if (!b.isAllergenSectionPresent()) { continue; }
        // 该批声称有过敏原检查章节，但没给出任何条目 —— 用文本层做一次 Java 佐证。
        // 词表 AllergenSectionWords 硬编码（§1.3），如「过敏原」「变应原」「过敏源」「IgE」。
        if (containsAnyNormalized(b.getPageTextUnits(), AllergenSectionWords.ALL)) { return true; }
    }
    return false;   // 佐证不上 → NOT_SCREENED → 多一条提示，符合 §1.3「未命中路径必须保守」
}
```

**佐证不上时落 `false`，包括文本层不可用（`textLayerSource == NONE`）的情况。**
代价是"报告确实做了全阴性筛查、但我们没能佐证"时会多显示一条提示 ——
按 §1.2 的安全不对称，这个方向的错误是可接受的；反方向不可接受。
修好 §4.4b/§4.5 的门槛后（§19 第 35 条）拍照件同样有文本层，该回退极少触发。

> 注意这条**不新增顶层字段**，九字段 Schema 不变；佐证发生在 Java 合并层，
> 用的是已经存在的文本层与 §3.0 的规范化比对。

#### 4.0.2 校验失败的分级处理

```
第 1 次 MODEL_OUTPUT_INVALID
    → 带 failReason 重试 LLM-A 一次
      「上次输出缺少字段 {fieldName}。请输出完整 JSON，九个顶层字段一个都不能少。
        数组类字段没有内容时输出 []，对象类字段（reportMeta/reportOverview）
        没有对应信息时输出 null，allergenSectionPresent 必须是 true 或 false。」

第 2 次仍失败 → 按缺失区块分级：
```

| 缺失字段 | 第 2 次仍缺失时的处理 |
|---|---|
| `reportMeta` | `identityStatus = UNKNOWN`，多文件时**禁止自动合并**（§4.0a） |
| **`reportOverview`** | **按 `null` 处理，走 §5.4 自行统计，仅记 warn，不 FAIL** —— 见下 |
| `sections` / `indicators` | **task FAILED** —— 模块一/二 无数据可展示，整体失败比展示半份报告诚实 |
| `textualFindings` | 模块二仅缺该类条目，标 `partialExtraction`，模块底部提示 |
| `summaryConclusions` | 同上；同时 `dietRequirement` 来源不完整，饮食建议模块标 `partialExtraction` |
| **`allergens`** | **`allergenStatus = UNKNOWN`** —— 见 §4.0.3，绝不解释为"无过敏原" |
| `doctorAdvices` | 饮食建议模块标 `partialExtraction`，展示已有内容 + 提示可能不完整 |

**`reportOverview` 必须落在最轻的一档，不能整体失败。**
它的失败代价只是"回退到我们自己算总览条"，不影响任何安全判定，也不影响四个模块的完整性。
为它把整个 task 判 FAILED 不成比例 —— 尤其考虑到**公立医院报告普遍不印总览数字**，
若模型在这类报告上偶尔省略字段而非填 `null`，会造成大面积误伤。

> 分级的判据始终是"缺这个字段会导致什么后果"，不是"这个字段重不重要"。

#### 4.0.3 `allergenStatus` 四态

`allergens: []` 本身仍有歧义：报告压根没做过敏原检查，和做了检查全阴性，是两件事。
因此 LLM-A 额外输出 `allergenSectionPresent`，Java 合成四态：

```java
enum AllergenStatus {
    SCREENED_POSITIVE,   // 有过敏原条目            → 正常走 Layer 1
    SCREENED_NONE,       // sectionPresent=true, []  → 已筛查无阳性，正常推荐
    NOT_SCREENED,        // sectionPresent=false, [] → 报告未做筛查
    UNKNOWN              // 字段缺失/校验失败        → 我们不知道
}
```

| 状态 | 菜品推荐模块行为 |
|---|---|
| `SCREENED_POSITIVE` | Layer 1 正常运行 |
| `SCREENED_NONE` | 正常推荐，无额外提示 |
| `NOT_SCREENED` | 正常推荐 + 模块顶部提示「本次报告未包含过敏原检查，推荐结果未考虑过敏因素。如有已知过敏，请自行确认菜品配料，并向食堂工作人员确认」 |
| **`UNKNOWN`** | **推荐列表整体转入「过敏成分待确认」列表**（§8.9），模块顶部醒目提示「过敏信息未能确认，请谨慎参考」 |

> **不提供"重新分析入口"。** 该入口在本方案中既不存在也无法实现：同一 `taskId` 的 SUCCESS
> 结果在 Redis 固化 2 小时、重复调用 `/result` 不触发任何重算（§7.8、§10.1）；同一批 `fileId`
> 只能关联一个 `taskId`，无法二次提交（§2.2）；原始文件在 SUCCESS 后已立即删除（§13.1）。
> 给一个点不动的按钮，比不给更糟。用户唯一的补救途径是重新上传，由前端在文案中说明。

> 本版不提供用户自填过敏原入口，因此 `activeAllergens` 完全来自报告。
> `NOT_SCREENED` 与 `SCREENED_NONE` 都会走向"Layer 1 不产生任何强制不推荐"，
> 但两者的提示文案必须不同 —— 前者是"我们不知道"，后者是"报告说没有"。

#### 4.0.4 提示词中的八条硬要求

```
1) 无论某个字段是否有内容，九个顶层字段必须全部输出，任何情况下都不要省略字段。
   三类字段的空值形态不同：
     数组类（sections / indicators / textualFindings / summaryConclusions /
             allergens / doctorAdvices）没有内容时输出空数组 []
     对象类（reportMeta / reportOverview）报告中没有该信息时输出 null
     布尔类（allergenSectionPresent）必须给出 true 或 false，不得为 null

2) conclusionText 只填报告上实际印刷的结论文字（如「↑偏高」「偏低」「H」「正常」
   「未见异常」）。报告未给结论的项目，hasConclusion 填 false，conclusionText 留空。
   不要根据数值自行判断是否偏高 —— 只记录报告上写了什么。

3) evidenceText 必须是报告上连续出现的原文片段，逐字照抄，不得改写、补全、
   合并或翻译。找不到连续原文的，该条不要输出。

4) sectionName 必须使用报告原文中的章节/分组标题（如「血脂检查」「生化全套」），
   不要自行归类或改用医学通用名。报告未分组的，填 "未分组"。

另：allergenSectionPresent 表示报告中是否存在过敏原检查相关章节，
    与是否检出阳性无关。有该章节但全部阴性，仍填 true。

6) 饮食相关建议可能出现在报告的任何位置，不限于"总检结论""专家建议"章节。
   请全文扫描，包括各专科的建议段落（如口腔科「饮食注重粗细搭配，少吃甜食」）。
   遗漏这类分散表述会直接导致菜品推荐缺失对应维度。

7) 报告若在首页或概览页给出了指标总数、异常项数等汇总数字，填入 reportOverview；
   没有则该对象填 null。不要自行统计后填入 —— 该字段的语义是"报告自己印的数字"。

8) 一条饮食医嘱常常同时包含多个要求（如「低脂、低糖、低胆固醇饮食」）。
   请在 adviceKeyCandidates 中列出**全部**命中的键，不要只填一个，也不要把一条医嘱
   拆成多条 doctorAdvices —— 原文、来源、evidenceText 都应保持是同一条。
   一个都判断不出时填空数组 []，由程序处理。
```

> **编号说明：** 上面缺的第 5) 条是分组小结的抽取要求，正文与提示词原文见 §4.0.5
> 「提示词补充」，两节合起来才是完整的八条。「另：」那一段是对第 1) 条的补充说明，
> 不单独占编号。拼装实际提示词时按 1~8 连续编号下发，不要把本行说明发给模型。

**`safetyFlag`：** 模型对该条建议是否涉及碘、肾病饮食、用药、孕产、儿童等敏感方向的判断，
取值 `IODINE` / `RENAL` / `DRUG` / `PERINATAL` / `PEDIATRIC` / `null`。
它是 §7.2 安全分类的**第二意见**，与 Java 关键词做**或运算**（高召回），不是唯一依据。

#### 4.0.5 `sectionSummary`：结论的第二个来源

**很多医院不在指标行标 `↑↓`，而是在每个检查分组末尾给一句「小结」。**
真实样本（北京大学深圳医院，17 页标准报告）中：

```
血糖         小结： 未见异常                    ← 覆盖组内全部指标
血脂4项      小结： 低密度脂蛋白胆固醇偏高(4.26mmol/L) 总胆固醇（TC）偏高(6.47mmol/L)
血常规       小结： 红细胞平均血红蛋白浓度(MCHC)偏低(312g/L)
免疫功能全套  小结： 未见异常
```

该报告 71 项指标中，只有 9 项在"提示"列带 `↑↓`，其余 62 项提示列为空 ——
**但它们所在分组的小结明确写着「未见异常」。**

「小结：未见异常」是**报告对该组全部指标给出的明确结论**，不是我们推断出来的，
因此完全符合需求 §5-1「严格依据报告原文，不额外推断」。
把它当作"无结论"丢弃，反而是对报告原文的曲解。

**因此 `sections` 增加两个必填字段：**

| 字段 | 说明 |
|---|---|
| `sectionSummary` | 该分组小结的**结论文字**，无小结时填空字符串 |
| `summaryEvidence` | 小结的原文片段，供 §4.5 回溯校验 |

**提示词补充：**

```
5) 很多报告在每个检查分组末尾有「小结」一行。请将该行的结论文字填入
   sectionSummary，原文填入 summaryEvidence。没有小结的分组，两者都填空字符串。
   不要把小结内容拆散填进各个指标的 conclusionText —— 指标的 conclusionText
   只填该指标所在行上实际印刷的标记（如 ↑ / ↓ / H / L）。
   小结后面若紧跟【疾病名】这类方括号标题，不要并入 sectionSummary，那是下一个内容块。
```

**最后一句针对实测发现的黏连**：某报告第 3 条输出为
`小结：低密度脂蛋白胆固醇偏高(4.26mmol/L) 总胆固醇（TC）偏高(6.47mmol/L) 【高脂血症】`，
`【高脂血症】` 本应独立成块。Java 侧同样以 `【...】` 为终止符做一次截断兜底，
不完全依赖提示词。

**小结的三种物理形态**（实测，抽取需全部覆盖）：

```
① 表外独立段落        </table>\n小结：未见异常
② 表内末行单元格      <tr><td>小结：</td><td>未见异常</td>...
③ rowspan 切碎        <tr><td rowspan=2>小结：</td><td>间接胆红素（IB）</td>
                          <td rowspan=2>偏高(21.9μmol/L)</td>...
```

形态 ③ 需按单元格顺序拼接还原，不能逐单元格独立处理。

> 最后一句很重要：小结里写的「总胆固醇（TC）偏高(6.47mmol/L)」不能被模型抄成
> 总胆固醇那一行的 `conclusionText` —— 那样会让 §4.4a 的交叉校验失去独立性
> （结论与数值来自同一句话，一起错了也发现不了）。

### 4.0a 多文件同一性校验（合并前置）

多文件被定义为"同一个人的多个报告附件"，但这是**系统假设，不是用户一定遵守的事实**。
两个人的报告被合成一个健康画像，是比 OCR 读错一项严重得多的语义错误。

**判定只有两种结果，没有中间态。** 不一致就停止任务，让用户改好上传的文件重新发起 ——
不做「弹窗确认后继续」这条交互。

```java
IdentityCheck check(List<ReportMeta> metas) {
    if (metas.size() <= 1) { return PASS; }

    // 只在**确实识别出体检信息**的文件之间比对；reportMeta 为 null 的文件不参与，也不因此拦截。
    // 理由：分页拍照是多文件的主流场景（§3.1a），第 2 页往往就是一张没有信息栏的化验单，
    // reportMeta 必然为 null。一律拦截会让这个场景直接不可用，而用户无从"修正"——
    // 那一页本来就没印姓名。
    List<ReportMeta> known = metas.stream()
            .filter(m -> m != null).collect(Collectors.toList());
    if (known.size() <= 1) { return PASS; }

    if (!allNamesSamePerson(known))                 { return BLOCK("姓名不一致"); }
    if (distinctNonNullGenders(known).size() > 1)   { return BLOCK("性别不一致"); }
    return PASS;
}
```

**姓名比对带有限容错，但绝不是裸的编辑距离。** 取消确认交互后，误判不再有出口 ——
用户只能重传，而重传后大概率仍被识别成同样的字。因此对识别误差留一条窄缝：

```java
// IdentityMatchConstants，硬编码（§1.3）
boolean samePerson(String a, String b) {          // 两侧均已过 §3.0 规范化
    if (a.equals(b))            { return true; }
    if (a.length() != b.length()) { return false; }  // 长度不同不容错
    if (a.length() < NAME_FUZZY_MIN_LEN) { return false; }   // = 3，两字姓名必须完全相等
    if (a.charAt(0) != b.charAt(0))      { return false; }   // 姓氏必须相同
    return editDistance(a, b) <= NAME_MAX_EDIT_DISTANCE;     // = 1，且只差一个字
}
```

**为什么不能只写"编辑距离 ≤ 1"：** 中文姓名多为 2~3 字。对两字姓名容错一个字，
等于容错了**一半的信息量** ——「张三」与「张四」会被判成同一个人，而这恰恰是
§1.1 第 5 条要拦的情形。三条限制缺一不可：

| 限制 | 挡住的情形 |
|---|---|
| 长度必须相同 | 「张伟」vs「张伟明」—— 少一个字通常是漏识别，不是同一个人的证据 |
| 长度 ≥ 3 才容错 | 「张三」vs「张四」、「李娜」vs「李丽」 |
| 首字必须相同 | 「张伟明」vs「王伟明」—— 姓不同基本可以排除 |

**方向是刻意放松的，要知情。** §1.3 那张表里其余词表的未命中路径都走向"更保守"，
这一条相反：它让本该 BLOCK 的极小一部分情形变成 PASS，换取识别误差不再让任务无法完成。
残留风险是**同姓、同性别、同长度、仅差一字**的两个人（如「张伟明」与「张伟民」）被合并。
两个阈值都是推演值，需按 §15.1 标注集校准；若线上出现真实误合并，
正确反应是收紧到"完全相等"，而不是继续放宽。

| 结果 | 处理 |
|---|---|
| `PASS` | 正常合并 |
| `BLOCK` | **task 直接置 `FAILED`，`failReason = IDENTITY_MISMATCH`**，提示「检测到这些文件可能不属于同一个人，请检查后重新上传」。按 §13.1，`FAILED` 会连同原始文件与预览图一起删除，用户修正后重新上传即可 |

**只拦"不同人"，不拦"不同次体检"。** 体检日期相差较大、机构不一致**不再拦截** ——
它们指向的是"同一个人的两次体检"，而不是"两个人"。这是 2026-08-23 的产品决策，
§1.1 第 5 条与 §3.1a 的多文件定义已同步收窄到"同一个人"。

> **为什么不做确认交互。** 「弹窗让用户选『仅使用文件 N』或『仍然合并』」需要一个
> 确认端点、一个 `NEED_CONFIRM` 状态、一份 `confirmPayload`、一套 2 小时 TTL 与重新入队逻辑，
> 而它换来的收益只是省掉用户一次重传。更麻烦的是"仍然合并"这个选项本身 ——
> 它让用户有权推翻一条安全判定，而用户并不比系统更清楚这两份报告是不是同一个人的。
> **直接失败 + 重传，语义单一，没有中间态需要维护。**

**姓名全程只在 Worker 内存中存在。**

本版抽取结果不落库（§10.2），因此不需要"剥离后再持久化"这套动作。三条约束：

```
① 姓名不写入 Redis 结果、不写入日志、不回传前端
② IdentityCheck 在同一次 Worker 执行内完成，用完即随对象一起回收
③ 判定为 BLOCK 时任务立即失败，不保留任何中间抽取结果，也不存在"等待用户确认"的中间状态
```

第 ③ 条是刻意的：一旦存在"等用户确认再继续"的状态，就必须把含姓名与全部化验值的抽取结果
暂存下来，**用一份高敏感数据换一次 LLM-A 调用的成本**。取消确认交互之后，这个存储需求
一并消失 —— 这是本决策在隐私上的额外收益。

> 姓名识别本身也可能出错，因此判定收窄到只比对**确实识别出信息**的文件，
> 且只看姓名与性别这两个最强的身份信号。误判的代价是用户需要重传一次，
> 而不是拿到一份混了两个人的健康画像。

### 4.1 抽取准入规则（需求 §5-2）

需求给了三条精确规则，程序层必须逐条实现，**不能靠提示词软约束**。
关键在于"有明确结论"的判定要走**两级**：指标行标记优先，其次采用分组小结（§4.0.5）。

```java
ConclusionSource resolveConclusion(RawIndicator i, Section sec) {
    // 一级：指标行自身的标记（↑ ↓ H L 偏高 偏低 …）
    if (i.isHasConclusion() && isNotBlank(i.getConclusionText()))
        return ConclusionSource.of(INDICATOR_ROW, i.getConclusionText());

    // 二级：分组小结
    String sum = sec.getSectionSummary();
    if (isBlank(sum)) return ConclusionSource.NONE;               // 真正的无结论

    if (isAllNormalSummary(sum))                                  // 「未见异常」类词表
        return ConclusionSource.of(SECTION_SUMMARY, sum, NORMAL);

    // 【严格】断言"小结点名了本指标"：要求 rawName 完整出现，不做子串/模糊
    if (mentionsIndicatorStrict(sum, i.getRawName()))
        return ConclusionSource.of(SECTION_SUMMARY, sum, extractLabel(sum, i));

    // 前置条件一：小结必须【严格】点名了本组的**其他**指标，才谈得上"未被点名即正常"。
    // 小结若是「建议复查」这类无法归类的文字，不构成对本指标的结论 —— 保守退回 NONE。
    if (!mentionsAnyIndicatorStrict(sum, sec))
        return ConclusionSource.NONE;

    // 前置条件二【宽松】：本指标名与小结只要有**任何**可疑重叠，就不许判正常。
    // 严格匹配的召回率低，"没匹配上"≠"没被点名" —— 见下方说明。
    if (possiblyMentioned(sum, i.getRawName()))
        return ConclusionSource.NONE;

    // 小结存在、严格点名了本组别的指标、且与本指标毫无重叠 ⇒ 本指标正常
    return ConclusionSource.of(SECTION_SUMMARY_IMPLIED, sum, NORMAL);
}
```

**两个方向必须用不同的松紧度，这是本节最容易写错的地方。**

`mentionsIndicatorStrict` 采用严格匹配（`rawName` 规范化后完整出现在小结中），
避免子串误判 ——「总胆固醇」是「低密度脂蛋白胆固醇」的子串，宽松匹配会让小结里
只点名 LDL 的那句话把 TC 也标成偏高。

但**严格匹配的召回率低，因此它的否定结果不可直接采信**：

```
小结原文：低密度脂蛋白偏高(4.26mmol/L)        ← 用了简称
rawName ：低密度脂蛋白胆固醇                   ← 完整名
→ mentionsIndicatorStrict = false
→ 若直接落到 IMPLIED，这项会被判成【正常】—— 而小结明说它偏高
```

这是**往错误方向失效**：一个报告明确指出异常的指标被静默判为正常，
比 §4.1.1 那个"62 项被丢弃"严重得多（丢弃只是少展示，误判正常是错误结论）。

因此 IMPLIED 之前加一道**宽松的否定校验** `possiblyMentioned`：

```java
// 只用于"否定"方向：宁可多拦，不可漏拦。命中即放弃 IMPLIED，退回 NONE（丢弃该项）
boolean possiblyMentioned(String sum, String rawName) {
    String core = stripParenthesized(rawName);                  // 去掉「（TC）」这类括号内容
    if (sum.contains(core)) { return true; }
    // 最长公共子串 ≥ 3 字即视为可疑（「低密度脂蛋白」vs「低密度脂蛋白胆固醇」）
    return longestCommonSubstringLen(sum, core) >= MENTION_SUSPECT_MIN_LEN;   // = 3
}
```

| 方向 | 松紧 | 命中后果 | 失效方向 |
|---|---|---|---|
| 断言"被点名" | **严格** | 采用小结中的结论标签 | 太松 → 张冠李戴，把别人的异常安到本项头上 |
| 否定"被点名" | **宽松** | 放弃 IMPLIED，该项丢弃 | 太松 → 把小结点名的异常项判成正常 |

**两侧都保守的净效果：** 名称对不齐时该指标**不展示**，而不是被判成正常。
少展示一项是能力边界，判错一项是错误结论 —— §1.2 的安全不对称在这里同样适用。
`MENTION_SUSPECT_MIN_LEN` 与匹配规则均为推演值，需按 §15.1 标注集校准
（重点看 `droppedNoConclusionRatio` 是否因此回升）。

```java
for (RawIndicator i : raw.getIndicators()) {
    boolean hasValue = hasDisplayableResult(i);      // 见下，不再要求是数字
    ConclusionSource c = resolveConclusion(i, sectionOf(i));

    if (hasValue && c != NONE) {
        → 模块一 健康指标卡片
        → 若结论为异常，同时进入 模块二 健康问题（指标异常类）
    } else if (hasValue) {
        → 丢弃（需求：仅列数值但未给结论的指标，不在本模块展示）
        → 计入 droppedNoConclusionCount，仅用于监控
    } else if (c != NONE) {
        → 转为 textualFinding，进入 模块二 健康问题
    } else {
        → 丢弃
    }
}
```

**三条配套约束：**

1. **`isAllNormalSummary` 的词表必须可穷举且保守**（`未见异常 / 正常 / 无异常 / 各项正常 / 未见明显异常`）。
   命中才判 NORMAL，不命中一律走后续分支 —— 与 §4.3 的 `UNRECOGNIZED` 同向。
2. **`SECTION_SUMMARY_IMPLIED` 是本次唯一新增的推断**，必须限定得极窄：
   仅当"该组有小结、小结不是全组正常、且小结点名了本组其他指标"时成立。
   它的语义是"报告已逐条列出本组异常项，未被列出的即正常"——
   这是体检报告小结的固定写法，不是我们的猜测。
3. **展示时区分来源。** 卡片的"报告结论"字段：
   - `INDICATOR_ROW` → 展示指标行原文（如「↑」）
   - `SECTION_SUMMARY` → 展示小结中与本指标相关的片段
   - `SECTION_SUMMARY_IMPLIED` → 展示「正常」，并在卡片附「依据：本组小结"未见异常"」的来源标注

#### 4.1.0 `hasDisplayableResult`：三要素齐备即准入

**判据不是"值能不能解析成数字"，而是"这一项能不能填满卡片的三个字段"。**

```java
boolean hasDisplayableResult(RawIndicator i) {
    return isNotBlank(i.getValue())              // ① 有检测结果（任意形态）
        && isNotBlank(i.getReferenceRange());    // ② 有参考范围
    // ③ 有结论 → 由 resolveConclusion 单独判定（两级，§4.1）
}
```

| 实例 | 结果 | 参考范围 | 结论 | 准入 |
|---|---|---|---|---|
| 甘油三酯 `1.69 mmol/L` | ✓ | `0～1.70` | `未见异常`（小结） | ✓ |
| 上皮细胞(镜检) `2-5/HP` | ✓ | `0~3` | `偏高` | **✓（原规则会丢弃）** |
| 红细胞(镜检) `+血尿` | ✓ | `负~3` | `偏高` | **✓（原规则会丢弃）** |
| 尿蛋白 `阴性` | ✓ | `阴性` | `未见异常`（小结） | **✓（原规则会丢弃）** |
| 肝胆B超「脂肪肝」 | — | ✗ 无参考范围 | ✓ | ✗ → 转 `textualFinding` |

**为什么必须放宽 —— 丢弃的代价很具体：**

镜检类与带符号结果往往正是健康问题的**源头指标**。实测某报告的「血尿」是一条严重级健康问题，
其来源就是「红细胞(镜检) +血尿」和「上皮细胞(镜检) 2-5/HP」两项。
按原规则这两项被丢弃 → 健康问题模块里的「血尿」**找不到可关联的指标卡片** →
需求 §6-3 的关联跳转按钮失效。

**值原样展示，不做任何解析。** 卡片上的"检测值"字段显示 `2-5/HP`、`+血尿`、`阴性`，
与报告原文一致（§0 原则 2）。**这类项不参与 §4.4a 的数值交叉校验**，
`verifyStatus` 标 `UNVERIFIED` —— 那是能力边界，不是错误。

#### 4.1.0a 「有参考范围」是健康指标与健康问题的分界线

放宽之后，§5-2 第三条（"仅给文字结论但无数值的指标归入健康问题"）的判据也随之收窄：

```
有参考范围  → 健康指标模块（无论值是数字、半定量还是定性）
无参考范围  → 健康问题模块（textualFinding）
```

> 这同时修掉了原规则的一处别扭：尿蛋白「阴性」是一个**正常结果**，
> 按原规则却会被塞进"健康问题"模块。一个正常项出现在健康问题里，用户看着会困惑。
> 现在它正确地落在健康指标模块，状态标签为「正常」。

**`textualFinding` 的判据因此是"无参考范围的文字结论"**，典型如
「肝胆B超：轻度脂肪肝」「甲状腺弥漫性病变」—— 这些确实没有可比对的区间。

**监控项 `droppedNoConclusionRatio`：** 加入二级判定后，该比例应显著下降。
若仍 > 50%，说明遇到了既无行标记、又无分组小结的报告样式，需回到需求层面讨论。

#### 4.1.1 真实样本验证（这条规则的由来）

某三甲医院 17 页标准体检报告，71 项数值指标：

| | 一级判定（V1.5.3） | 两级判定（本版） |
|---|---|---|
| 进入健康指标模块 | **9 项** | **71 项** |
| 被丢弃 | 62 项 | 0 项 |
| `droppedNoConclusionRatio` | **87%** | **0%** |
| 总览条 | 总指标 9 · 正常 0 · 异常 9 · **异常占比 100%** | 总指标 71 · 正常 62 · 异常 9 · **异常占比 13%** |

**「异常占比 100%」会出现在一份大部分指标正常的报告上。**
这不是边缘 case —— 那是一份完全标准的三甲医院报告，
只是把结论写在分组小结而非提示列。这个失败模式的覆盖面可能是全部医院的多数。

### 4.2 分组与顺序保持（需求 §5-4）

```java
class IndicatorGroup {
    String sectionName;     // 报告原文章节名，不做任何映射
    int displaySectionOrder;
    List<Indicator> items;  // 组内按报告原始顺序
}
```

1. `sectionName` **原样使用，不做标准化映射**。报告写「血脂四项」就显示「血脂四项」。
2. 章节顺序 = 报告中首次出现顺序（fileIndex 升序 + pageNo 升序 + 页内 Y 坐标升序）。
3. 组内顺序 = 报告原始顺序，**不按异常/正常重排，不按重要性重排**（需求 §6-4 同理）。
4. 未分组兜底：`sectionName = "未分组"` 的组排在所有具名章节之后，展示名「其他检查项目」（`DisplayConstants.UNGROUPED_LABEL`）。

### 4.3 报告结论归一化 → 状态标签（需求 §5-3）

```java
enum ConclusionLabel { NORMAL, HIGH, LOW, OTHER_ABNORMAL, UNRECOGNIZED }
```

**归一化词表（`ConclusionLabelWords` 常量类，改动走发版，理由见 §1.3）：**

```
NORMAL:  正常, 未见异常, 阴性, (-), -, 正常范围内, 无异常, 参考范围内
HIGH:    ↑, ↑偏高, 偏高, 高, H, 增高, 升高, 偏高↑, 异常↑
LOW:     ↓, ↓偏低, 偏低, 低, L, 降低, 减低, 偏低↓, 异常↓
OTHER_ABNORMAL:  阳性, (+), +, ++, +++, 异常, 阳性(+), 可疑
```

**关键设计：NORMAL 词表可穷举，其余一律保守处理。**

```java
ConclusionLabel classify(String raw) {
    String s = TextNormalizer.normalize(raw);      // §3.0，含 NFKC + 部首映射 + 去空白
    if (NORMAL_SET.contains(s))  return NORMAL;
    if (matchAny(s, HIGH_SET))   return HIGH;
    if (matchAny(s, LOW_SET))    return LOW;
    if (matchAny(s, OTHER_SET))  return OTHER_ABNORMAL;
    return UNRECOGNIZED;     // 保守：计入异常，卡片原样展示结论文字
}
```

| Label | 展示文案 | 颜色 | 计入总览 |
|---|---|---|---|
| `NORMAL` | 正常 | 绿色 | 正常项 |
| `HIGH` | 偏高 ↑ | 红色 | 异常项 |
| `LOW` | 偏低 ↓ | 蓝色 | 异常项 |
| `OTHER_ABNORMAL` | 异常 | 橙色（待产品确认） | 异常项 |
| `UNRECOGNIZED` | **原样展示报告结论文字** | 灰色（待产品确认） | 异常项 |

卡片上的"报告结论"字段**始终展示 `conclusionText` 原文**，状态标签只是它的可视化。

### 4.4 交叉校验

#### 4.4a 结论 vs 数值交叉校验

**结论是要展示的真相，计算值是校验工具。**

**性别从哪来（跨文件唯一口径）：**

```java
// 参与校验的性别 = 全部文件中非 null 的性别取值
//   全部一致        → 用该性别
//   一个都没有      → UNKNOWN
//   互相冲突        → 不可能走到这里：§4.0a 已判 BLOCK，任务在此之前就失败了
//                     （防御性分支仍写成 UNKNOWN，不得默认取第一个）
Gender resolveGender(List<ReportMeta> metas) {
    Set<Gender> known = metas.stream().filter(Objects::nonNull)
            .map(ReportMeta::getGender).filter(Objects::nonNull)
            .collect(Collectors.toSet());
    if (known.size() == 1) { return known.iterator().next(); }
    return Gender.UNKNOWN;
}
```

`Gender.UNKNOWN` 时性别相关参考区间一律 `UNPARSEABLE` → `UNVERIFIED`，
**不得默认按男性**（女性血红蛋白 120 会被误判偏低）。

```java
public VerifyStatus crossCheck(Indicator i, Gender gender) {   // gender 由 resolveGender 解出
    ConclusionLabel reported = i.getConclusionLabel();
    Range r = parseRange(i.getReferenceRange(), gender);

    if (r == Range.UNPARSEABLE)          return UNVERIFIED;
    if (!isUnitAllowed(i))               return UNVERIFIED;
    if (!isParsableNumber(i.getValue())) return UNVERIFIED;   // 半定量/定性项不参与数值校验
    if (reported == UNRECOGNIZED
     || reported == OTHER_ABNORMAL)      return UNVERIFIED;

    ConclusionLabel computed = compute(parse(i.getValue()), r);
    return reported == computed ? CONSISTENT : CONFLICT;
}
```

报告上的结论标记与数值是同一页上两个独立的视觉元素，模型读错数值时标记大概率仍是对的：

```
真实：血红蛋白 98 ↓偏低
误读：血红蛋白 138，结论仍抄到「↓偏低」
→ 计算 = NORMAL，报告结论 = LOW → CONFLICT，立刻暴露
```

**`CONFLICT` 的处理：** 卡片标存疑角标 + 红框；**仍正常进入健康问题模块**
（问题名称来自结论文字，不依赖数值，删掉反而会漏报真实异常）；不影响饮食建议与菜品推荐
（两者本就不由指标驱动）。

**参考区间解析：**

```java
private static final Pattern RANGE = Pattern.compile(
    "^\\s*(-?\\d+\\.?\\d*)\\s*[-－~～—–一]\\s*(-?\\d+\\.?\\d*)\\s*$");
private static final Pattern UPPER = Pattern.compile("^\\s*[<≤﹤]\\s*(\\d+\\.?\\d*)\\s*$");
private static final Pattern LOWER = Pattern.compile("^\\s*[>≥﹥]\\s*(\\d+\\.?\\d*)\\s*$");
```

三个必须处理的点：
1. **性别未知不要默认按男性** —— 女性血红蛋白 120 会被误判偏低，直接 `UNPARSEABLE` 降级。
2. **量级合理性检查** —— `value` 与区间中点相差超过 100 倍 → `UNPARSEABLE`。
3. **边界闭区间** —— `4.0-10.0` 的 `10.0` 判 NORMAL，浮点转 `BigDecimal`。

#### 4.4b 行级/单元格级联合校验

单值匹配（`pageText.contains(value)`）的漏洞：

```
白细胞    6.8        LLM 误识别：{"rawName":"血红蛋白","value":"198"}
红细胞    4.98       contains("198") → true → 被判一致
血红蛋白  156        但 198 是血小板的值
血小板    198        ← 表格错位被静默确认
```

**校验分两档，按文本层来源自动选择（§3.2）：**

##### A档 单元格级精确比对（DOC-VL 表格区，首选）

HTML 直接给出行列关系，可以做比"同行"严格得多的校验：

```java
// <tr><td>总胆固醇（TC）</td><td>6.47</td><td>mmol/L</td><td>0～5.72</td><td>↑</td></tr>
Optional<HtmlRow> row = table.findRowByFirstCell(item.getRawName());
if (!row.isPresent()) { return UNCERTAIN; }        // Java 8：不能用 Optional.isEmpty()

boolean ok = eq(row.cell(colIndexOf("检查结果")), item.getValue())
          && eq(row.cell(colIndexOf("参考值")),   item.getReferenceRange())
          && eq(row.cell(colIndexOf("提示")),     item.getConclusionText());
return ok ? CONSISTENT : CONFLICT;
```

**这比原来的启发式强一个量级** —— 不只是"项目名和值出现在同一行"，
而是"值在检查结果列、参考区间在参考值列、标记在提示列"。列名从表头行动态解析，
不假设固定列序（实测两份报告的列序不同：一份是 `结果/单位/参考值/提示`，
另一份是 `结果/提示/参考区间/单位`）。

**空单元格必须保留。** 实测 DOC-VL 输出中未命中提示的行是 `<td></td>` 而非省略 —— 
若塌陷，后续所有列会左移一位，参考区间会被当成结论。解析时校验每行 `<td>` 数量与表头一致，
不一致则该表降级为 B 档。

##### B档 行级联合校验（原生文本层 / 非表格区，兜底）

```java
public VerifyStatus verifyByLine(Indicator item, PageResult page) {
    // 门槛是"有没有文本层"，不是"路由模式是不是 IMAGE"（§3.1）。
    // 拍照件 mode=IMAGE 但 textLayerSource=DOC_VL，必须继续校验。
    if (page.getTextLayerSource() == NONE) { return IMAGE_ONLY; }

    List<TextLine> lines = page.getLines();

    // 1. 项目名 + 值 + 结论 同行
    if (lines.stream().anyMatch(l -> fuzzyContains(l.text, item.getRawName())
            && containsNormalized(l.text, item.getValue())
            && containsNormalized(l.text, item.getConclusionText()))) return CONSISTENT;

    // 2. 放宽：项目名 + 值 同行（↑↓ 常为矢量图形，原生文本层无字符）
    if (lines.stream().anyMatch(l -> fuzzyContains(l.text, item.getRawName())
            && containsNormalized(l.text, item.getValue()))) return CONSISTENT;

    // 3. 相邻行重试：项目名过长换行，数值被挤到下一行
    for (int i = 1; i < lines.size(); i++) {
        String merged = lines.get(i-1).text + lines.get(i).text;
        if (fuzzyContains(merged, item.getRawName())
         && containsNormalized(merged, item.getValue())
         && isAdjacent(lines.get(i-1), lines.get(i))) return CONSISTENT;
    }
    return UNCERTAIN;
}
```

> B 档第 2 步的放宽在 A 档不需要 —— DOC-VL 能识别出 `↑` 图形并放入提示列（实测 6/6），
> 因此 A 档可以要求结论列也一致。

**最终状态取所有校验的合并结果**（`CONFLICT` 优先级最高，其次 `UNCERTAIN`）：

| 状态 | 含义 | 卡片展示 |
|---|---|---|
| `CONSISTENT` | 至少一个文本层来源与 LLM-A 一致 | 正常 |
| `IMAGE_ONLY` | **DOC-VL 与原生文本层均不可用**（罕见） | 正常 + 模块底部提示"建议核对原报告" |
| `UNVERIFIED` | 参考区间/单位无法解析 | 正常展示（能力边界，不是错误） |
| `UNCERTAIN` | 有文本层但对不上，或两个文本层来源互相矛盾 | 角标「识别存疑」 |
| `CONFLICT` | 结论与数值矛盾，或单元格级比对失败 | 角标「识别存疑，请核对原报告」+ 红框 |

> **`IMAGE_ONLY` 从常态变成了罕见状态。** 启用 §3.11 后，拍照页面同样有文本层，
> 只有 DOC-VL 质量不达标且无原生文本层时才会出现。

### 4.5 原文回溯校验

需求有 8 处要求"引用报告原文"。一句伪造的原文会同时污染多个模块，且用户**无法自行发现**
（他信任的正是"这是原文"这个承诺）。

**比对单元是"有界的文本单元"，绝不是整页拼接串。**

```java
public EvidenceStatus verifyEvidence(String evidenceText, PageResult page) {
    // 同 §4.4b：拍照件 mode=IMAGE 但有 DOC-VL 文本层，不能在这里短路，
    // 否则幻觉引用在拍照场景完全无人把守（§1.1 失败 2），而该场景是预期的主流形态。
    if (page.getTextLayerSource() == NONE) { return EVIDENCE_UNVERIFIABLE; }

    // §3.0.4：校验层禁止裸 String 比对，两侧都必须过同一个 normalize
    NormalizedText norm = NormalizedText.of(evidenceText);

    // 文本单元：原生文本层 → §3.5 的 TextLine（带 bandIndex）；
    //           DOC-VL    → §3.11.6 的 Block（表格单元格 / 段落块）。
    List<TextUnit> units = page.getTextUnits();

    // 1. 单个文本单元内命中
    for (TextUnit u : units) {
        if (u.normalized().contains(norm)) { return EVIDENCE_EXACT; }
    }

    // 2. 同栏相邻单元拼接（原文换行/跨单元格断句，§3.11.3 判据 9 的跨页断句同理）
    for (int i = 1; i < units.size(); i++) {
        TextUnit a = units.get(i - 1);
        TextUnit b = units.get(i);
        // ★ 必须同栏且相邻。跨 bandIndex 拼接会把双栏左右两侧接成一句，
        //   使"报告上连续出现"这个承诺失效 —— 与 §3.5 要防的假一致是同一个失效方向。
        if (a.getBandIndex() != b.getBandIndex() || !isAdjacent(a, b)) { continue; }
        if (a.mergeWith(b).contains(norm)) { return EVIDENCE_EXACT; }
    }

    // 3. 模糊：仍只在单元或同栏相邻窗口内比，不与整页比
    if (bestSimilarityWithinUnits(norm, units) >= 0.85) { return EVIDENCE_FUZZY; }
    return EVIDENCE_NOT_FOUND;
}
```

> **为什么不能用 `page.getFullText()` 整页比对。** 整页文本会把双栏左右拼在一起，
> 而 `normalize()` 又去掉了全部空白：
>
> ```
> 血尿酸 480 208-428 │ 甘油三酯 1.2 3.9-6.1
> 去空白后 → "血尿酸480208-428甘油三酯1.23.9-6.1"
> ```
>
> 模型编造的 `evidenceText`「208-428甘油三酯」会 `contains` 命中，判成 `EVIDENCE_EXACT`。
> §4.4b 为此做了 X 分栏，§4.5 若按整页比对，等于从另一个入口把 §3.5 极力避免的
> **假一致**放了回来，而回溯校验对用户的承诺恰恰是"这是报告上连续出现的原文"。
> 比对单元与 §4.4b B 档共用同一份 `TextLine`，两处校验的口径因此天然一致。

| 状态 | 处理 |
|---|---|
| `EVIDENCE_EXACT` / `EVIDENCE_FUZZY` | 正常使用 |
| `EVIDENCE_UNVERIFIABLE` | 正常使用，**前端提供"查看原图 文件N 第M页"入口**供用户核对 |
| `EVIDENCE_NOT_FOUND` | **该条目整条丢弃**，记录 `hallucinationCount` 告警 |

**本版起，`EVIDENCE_UNVERIFIABLE` 从"拍照场景的常态"变为罕见状态。**
§3.11 使拍照页面同样具备文本层，实测综述建议页的饮食医嘱
「请您戒烟忌酒，低脂、低糖饮食，控制食量」**逐字无损**，回溯可正常进行。
只有 DOC-VL 质量不达标且无原生文本层时才落到该状态。

> 这是本方案挂了多个版本的一处缺口，本版关闭。
> 能力边界中"拍照场景无法程序化验证"一条随之删除。

**丢弃而非降级展示：** 一条回溯不上的"原文"，展示出来就是在撒谎。
需求 §6-3 说明报告原文是"便于用户与纸质报告核对"—— 用户核对不到，这个功能就是负价值。

> **两侧规范化必须包含反 LaTeX（§3.0.2a）。** DOC-VL 会把 `(489μmol/L)`
> 输出成 `$ 489 \mu mol/L $`，而模型看图输出的是原文形态。不做反 LaTeX，
> 这类条目会 100% 判成幻觉被丢弃 —— 而它们恰恰是小结里的关键结论。

**告警阈值：** `hallucinationCount / totalEvidenceCount > 2%` 触发提示词回归检查。

> **排查顺序：先看 `residualNonStandardCount`，再查提示词。**
> 字符规范化缺失会让这个比例瞬间冲到 100%，而根因与提示词毫无关系（§3.0.3）。
> 不先排除字符问题，会在提示词上白查很久。

### 4.6 名称归一化与单位白名单

推荐不由指标驱动，别名表**只剩一个用途：单位白名单校验的键**。

> 原先还写着"多文件同名指标去重"，但 §4.7 的合并规则用的是
> 同 `(sectionName, rawName)` —— **原文名，不经过别名表**。该用途实际已不存在。
> 于是 `IndicatorCode` 这层枚举现在只服务单位白名单一件事；
> 若后续觉得不值得维护，可直接按 `rawName` 别名匹配合法单位、去掉 `code` 中间层（P2 简化项）。
> 保留它的唯一理由是单位白名单的可读性与单测覆盖面。

```java
// IndicatorAliasWords：别名与合法单位一并写死，enum + static final Map
public enum IndicatorCode { UA, TG, TC, LDL_C, HDL_C, FPG, HBA1C, HGB, ALT, AST, CREA, BMI, SBP, DBP, UNKNOWN }

UA:    [尿酸, 血尿酸, UA]              → [μmol/L, umol/L]
TG:    [甘油三酯, 三酰甘油, TG]         → [mmol/L]
TC:    [总胆固醇, 胆固醇, TC, CHOL]     → [mmol/L]
LDL_C: [低密度脂蛋白胆固醇, LDL, LDL-C] → [mmol/L]
FPG:   [空腹血糖, 血糖, 葡萄糖, GLU]    → [mmol/L]
HGB:   [血红蛋白, Hb, HGB]             → [g/L]
HBA1C: [糖化血红蛋白, HbA1c, GHb]      → [%, mmol/mol]
```

单位不在白名单 → `UNVERIFIED`（放弃数值校验），**不做换算**，不影响展示。
`code == UNKNOWN` 的指标完全正常展示 —— 需求要的是展示报告原文，别名表认不认识与能不能展示无关。

### 4.7 多文件合并与全局 ID 分配

**合并前必须通过 §4.0a 同一性校验。**

| 对象 | 合并规则 |
|---|---|
| `reportMeta` | 取第一个非 `null`（按 `fileIndex` 升序）。**性别单独按 `resolveGender()` 解**（§4.4a）：全部非 `null` 取值一致才采用，否则 `UNKNOWN`。姓名只用于 §4.0a 比对，合并后即丢弃，不进入任何输出 |
| `reportOverview` | 取第一个非 `null`（总览数字只印在首页/概览页，§4.0.1a 同口径） |
| `allergenSectionPresent` | OR 合并，`true` 方向需 Java 佐证（§4.0.1a） |
| `sections` | 按 (fileIndex, pageNo, y) 排序；**同名章节跨文件合并为一组**，组内保持各自原顺序拼接 |
| `indicators` | 同 `(sectionName, rawName)` 视为同一项；保留 `verifyStatus` 更优者（`CONSISTENT` > `IMAGE_ONLY` > `UNVERIFIED` > 其余）；同级且值不同 → 标 `CONFLICT` 全部展示 |
| `textualFindings` | 按 `findingName` 去重，`evidenceText` 更长者胜 |
| `summaryConclusions` | **不去重**，按 (fileIndex, pageNo, itemNo) 拼接后重新编号 |
| `allergens` | 按归一化后的 `allergenText` 去重，来源标注合并展示 |
| `doctorAdvices` | 按 (adviceType, 归一化 key) 去重 |

**ID 体系（展示顺序不承担主键职责）：**

```java
// ❌ 不要用 {taskId}-s{sectionOrder}-i{orderInSection}
//    跨文件同名章节合并后，两条都是 s1-i1，直接碰撞

class Indicator {
    String indicatorId;          // UUID 或 taskId + 自增序号，合并后统一分配，唯一
    int    displaySectionOrder;  // 仅用于展示排序
    int    displayItemOrder;     // 仅用于展示排序
    int    fileIndex;            // 证据定位，0 起，与 §10.1.1 预览接口同源
    int    pageNo;
}
```

`linkedIndicatorId`（需求 §6-3 的关联跳转）指向 `indicatorId`，与展示顺序解耦，
后续调整排序规则不会破坏跳转关系。

**证据定位必须带 `fileIndex`：** 只有 `pageNo: 2` 时，多文件场景下"查看原报告第 2 页"
不知道该打开哪个文件。所有 `evidence` 对象统一为 `{fileIndex, pageNo, text, status}`。

> **序号口径全案唯一：`fileIndex` 从 0 开始，取值就是 §9.1.1a 落库的 `file_index`
> （分析请求中 `fileIds` 的提交顺序）。** 不要再引入第二套 1 起的 `fileSeq` ——
> 前端拿到 evidence 后要直接拼 §10.1.1 的
> `/original/{fileIndex}/page/{pageNumber}`，中间任何一次 ±1 换算的后果都是
> **点开原图看到的是另一份报告的页**，而本系统在外部鉴权层之外没有第二道防线（§13.1.2）。
> 用户可见的「报告N」是展示层由 `fileIndex + 1` 生成的，不回流成存储或接口字段。

**多文件下的来源标注：** `总检结论第3条` → `报告2-总检结论第3条`（`fileIndex > 0` 时带文件标识，
展示编号 = `fileIndex + 1`）。

### 4.8 本阶段输出

```json
{
  "indicatorId": "9f2c1a7e-...",
  "sectionName": "血脂检查",
  "displaySectionOrder": 2,
  "displayItemOrder": 3,
  "rawName": "甘油三酯",
  "code": "TG",
  "value": "2.8", "unit": "mmol/L", "referenceRange": "0.56~1.70",
  "conclusionText": "↑偏高", "conclusionLabel": "HIGH", "computedLabel": "HIGH",
  "verifyStatus": "CONSISTENT",
  "evidence": {"fileIndex": 0, "pageNo": 2,
               "text": "甘油三酯 2.8 mmol/L 0.56~1.70 ↑偏高", "status": "EVIDENCE_EXACT"}
}
```

**监控指标：**

| 指标 | 含义 | 告警线 |
|---|---|---|
| `schemaInvalidRatio` | LLM-A 输出不符 Schema 占比 | > 3% 触发提示词回归 |
| `allergenUnknownCount` | `allergenStatus = UNKNOWN` 的任务数 | **非 0 即告警** |
| `blockedAdviceCount` | Tier-0 命中被 `BLOCKED` 的医嘱数，**按 `SafetyAdviceKey` 分维度**（只计数不记原文，§13.1.1） | 占比 > 10% 需排查是否某个裸词（如 `孕`、`儿童`）误伤面过大 |
| `safetyFlagMissingCount` | LLM-A 未输出 `safetyFlag` 的医嘱数 | 占比 > 5% 触发提示词回归 —— 第二道保险正在静默失效 |
| `identityBlockRatio` | 多文件同一性校验拦截占比 | 用于评估交互文案 |
| `droppedNoConclusionRatio` | 因无结论被丢弃的指标占比 | > 50% 需回头讨论需求 |
| `conflictRatio` | 结论与数值矛盾占比 | > 5% 说明识别质量下滑 |
| `hallucinationRatio` | 原文回溯失败占比 | > 2% 触发提示词回归 |
| `unrecognizedConclusionRatio` | 结论词表未命中占比 | > 10% 需补词表 |
| `imageOnlyRatio` | 无文本层页面占比 | 评估 §3.11 投入 |

---

## 5. 模块一：健康指标（需求 §5）

### 5.1 数据来源

**仅** 来自 §4.1 准入规则中「有结果 + 有参考范围 + 有结论」三要素齐备的指标（§4.1.0）。
结果不要求是数字，`2-5/HP`、`+血尿`、`阴性` 同样准入；**有无参考范围**才是本模块与
健康问题模块的分界线（§4.1.0a）。不做任何补充、推断、二次分析。

### 5.2 卡片字段

| 字段 | 来源 | 说明 |
|---|---|---|
| 指标名称 | `rawName` | 报告原文名称，不做标准化。报告写「血尿酸」就显示「血尿酸」 |
| 检测值 + 单位 | `value` + `unit` | 拼接展示，如「2.8 mmol/L」。单位缺失时只展示数值。**半定量与定性结果原样展示**（`2-5/HP`、`+血尿`、`阴性`），不做解析或改写（§4.1.0） |
| 参考正常范围 | `referenceRange` + `unit` | 报告原文，如「0.56~1.70 mmol/L」。**报告未给参考范围时展示「报告未提供」，不填充通用参考值** |
| 报告结论 | `conclusionText` | 原文直引，如「↑偏高」 |
| 状态标签 | `conclusionLabel` | 见 §4.3 映射表 |
| 存疑角标 | `verifyStatus` / `evidenceStatus` | `CONFLICT`/`UNCERTAIN` 时展示 |

> **"报告未提供参考范围"这一条必须硬守。** 填入通用参考值是最容易被忽视的"额外推断" —— 它看起来是善意补全，实际上违反需求 §5-1，且不同实验室参考区间不同，填错会误导用户。

### 5.3 分组展示

```
按 sectionOrder 升序渲染分组
  分组标签 = sectionName（报告原文章节名）
  组内按 orderInSection 升序渲染卡片
```

**不做的事：** 不按异常优先重排、不折叠正常项、不合并相似章节、不做重要性排序。

> 若产品希望"异常项置顶"，需在需求层面单独提出 —— 那与 §5-4「同一分组内的指标按报告原始顺序排列」直接冲突。

### 5.4 指标总览条（需求 §5-5）

```java
int total    = allDisplayedIndicators.size();          // 注意：仅统计已展示的
int normal   = count(l -> l == NORMAL);
int abnormal = total - normal;                         // HIGH + LOW + OTHER + UNRECOGNIZED
int ratio    = total == 0 ? 0
             : BigDecimal.valueOf(abnormal * 100.0 / total)
                         .setScale(0, RoundingMode.HALF_UP).intValue();
```

展示：`总指标 71 项 · 正常 62 项 · 异常 9 项 · 异常占比 13%`

#### 5.4.1 报告自带总览数字时，优先展示报告的

部分体检机构在首页/概览页直接印出汇总数字（实测某连锁机构：`136项总指标 / 0项重要异常 / 13项异常结果`）。

**此时两个数字会同时存在于同一份报告里，用户一定会对比。**
而我们按 §5.4 算出的口径必然不同 —— 报告把定性项与文字所见也计入总数，
我们只统计有数值的项，实测该报告我们只会数出约 79 项，"异常 3 项"看起来还比报告的"13 项"少得多，
显得系统漏了一堆。

```java
// evidenceVerified 的口径是明确的：只有回溯到真实原文才算数
boolean evidenceVerified(ReportOverview ov) {
    EvidenceStatus st = verifyEvidence(ov.getEvidenceText(), pageOf(ov.getPageNo()));
    return st == EVIDENCE_EXACT || st == EVIDENCE_FUZZY;   // UNVERIFIABLE 不算
}

if (reportOverview != null && evidenceVerified(reportOverview)) {
    展示报告原文数字 + 来源标注「以上数字取自报告第 N 页」
    我方统计结果不展示，仅作监控（用于发现抽取遗漏）
} else {
    展示 §5.4 计算结果
}
```

> **`EVIDENCE_UNVERIFIABLE` 必须退回自算，不能当作已核实。** 该状态现在虽已罕见
> （仅 DOC-VL 不达标且无原生文本层时出现，§4.5），但它意味着"我们没法确认报告上真印了这几个数"。
> 把它当 verified，一组模型编造的「136 项 / 13 项异常」就会被冠以"取自报告第 N 页"展示出去 ——
> 而用户**无法自行发现**，他信任的正是"这是报告印的数字"这个承诺（§1.1 第 2 条）。
> 代价是同一份报告在无文本层的页面上会退回自算口径，两台设备可能显示不同数字；
> 但"数字口径不一致"是可解释的，"伪造的报告原文"不是。

**依据是需求 §5-1「严格依据报告原文」** —— 报告自己印的数字才是原文，
我们算的是二次统计。这条同时更省事：口径冲突的问题直接消失。

> 报告自己的数字也未必自洽（实测该报告图示"13项异常"，而阳性结果列表只列了 12 条，
> 第 13 项龋齿在另一章节）。**但仍应照抄** —— 展示报告原文不代表为报告的算术背书，
> 这与 §4.4a 中"结论与数值矛盾时仍展示报告结论"是同一原则。

**三个必须处理的细节（无 `reportOverview` 时适用）：**

1. **`total` 是"已展示的指标数"，不是报告上的全部项目数。**
   加入 §4.1 的两级判定后两者通常已经一致，但仍可能有分组既无小结也无行标记。
   **`droppedNoConclusionCount > 0` 时**，总览条下加一行小字
   「已提取报告中有明确结论的 N 项指标」。**需评审确认文案。**
2. **`total == 0` 时隐藏整个总览条**，模块内展示「本次报告未提取到带明确结论的指标数据」。
3. **上线前必须用真实报告校验这个数字。**
   「异常占比」是用户在整个功能里看到的第一个数字，也是他对系统是否靠谱的第一印象。
   一份大部分正常的报告显示"异常占比 100%"，用户会直接判定功能有问题 ——
   而且他是对的。**这个指标进 §15.1 的必测项。**

### 5.5 底部声明

```
以上指标数据均来自体检报告原文，仅供参考，如有疑问请咨询医生。
```

（常量，`DisclaimerConstants.HEALTH_INDICATOR`）

### 5.6 边界与降级

| 场景 | 处理 |
|---|---|
| 无任何合格指标 | 隐藏总览条，展示空态文案，模块仍渲染（保留底部声明） |
| 全部指标 `IMAGE_ONLY` | 正常展示，模块底部增加一行「本次为图片识别结果，建议与原报告核对」 |
| 某指标 `CONFLICT` | 卡片红框 + 角标，点击展开显示 `evidenceText` 与"查看原图第 N 页" |
| 参考范围为性别相关且性别未知 | 展示原文（如「男130-175 女115-150」），`verifyStatus = UNVERIFIED` |

---

## 6. 模块二：健康问题（需求 §6）

### 6.1 两类来源（严格限定）

来源在**概念上是两类**（指标类 / 总检结论类），但指标类有两种条目形态，
`title` 生成与来源标注模板对二者的处理不同（§6.2），因此枚举定义为三个值：

```java
enum IssueSourceType {
    INDICATOR_NUMERIC,   // 指标异常结论：来自 §4.8 的 indicators，title 需拼接
    INDICATOR_TEXTUAL,   // 文字结论项：来自 textualFindings，title 原文直取
    SUMMARY              // 总检结论 / 医生建议：title 为整条原文
}
// 排序用的 sourceTypeOrder：INDICATOR_NUMERIC = INDICATOR_TEXTUAL = 0，SUMMARY = 1（§6.3）
```

| 来源 | 数据来源 | 生成条件 |
|---|---|---|
| `INDICATOR_NUMERIC` 指标异常结论 | §4.8 的 indicators | `conclusionLabel != NORMAL`（即 HIGH/LOW/OTHER_ABNORMAL/UNRECOGNIZED） |
| `INDICATOR_TEXTUAL` 文字结论项 | §4.0 的 textualFindings | 全部（有结论无数值，如「肝胆B超：脂肪肝」） |
| `SUMMARY` 总检结论/医生建议 | §4.0 的 summaryConclusions + doctorAdvices | 全部条目 |

**除这三条以外，不生成任何健康问题。** 不做疾病关联（如「甘油三酯偏高 + BMI 偏高 → 代谢综合征风险」），不做严重程度分级。

> `textualFindings` 归入 `INDICATOR_TEXTUAL` 而非 `SUMMARY` 类，依据是需求 §5-2 第三条「报告中仅给文字结论但无数值的指标（如「肝胆B超：脂肪肝」），归入健康问题模块展示」—— 它本质上仍是"某项检查的结论"，排序上应与指标异常同组。**需评审确认。**

### 6.2 条目字段（需求 §6-3）

| 字段 | 取值 | 示例 |
|---|---|---|
| `title` 问题名称 | **原文直引，不改写** | 报告写「脂肪肝」→ 展示「脂肪肝」，不改为「肝脏脂肪浸润」 |
| `sourceLabel` 来源标注 | 按类型模板生成 | `血脂检查–甘油三酯` / `总检结论第3条` |
| `originalText` 报告原文 | `evidenceText` | 「甘油三酯 2.8 mmol/L 0.56~1.70 ↑偏高」 |
| `linkedIndicatorId` 关联指标 | 有则填，无则 `null` | `9f2c1a7e-...`（UUID，**不得用 `s2-i3` 这类展示序号，§4.7**） |

**`title` 的生成规则（这是最容易出错的地方）：**

```java
switch (sourceType) {
    case INDICATOR_NUMERIC:
        // 报告结论文字本身通常不成句（「↑偏高」），需拼接
        title = rawName + conclusionDisplayText;   // 「甘油三酯」+「偏高」
        break;
    case INDICATOR_TEXTUAL:
        title = findingName;                       // 「脂肪肝」，原文直取
        break;
    case SUMMARY:
        title = evidenceText;                      // 总检结论整条原文
        break;
}
```

> **需求 §6-2 的示例「甘油三酯偏高」「血红蛋白偏低」本身就是拼接结果**（报告上印的是「甘油三酯 2.8 ↑」）。这个拼接是必要的、机械的、不引入新信息的。但必须用**归一化后的展示词**（偏高/偏低/异常），不能用原始符号（「甘油三酯↑」读起来不像问题）。**这是本模块唯一允许的文字加工，需在评审中明确。**

**`sourceLabel` 模板：**

```
INDICATOR_NUMERIC  →  {sectionName}–{rawName}          「血脂检查–甘油三酯」
INDICATOR_TEXTUAL  →  {sectionName}–{findingName}      「肝胆B超–脂肪肝」
SUMMARY            →  {summarySectionName}第{itemNo}条
SUMMARY(多文件)    →  {fileLabel}-{summarySectionName}第{itemNo}条
```

**`summarySectionName` 取报告原文的章节标题，不写死「总检结论」。** 各机构叫法不同：

| 报告样式 | 实际章节名 | 生成的来源标注 |
|---|---|---|
| 公立医院常见 | 综述建议 | 「综述建议第5条」 |
| 连锁机构常见 | 专家建议与指导 | 「专家建议与指导第2条」 |
| 部分报告 | 总检结论 | 「总检结论第3条」 |

写死「总检结论」会让来源标注与用户手上的纸质报告对不上 ——
而需求 §6-3 的原话就是"便于用户与纸质报告核对"，对不上等于这个字段失效。
`summarySectionName` 由 LLM-A 随 `summaryConclusions` 一并输出。

**`linkedIndicatorId` 规则（需求 §6-3 第四条）：**

```java
// 来自指标异常 → 填对应 indicatorId，前端展示【查看指标】跳转按钮
// 来自 textualFinding → null（无对应数值指标卡片）
// 来自 summaryConclusion → null（需求明确：不直接对应某个指标则不展示关联按钮）
```

> **不做"总检结论智能关联指标"。** 「超重，建议控制体重」看起来能关联 BMI，但那是推断。需求写得很清楚：「若该问题来自总检结论且不直接对应某个指标，则不展示关联按钮」。

### 6.3 排序（需求 §6-4）

```java
issues.sort(comparing(Issue::getSourceTypeOrder)      // INDICATOR_NUMERIC/TEXTUAL=0, SUMMARY=1
      .thenComparing(Issue::getSectionOrder)          // 报告章节顺序
      .thenComparing(Issue::getOrderInSection));      // 章节内原顺序
// SUMMARY 类的 sectionOrder 统一为 MAX，orderInSection = itemNo
```

**不做严重程度分级，不做风险排序。**

### 6.4 空态（需求 §6-5）

```java
boolean allNormal = displayedIndicators.stream().allMatch(i -> i.label == NORMAL)
                 && textualFindings.isEmpty()
                 && summaryConclusions.stream().allMatch(this::isNoAbnormalHint)
                 && doctorAdvices.isEmpty();   // §6.1：SUMMARY 类来源 = summaryConclusions + doctorAdvices
```

`isNoAbnormalHint`：总检结论条目归一化后命中「未见明显异常 / 各项指标正常 / 本次体检未见异常」等词表。

`allNormal == true` 时展示：

```
本次体检各项指标均在正常范围内，请继续保持良好的生活习惯。
```

> **注意判定的保守性：** 只有当总检结论**能被词表明确判定为"无异常提示"**时才算 allNormal。总检结论存在但无法判定 → 照常作为健康问题条目展示，不走空态。宁可多展示一条，不可漏报。

### 6.5 底部声明

```
以上内容均为体检报告原文结论的汇总，不构成二次诊断，如有疑问请咨询医生。
```

（常量，`DisclaimerConstants.HEALTH_ISSUE`）

---

---

---

## 7. 模块三：饮食建议（需求 §7）

### 7.1 三类来源（严格限定）

| 来源 | 识别方式 | LLM-A 输出位置 |
|---|---|---|
| 报告中明确的过敏原 | 过敏原检查章节的阳性项、报告中"对X过敏"表述 | `allergens[]` |
| 明确建议补充的营养物质 | 医生建议中的"建议补充X""建议增加X摄入" | `doctorAdvices[NUTRITION_SUPPLEMENT]` |
| 总检结论中的饮食要求 | 总检结论中的饮食相关建议 | `doctorAdvices[DIET_REQUIREMENT]` |

**LLM-A 输出的是"报告里提到了什么"，不是"应该吃什么"。** 内容生成是 LLM-B 的职责，
两者不能合并成一次调用：抽取受原文约束而生成不受，混在一起会诱导模型把生成内容当原文输出；
且两者缓存粒度不同、失败处理不同。

### 7.2 Safety Normalize —— 安全分类必须前置于归一化

**这是本模块的第一道闸门。** 若先归一化，无法识别的建议会被折叠成 `OTHER`，
而 `OTHER` 恰恰最需要被安全规则拦住 ——「建议限制碘摄入」若先归一化成 `OTHER`，
再去查安全黑名单就永远查不中。

```
raw adviceText
      │
      ▼
① Tier-0 安全分类  Java 关键词 OR 模型 safetyFlag（或运算，取高召回）
      ├─ 命中 → BLOCKED，止步：不归一化、不生成内容、不参与菜品匹配
      ▼
② Tier-1 adviceKey 归一化
      ├─ LLM-A 给候选 key → Java 用枚举 + 别名表校正
      ├─ 命中枚举 → 正常流程
      └─ 未命中 → OTHER（纯枚举值，不带原文后缀）
                    不查/写建议缓存，不调用 LLM-B，不进生成队列，不参与菜品匹配
                    只展示本条报告原文、来源与未收录提示，并增加 adviceKeyMissCount
```

#### Tier-0 安全键（命中即 BLOCKED）

```java
enum SafetyAdviceKey {
    IODINE_RESTRICT, IODINE_SUPPLEMENT,                        // 碘方向双向，取决于诊断
    LOW_PROTEIN, LOW_POTASSIUM, LOW_PHOSPHORUS, RENAL_DIET,    // 肾病，依赖分期
    DRUG_INTERACTION,                                          // 报告不含用药全貌
    PREGNANCY, LACTATION, PEDIATRIC                            // 摄入量标准不同
}

// Java 关键词（宽松匹配，宁可误拦）
BLOCKED_PATTERNS = {
  "碘", "甲状腺.{0,6}饮食",
  "低蛋白", "优质低蛋白", "限制蛋白", "限钾", "低钾", "高钾", "限磷", "低磷",
  "透析", "肾功能不全.{0,6}饮食", "肌酐.{0,10}饮食",
  "华法林", "抗凝.{0,6}维生素K", "服药期间.{0,6}饮食", "忌与.{0,6}同服",
  "孕", "妊娠", "哺乳", "儿童", "婴幼儿", "青少年"
};
```

```java
boolean blocked = matchAnyPattern(rawAdviceText, BLOCKED_PATTERNS)
               || advice.getSafetyFlag() != null;     // 模型第二意见，或运算

// safetyFlag 是可选子字段（§4.0.1）：缺失 == null == "模型未给出第二意见"，
// 此时 Tier-0 完全由 Java 关键词承担。这是刻意接受的退化，但必须可观测：
if (advice.isSafetyFlagAbsent()) { metrics.incr("safety_flag_missing"); }
```

**安全键与业务键必须是一套体系。** `LOW_PROTEIN` 等只存在于 Tier-0，
不在 Tier-1 枚举中出现 —— 避免两套清单各有一份、判断落到哪一份取决于代码路径。

**BLOCKED 的展示：** 仍展示报告原文、来源标注（这是报告事实，必须展示），
内容区显示「该项建议涉及需个体化制定的饮食方案，请遵医嘱」。**不参与菜品匹配。**

#### Tier-1 业务 adviceKey（只是键，不含内容）

```
NUTRITION_SUPPLEMENT:
  IRON PROTEIN CALCIUM VITAMIN_D VITAMIN_B12 VITAMIN_C FOLATE ZINC
  DIETARY_FIBER MAGNESIUM OTHER

DIET_REQUIREMENT:
  LOW_FAT LOW_SALT LOW_SUGAR LOW_PURINE LOW_CHOLESTEROL HIGH_FIBER
  LIGHT_DIET ENERGY_CONTROL REGULAR_MEAL OTHER

ALLERGEN:
  SHRIMP_CRAB FISH MILK EGG PEANUT SOY WHEAT NUTS MANGO BEEF MUTTON OTHER
```

**归一化由 LLM-A 出候选、Java 定终审：** 模型输出 `adviceKeyCandidates`（**数组**），
Java 逐个校验是否在枚举内；不在枚举内的用 `AdviceKeyAliasWords` 常量表二次匹配，
去重后仍为空才落 `OTHER`。模型负责应对千变万化的说法，Java 负责保证结果一定是合法枚举值。

**一条原文命中多个键时，按键拆成多张卡片，原文与来源共享。** 这不是边缘情况 ——
「低脂、低糖、低胆固醇饮食」在体检报告里极其常见，只取其中一个键，
用户就会少拿到对应维度的全部菜品推荐，而系统不会报任何错。

```java
// 一条 doctorAdvice → 0..N 张卡片；BLOCKED 在此之前已经拦掉（Tier-0 前置于归一化）
List<String> keys = advice.getAdviceKeyCandidates().stream()
        .map(k -> AdviceKeyCatalog.normalize(advice.getAdviceType(), k))  // 枚举 + 别名表
        .filter(Objects::nonNull)
        .distinct().collect(Collectors.toList());
if (keys.isEmpty()) { keys = Collections.singletonList("OTHER"); adviceKeyMissCount++; }
// 每个 key 各自成卡，originalText / sourceLabel / evidenceText 全部引用同一条原文，不合并（§7.6）
```

> `adviceKeyCandidates` 缺失或为空数组时按"一个都没判断出来"处理，落 `OTHER` ——
> 它是可选子字段（§4.0.1），不因缺失丢弃整条医嘱。

```
「建议补充铁剂」「建议补铁」「血红蛋白偏低，注意补铁」「建议增加含铁食物摄入」 → IRON
```

`OTHER` 的出现次数计入 `adviceKeyMissCount`（**只计数、不记原文**，§13.1.1），高频时人工排查后提升为正式枚举 ——
**扩充枚举只需改一个 Java enum + 提示词可选值 + 一行别名常量，不需要写任何内容。**

> 本节的建议类 `OTHER` 与 §8.4 的未知过敏原运行时键不是同一种数据：
> 建议类固定为纯枚举值 `OTHER`；未知过敏原仍使用 `OTHER:{term}` 做名称精确匹配，
> 该 `term` 不进入 MySQL、日志或长期缓存，只随 Worker 内存和最终结果的 2 小时 TTL 存在。

### 7.3 内容生成与校验工作流

**用户永远不会成为某个 `adviceKey` 的首次试验者。**

```
LLM-B 生成并自检 ─▶ Java 校验(§7.5) ─▶ 保存非活动版本 ─▶ LLM-C 标签预热
                                        ├─ 该维度至少一条通过 Java 校验的标签 → 事务切换 ACTIVE
                                        └─ 一条都没有 → 不启用，旧 ACTIVE 继续服务并告警
```

**激活门槛不得以任何比值形式表达** —— 不使用覆盖率、完整度或百分比阈值。理由见 §9.8。
**先预热、后激活的顺序不可颠倒**：标签缓存的 Field 含 `adviceContentHash`，激活一个尚未预热的
版本会使该维度在下次预热前始终为空，等同于对用户关闭该维度。

| 时机 | 动作 |
|---|---|
| **上线前** | **全部**正式非过敏枚举（排除 `OTHER` 与 `ALLERGEN`）批量生成、自检、Java 校验和预热，逐个切换为活动版本。**不得按活跃度、命中次数或用户身份裁剪处理集合** |
| 新增枚举 | 同上，随发版一起完成；完成前不得认为该键已支持菜品匹配 |
| 线上遇到无活动版本的正式枚举 key | **不触发、不排队、不等待任何模型调用**。模块三仍按报告原文与来源正常展示该条建议，**不加任何"正在处理""稍后再试"或数据不完整提示**；该维度不参与菜品匹配，且**不向用户披露这一事实**（§7.8、§10.1） |
| 线上遇到 `OTHER` | **不查库、不写库、不调用 LLM-B**；展示原文、来源和未收录提示，只增加 `adviceKeyMissCount` |

> 正式非过敏枚举只有 19 条，全量自动校验成本约 1~2 小时，一次性投入。
> 相比之下，一条错误内容被缓存后会推给所有命中该维度的用户，直到有人发现 ——
> 这个爆炸半径是全量校验而非抽样校验的核心理由。

**LLM-B 的输入只有 `adviceKey` 和类型，不含任何用户数据** —— 这是缓存可跨用户复用的前提，
也消除了这一步的 PII 面。

**提示词（NUTRITION_SUPPLEMENT）：**

```
你是营养科普助手，面向企业食堂 App 的普通用户。
针对营养素「{displayName}」，输出用于"建议补充该营养素"的科普展示内容。

要求：
- 推荐食材 3~8 种，必须是中国大陆日常可获得的常见食材，使用通用名称
- 每种食材给出一句摄入量说明，必须包含具体频次与分量（如"每周1~2次，每次约50g"）
- 给出 1~2 条促进吸收或搭配的小贴士
- 不得提及任何药品、保健品、品牌名
- 不得使用"治疗""治愈""根治""降低""预防疾病"等医疗功效表述
- 不得针对特定疾病给出建议

只输出 JSON：
{"foods":[{"name":"猪肝","intakeNote":"每周1~2次，每次约50g"}],
 "tips":["含铁食物与维生素C同食可促进吸收"]}
```

**提示词（DIET_REQUIREMENT）：**

```
针对饮食要求「{displayName}」，输出：
- recommendIngredients：符合该要求的常见食材 4~8 种
- avoidIngredients：违反该要求的常见食材/食物 4~8 种
- cookingAdvice：一句烹饪方式建议，同时说明推荐与避免的做法
- tagLabel / avoidTagLabel：各不超过 4 字的正向/负向标签（如"低脂"/"高脂"）
（其余禁止项同上）

只输出 JSON：
{"recommendIngredients":["鸡胸肉","鱼","豆腐"],
 "avoidIngredients":["油炸食品","肥肉","奶油"],
 "cookingAdvice":"建议蒸、煮、炖、凉拌，避免煎、炸、红烧收汁",
 "tagLabel":"低脂","avoidTagLabel":"高脂"}
```

**过敏维度不调用 LLM-B。** 过敏提醒区的内容直接取自 §7.4 关键词族，
展示与匹配同源，保证"页面上说要避开虾丸"和"虾丸被标成不推荐"永远一致。

### 7.4 硬编码常量：过敏关键词族

```java
// 用途：§7.6 过敏提醒展示 + §8.3 Layer 1 硬兜底
static final Map<String, AllergenGroup> ALLERGEN_KEYWORDS;
static {
    Map<String, AllergenGroup> groups = new LinkedHashMap<String, AllergenGroup>();
    groups.put("SHRIMP_CRAB", group(
        /* 需避免食材 */ "虾","蟹","龙虾","小龙虾","皮皮虾","虾仁","虾米","虾皮","蟹肉","蟹粉",
        /* 易忽略食物 */ "虾滑","虾丸","虾饺","蟹柳","蟹棒","虾酱","xo酱","海鲜酱","虾油"));
    groups.put("MILK", group(
        /* 需避免食材 */ "牛奶","奶油","黄油","芝士","奶酪","淡奶","炼乳","酸奶","乳酪","奶昔",
        /* 易忽略食物 */ "布丁","奶盖"));
    groups.put("EGG", group(
        /* 需避免食材 */ "鸡蛋","蛋液","蛋清","蛋黄","滑蛋",
        /* 易忽略食物 */ "蛋饺","蛋黄酱","蛋挞"));
    // FISH / PEANUT / SOY / WHEAT / NUTS / MANGO / BEEF / MUTTON 同构
    ALLERGEN_KEYWORDS = Collections.unmodifiableMap(groups);
}
```

> **本节示例必须逐字等于数据真源，不得凭常识增删。**
> 完整词表是 `docs/allergen_display_split.csv`（11 组 73 词，2026-08-21 定稿），
> 上面三组是**照抄**，其余八组同构。注释里的两个分桶就是 CSV 的 `bucket` 列，
> 并集即 Layer 1 的匹配集（§7.6 规则 1）。
>
> **匹配是逐词字面匹配，不做任何词形推断。** 例如 `EGG` 组只有 `鸡蛋/蛋液/蛋清/蛋黄/滑蛋`
> 等具体词，**没有单字 `蛋`** —— 「韭菜炒蛋」这类只写单字的菜名不会被 Layer 1 拦下。
> 这是当前词表的已知覆盖边界（见附录能力边界），**不是可以顺手补一个字解决的问题**：
> 增删任何一个 `matchWord` 都直接改变 Layer 1 的拦截行为，属安全变更，
> 必须单独走变更流程（`OPEN_QUESTIONS.md` H.2 第 1 条），不得夹在文档修订里一并处理。

初版内容由模型生成候选 + 人工扫一遍（约半小时）后提交进代码，
之后由 §8.4 的未知过敏原日志与线上抽查增量补充。

> **这张表和 Tier-0 黑名单为什么不能也交给模型：** 它们是"模型出错时的兜底"，
> 兜底本身必须是确定性的。一个由模型每次生成的兜底，等于没有兜底。

**关键词族未收录的过敏原不是"不支持"** —— 走 §8.4 的 fail-safe 路径，不得当作无过敏原。

### 7.5 Java 对 LLM-B 输出的校验

```java
ValidationResult validate(AdviceContent c, AdviceType type) {
    if (type == NUTRITION_SUPPLEMENT) {
        if (c.foods.size() < 3 || c.foods.size() > 8)  return fail("食材数量不符需求 3~8 条");
        for (Food f : c.foods) {
            if (isBlank(f.name))                        return fail("食材名为空");
            if (!QUANTITY_PATTERN.matcher(f.intakeNote).find())
                                                        return fail("摄入量缺少具体分量");
        }
        if (c.tips.isEmpty() || c.tips.size() > 2)      return fail("贴士数量不符");
    }
    if (type == DIET_REQUIREMENT) {
        if (c.recommendIngredients.size() < 4
         || c.avoidIngredients.size() < 4)              return fail("食材清单过少");
        if (isBlank(c.cookingAdvice))                   return fail("缺烹饪方式建议");
        if (len(c.tagLabel) > 4 || len(c.avoidTagLabel) > 4) return fail("标签超长");
    }
    if (containsAny(c.allText(), MEDICAL_CLAIM_WORDS))  return fail("含医疗功效表述");
    if (containsAny(c.allText(), DRUG_NAME_HINTS))      return fail("含药品/保健品名");
    if (matchAnyPattern(c.allText(), BLOCKED_PATTERNS)) return fail("触及禁止方向");
    return ok();
}
// MEDICAL_CLAIM_WORDS = {治疗, 治愈, 根治, 疗效, 降血脂, 降血糖, 预防癌症, 抗癌, 药用}
// QUANTITY_PATTERN    = 数字 + (g|克|次|颗|片|ml|毫升|个|周|日|天)
```

LLM-B 响应必须同时返回 `contentJson`、`selfCheckPassed` 和 `selfCheckReason`。
只有 `selfCheckPassed=true` 才进入 Java 校验；模型自检不能绕过 Java Schema、安全词和冲突校验。
第 1 次失败携带失败原因重试；第 2 次仍失败则不持久化为可用版本并告警，旧活动版本不受影响。

> 需求 §7-3 明文要求"3~8 种"，这条由程序守，不靠提示词软约束。
> **我们不再让 Java 决定内容是什么，但仍让 Java 决定内容合不合规。**

### 7.6 三个展示区的字段

#### 过敏提醒

```json
{
  "adviceId": "advice-allergy-SHRIMP_CRAB",
  "allergenKey": "SHRIMP_CRAB",
  "allergenText": "虾蟹类",
  "avoidIngredients": ["虾", "蟹", "龙虾", "小龙虾", "皮皮虾",
                       "虾仁", "虾米", "虾皮", "蟹肉", "蟹粉"],
  "hiddenFoods": ["虾滑", "虾丸", "虾饺", "蟹柳", "蟹棒",
                  "虾酱", "XO酱", "海鲜酱", "虾油"],
  "sourceLabel": "来源：过敏原检查–虾蟹类 阳性(+)",
  "originalText": "虾蟹类 阳性(+)",
  "matchCapability": "FULL",
  "origin": "REPORT"
}
```

`matchCapability`：`FULL`（关键词族已收录）/ `TERM_ONLY`（未收录，仅按名称精确匹配，见 §8.4）。
`TERM_ONLY` 时卡片附提示「该过敏原暂无法自动识别加工食品中的隐藏成分，请自行确认菜品配料」。

`origin` 固定为 `REPORT`（本版无自填入口）。字段保留是为了后续开放自填时不改协议。

> **上面两张清单是完整的，不是节选**（10 + 9 = 19 词，等于 §7.4 `SHRIMP_CRAB` 组的全部
> `matchWord`）。示例写成节选会诱导实现者以为可以只下发一部分，而 §7.6 规则 1 的并集不变量
> 恰恰是靠单测强制的：少一个词 = 用户对着清单找不到某道菜被判不推荐的理由。
> 注意 `XO酱` 是 `displayName`，其 `matchWord` 是小写的 `xo酱`（§7.6 规则 2）。

`allergenText` 为卡片标题的**首选来源**，`sourceLabel` 保留报告原文来源；标准化 `label`
仅在原文缺失或过短时兜底。`matchCapability` **必须下发**供前端决定是否展示能力边界提示，
但 **UI 不得直接渲染 `FULL` / `TERM_ONLY` 枚举值**：`FULL` 不加限定语，`TERM_ONLY` 展示上述固定提示。
**每个报告过敏原独立成卡，卡片之间不合并、不去重**；同一过敏原重复出现时合并为一张并保留首条证据，
卡片内部清单去重。

#### 两张展示清单与 Layer 1 匹配集的关系（四条强制规则）

**1. 并集必须等于该组 Layer 1 实际使用的全部匹配词，且两张清单不得重叠。**
这不是形式要求：**展示多于匹配**等于卡片替系统承诺了它拦不住的东西，用户以为被保护实际没有，
失效方向与 §1.1 第 3 条同类；**展示少于匹配**则用户对着清单找不到某道菜被判不推荐的理由，
无法与纸质报告和配料表核对。该不变量必须由自动化校验强制且**长期保留** —— 它同时是关键词族
重构的行为等价证明，并永久阻止"只展示不匹配""只匹配不展示"两类词混入。

**2. 匹配用词与展示用词必须可分离。** 匹配词为提高召回而写，形态未必适合直接呈现给用户
（现有词表中的 `xo酱` 即为实例）。每个词必须有经过措辞审核的展示形态，**不得在缺失时静默回退为
匹配词原文**；下发内容中不得出现仅为提高召回而写的匹配词原始形态。本条只规定二者可分离，
不规定用何种数据类型表达。

**3. 分类判据。** `avoidIngredients` 收**该词本身就是该过敏原、或它的直接制品与另一种形态**
（生鲜、干制、分割、乳制品本体）；`hiddenFoods` 收**该过敏原只是其成分之一、且从品类名称看不出来**
的食物（复合加工食品、酱料、点心、饮品配料）。判据取自需求 §7-3 的「等加工食品中**可能含**虾蟹成分」。
**仍判不准时归入 `hiddenFoods`** —— 两张清单都是要避开的，区别只在提示强度，按 §1.2 的安全不对称，
不确定时选警示更强的一侧。

> **不得使用"原料 vs 加工品"作为判据**：它会把奶油、黄油、芝士、酸奶全判成"易忽略"，
> 使 `MILK` 组绝大多数词落入 `hiddenFoods`，稀释真正需要提示的条目。
> **分组内分布严重倾斜本身即是判据出错的信号。**

11 组 73 词的判定结果见 `docs/allergen_display_split.csv`（**数据真源**，2026-08-21 定稿）。
**词条本身不得增删** —— 关键词族成员的增删直接改变 Layer 1 的拦截行为，属**安全变更**，
必须单独走变更流程，不得夹在展示分类调整中一并处理。

**4. 分类确认前的过渡行为**（现有 11 组已定稿，本条适用于将来新增的分组）：未完成分类的分组，
其全部词一律进 `hiddenFoods`、`avoidIngredients` 留空。**不得让 `FULL` 分组的两张清单同时为空**
（会破坏第 1 条的并集关系，且用户拿到的信息比需求 §7-3 要求的更少）；
**也不得给 `FULL` 分组套用 `TERM_ONLY` 的能力边界提示**（该组匹配能力是完整的，属反方向的虚假陈述）。

#### 营养补充

```json
{
  "adviceId": "advice-nutrition-IRON",
  "adviceKey": "IRON",
  "nutrientText": "铁",
  "foods": [
    {"name": "猪肝", "intakeNote": "每周 1~2 次，每次约 50g"},
    {"name": "鸭血", "intakeNote": "每周 1~2 次，每次约 100g"},
    {"name": "菠菜", "intakeNote": "每日可食，每次约 150g"},
    {"name": "黑木耳", "intakeNote": "每周 2~3 次，每次约 30g（泡发后）"},
    {"name": "瘦牛肉", "intakeNote": "每周 2~3 次，每次约 75g"}
  ],
  "tips": ["含铁食物与维生素C同食可促进吸收，如搭配彩椒、西兰花、猕猴桃"],
  "sourceLabel": "来源：总检结论–建议补充铁剂",
  "originalText": "血红蛋白偏低，建议补充铁剂",
  "contentHash": "a3f1c9d2e7b40185"
}
```

#### 饮食注意

```json
{
  "adviceId": "advice-diet-LOW_FAT",
  "adviceKey": "LOW_FAT",
  "reqText": "低脂饮食",
  "recommendIngredients": ["鸡胸肉", "鱼", "豆腐", "各类蔬菜", "脱脂奶"],
  "avoidIngredients": ["油炸食品", "肥肉", "奶油", "动物内脏", "黄油"],
  "cookingAdvice": "建议蒸、煮、炖、凉拌，避免煎、炸、红烧收汁",
  "tagLabel": "低脂", "avoidTagLabel": "高脂",
  "sourceLabel": "来源：总检结论–建议低脂低盐饮食",
  "originalText": "超重，建议控制体重，低脂低盐饮食",
  "contentHash": "7e2b8140fa9c3d55"
}
```

> **一条原文可命中多个 `adviceKey`**（「低脂低盐饮食」→ `LOW_FAT` + `LOW_SALT`）。
> 需求 §7-3 要求展示每个饮食要求，因此拆成两个卡片区，各自引用同一条原文，不合并。

`contentHash` 不下发给前端，仅用于缓存级联（§9.2、§9.8）。

#### 未收录医嘱（`OTHER`）

`OTHER` 条目不依赖建议内容缓存，展示层直接使用该条报告自身的原文和来源：

```json
{
  "adviceId": "advice-other-1",
  "adviceKey": "OTHER",
  "originalText": "建议限制某类食物摄入",
  "sourceLabel": "来源：专科建议–建议限制某类食物摄入",
  "unsupportedNotice": "该项建议暂未收录，建议咨询医生或营养师"
}
```

同一报告中多条未收录医嘱可以全部使用 `OTHER`：展示与溯源依赖各条自己的
`originalText`、`sourceLabel`，而不是依赖 `adviceKey`。这些条目不进入 §8 菜品匹配。

### 7.7 内容版本自动管理

系统不提供人工校验、编辑、通过或驳回功能。正式枚举内容由 LLM-B 生成并自检，
通过 Java 硬校验和 LLM-C 预热后自动切换活动版本。生成、自检、Java 校验或预热失败时告警，
但不切换版本；`OTHER` 不进入该流程。版本切换后依赖 `contentHash` 的缓存自然失效。

### 7.8 空态（需求 §7-4）

三个区域各自独立判空：

```
过敏提醒为空  → 「本次体检报告未涉及过敏原相关内容」
营养补充为空  → 「本次体检报告未涉及营养补充相关内容」
饮食注意为空  → 「本次体检报告未涉及饮食要求相关内容」
```

> 需求原文只给了过敏提醒的文案，另两条写的是"同理"，上述为按同句式生成的建议文案，**待产品确认**。

**"报告没写"与"我们没读出来"必须用不同文案**（§4.0.2）：
`partialExtraction` 时展示「本次报告的该部分内容未能完整识别，建议核对原报告」，
不得复用上面的空态文案。`BLOCKED`、**无 ACTIVE 内容的正式枚举**、`OTHER` 条目同样**不算空**，
仍展示原文与来源；其中 `OTHER` 额外展示 §7.6 的未收录提示。

**命中正式非过敏 `adviceKey` 但该键无 ACTIVE 内容时，模块三必须保留该条报告原文和来源**，
不能把它当成"报告没有建议" —— 那是对报告内容的虚假陈述。

> V1.6.4 的「该项建议正在完善中」文案**已废止**，且**没有替代文案**：本版取消在线生成，
> 不存在任何"正在进行中"的状态。该条建议按报告原文与来源正常展示，**不增加**"正在处理"、
> "稍后再试"或数据不完整提示。SUCCESS 结果在 Redis 中固化 2 小时，重复调用 `/result`
> 不会触发任何重新计算。

### 7.9 底部声明

```
以上建议均基于体检报告原文，不构成医疗或营养处方，具体饮食方案请遵医嘱。
```

（常量，`DisclaimerConstants.DIET_ADVICE`）

---

## 8. 模块四：食堂菜品推荐（需求 §8）

### 8.1 菜品数据前置要求

**菜品侧零改造。** 现有数据即可满足全部需求：

```
菜品名称
食材列表：每个食材的名称 + 总量(g)
```

食材由模型直接读原文名，**不需要 `ingredient_id` 主数据与别名表** ——
"龙利鱼柳是鱼"这件事不需要谁去维护。

| 需要的东西 | 从哪来 |
|---|---|
| 拼音排序键 | **请求时实时计算**（§8.8），TinyPinyin 500 道菜 < 5ms，不需要落库字段 |
| 主料集 | **运行时按重量双规则推导**（§8.6），有食材重量就够 |
| 配料完整性 | **运行时 Java 推导**（§8.9），不需要落库字段 |
| `dishVersion` | **运行时计算** `SHA256(菜名 + 有序食材名 + 重量)`，用作 Redis Key（§9.2） |

> 这是本方案与食堂系统之间**唯一的接口**：读当日在售菜品的名称与食材重量。
> 不写、不改、不加字段。跨团队协调成本为零。

### 8.2 三层判定架构

```
                    当前在架菜品全量
                           │
        ┌──────────────────┴──────────────────┐
        ▼                                     ▼
┌───────────────────┐              ┌──────────────────────┐
│ Layer 1  Java     │              │ Layer 2 只读打标缓存 │
│ 过敏硬兜底         │              │ 按 adviceKey 逐维判定 │
│ 菜名+主料+配料全文 │              │ 营养补充 / 饮食注意   │
│ 无阈值，微量即命中 │              │ 维度级有无判定        │
│ 未知过敏原 fail-safe│             │ 零标签→整维度跳过    │
└─────────┬─────────┘              └──────────┬───────────┘
          │ forcedReject / uncertain          │ tagResults
          └──────────────┬────────────────────┘
                         ▼
              ┌────────────────────────────┐
              │ Layer 3  Java 合并与校验    │
              │ 强制不推荐 > 不推荐 > 推荐   │
              │ + 七条校验（含主料校验）     │
              │ + 拼音排序                  │
              └────────────────────────────┘
```

**核心性质：Layer 2 的输出只能让结果更保守或更丰富，不能让结果更危险。**
模型漏标一道菜 → 少推荐一道，无害。模型把含虾的菜标成推荐 → Layer 1 已把它扣在强制不推荐集，
Layer 3 直接丢弃该推荐判定并告警。

#### Layer 2 是纯读路径：维度级有无判定

**在线链路不调用 LLM-C、不等待其他 Worker、不向标签缓存写入任何数据**（包括不得把未打标的菜
写成 `NEUTRAL` 占位）。每个正式非过敏 `adviceKey` **独立判定**：

```text
无 ACTIVE 内容              → 跳过整个该维度
该维度一条有效标签都没有      → 跳过整个该维度
该维度存在标签              → 照常参与 Layer 2/Layer 3；
                             即使未覆盖全部在架菜品也不影响，
                             未打标的菜自然拿不到该维度的 RECOMMEND
```

**各维度互不影响。** 两个维度同时命中、其中一个零标签时，只有那一个不参与，另一个必须继续输出。

#### Layer 1 与 Layer 2 的独立性是硬要求

- Layer 1 **先于且独立于** Layer 2，对**全部当前在架菜品**运行，**包括预热窗口之后新增或变更的菜品**。
- 任何非过敏维度被跳过，**都不得删除或吞掉** Layer 1 已经得出的过敏不推荐与过敏待确认结果。
- 全部非过敏维度都被跳过时，Layer 1 的两个列表仍必须返回；**正常推荐列表不得由过敏维度反向生成**。

#### 跳过不对用户披露

某维度被跳过时，**不下发跳过提示、缺失的 `adviceKey`、缓存状态、内容哈希、模型版本或任何内部原因**
（§10.1）。模块三仍按报告原文与来源正常展示该条饮食建议（§7.8）。

### 8.3 Layer 1：过敏硬兜底（已知过敏原）

```java
Set<Long> forcedReject = new HashSet<>();
Set<Long> allergenUncertain = new HashSet<>();
Set<String> activeAllergens = reportAllergenKeys;   // 本版无自填入口，§1.2

for (Dish d : dishes) {
    // §3.0：菜品数据同样可能含部首字符（Excel/PDF 菜单导入），必须规范化
    NormalizedText haystack = NormalizedText.of(d.getDishName() + " "
                    + join(" ", d.getIngredients().stream().map(DishIngredient::getRawName)));

    for (String key : activeAllergens) {
        AllergenGroup g = ALLERGEN_KEYWORDS.get(key);
        if (g == null) {
            handleUnknownAllergen(d, key, haystack);      // §8.4 fail-safe，同时防 NPE
            continue;
        }

        for (NormalizedText kw : g.allWordsNormalized()) {   // 常量表启动时预规范化
            if (haystack.contains(kw)) {
                forcedReject.add(d.getId());
                rejectTags.put(d.getId(), new Tag(ALLERGY, key, g.label()));
                break;
            }
        }
    }
}
```

**四条与其他维度刻意不同的规则：**

1. 匹配范围含**菜名**（不只是配料表）——「白灼虾」即使配料表漏录也能命中
2. **无重量占比阈值** —— 微量即命中
3. 关键词族含**加工食品**（虾丸、蟹棒、XO 酱、沙拉酱），覆盖隐含成分
4. **模型不能推翻**

**已知代价：过杀。**「鱼香肉丝」在鱼过敏时会被误标不推荐，「蟹味菇炒肉」在虾蟹过敏时同理。
依据 §1.2 的不对称，主动接受这个精度损失。若线上反馈误杀集中在少数几个词，
加一个不超过 20 条的例外词典即可（常量数组，P2）。

### 8.4 未知过敏原的 fail-safe（关键词族未收录）

需求没有把过敏原限定在 11 类，报告可能写出「芹菜」「芥末」「亚硫酸盐」等。
这类过敏原**必须 fail-safe，不能当作无过敏原**。

```java
// haystack 必须是 NormalizedText：过敏判定禁止裸 String 比对（§3.0.4 ArchUnit 规则）
void handleUnknownAllergen(Dish d, String otherKey, NormalizedText haystack) {
    String rawTerm = extractTerm(otherKey);              // "OTHER:芹菜" → "芹菜"
    if (isBlank(rawTerm)) {
        allergenUncertain.add(d.getId());                 // 连过敏原名都拿不到 → 待确认
        return;
    }
    NormalizedText term = NormalizedText.of(rawTerm);

    if (haystack.contains(term)) {
        forcedReject.add(d.getId());                      // 名称精确命中 → 强制不推荐
        rejectTags.put(d.getId(), new Tag(ALLERGY, otherKey, rawTerm + "过敏"));
    } else {
        // 未命中 ≠ 安全：关键词族没收录它，加工食品里的隐藏成分无从识别
        d.setUnverifiedAllergen(rawTerm);                 // 供展示 uncertainReason 用
        allergenUncertain.add(d.getId());                 // ★ 必须真正进第三列表
    }
}
```

> **最后那行 `allergenUncertain.add()` 是本节的全部意义所在。** 只写 `setUnverifiedAllergen`
> 而不入集合，这道菜仍会走到 §8.6 `merge()` 的 `RECOMMENDED` 分支 —— 正文结论是对的，
> 代码却是 fail-open 的。第三列表必须由集合驱动，不能由对象上的一个字段驱动。

| 情形 | 处理 |
|---|---|
| 菜名/配料**精确包含**该过敏原名称 | 强制不推荐，标签「{term}过敏」 |
| 未命中 | **不判为安全，且不得进入推荐列表** → 转入「过敏成分待确认」列表（§8.9） |

**为什么不能只加横幅、把菜留在推荐列表：**

```
用户对芥末过敏 → 食堂配料只录了"沙拉酱" → 名称精确匹配落空
→ 这道菜出现在标题为「推荐菜品」的列表里
```

横幅是真实披露，但**一级分类的标题仍然在替系统说"推荐"**，
与 §1.1 第 3 条"含过敏原的菜品出现在推荐列表 = 不可接受的失败"直接冲突。
文档不能一边定绝对承诺，一边留一条例外。

**转入第三列表不损失任何信息量**，匹配理由照常展示，只是标题换了：

```
【过敏成分待确认】
  蒜蓉西兰花 —— 含西兰花，符合报告建议低脂饮食
  因「芥末」过敏成分无法完整核验，请确认配料后选择
```

用户拿到的内容一样，但系统不再声称"推荐"。

> 只要存在任一 `TERM_ONLY` 过敏原，**本次分析中**所有未被强制不推荐的菜品
> 一律进入第三列表，推荐列表在本次分析中为空。（Layer 1 是全局的、不按维度切分，
> 这里说的不是"某个 `adviceKey` 下"。）这是刻意的：
> **在无法完成安全核验时，"推荐"这个词就不该出现。**

**`OTHER` 过敏原只计入 `unknownAllergenCount`，不记录原文**（§13.1.1）；计数偏高时人工抽查样本，再补进 §7.4 关键词族。

### 8.5 Layer 2：LLM-C 按维度打标

**调用单元：一个 adviceKey × 一批菜品**，不是"用户的建议组合 × 全部菜品"（缓存切分见 §9.2）。

**输入格式（约 35 token/道）：**

```
饮食维度：低脂饮食
判断标准：recommendIngredients / avoidIngredients / cookingAdvice（取自 §7.6 该 key 的已校验并激活内容）

菜品列表：
1. 清蒸鲈鱼｜鲈鱼150 姜5 葱5 油3
2. 红烧五花肉｜五花肉200 冰糖15 老抽10 油20
3. 蒜蓉西兰花｜西兰花200 蒜10 油10
4. 杂粮饭｜糙米80 燕麦20 红豆20
```

**提示词：**

```
你是营养师。针对上述饮食维度，逐一判断每道菜属于以下哪一类：
  RECOMMEND  明显符合该饮食要求
  REJECT     明显违反该饮食要求
  NEUTRAL    既不特别符合也不违反，或无法判断

判断依据：菜品的主要食材、食材用量、菜名体现的烹饪方式。
- 只看该饮食维度，不要考虑其他健康因素
- 不确定的一律给 NEUTRAL，不要勉强归类
- ingredientMatches 只填菜品配料中实际出现的食材名，不得写配料里没有的东西
- cookingMatch 必须是**菜名中连续出现的原文片段，直接截取**，
  不得使用菜名以外的同义词、菜系名或推断出的做法
    「清蒸鲈鱼」→ "清蒸"      「红烧肉」→ "红烧"
    「烤鸭」→ "烤"            「蒜蓉西兰花」→ null（菜名无做法词，留空是正确答案）
    「烤鸭」→ ✗ "挂炉"（菜名里没有这两个字）
- 优先截取**完整词组**而非单字：「水煮肉片」填 "水煮" 不填 "煮"；
  「干煸豆角」填 "干煸" 不填 "煸"
- 以下做法虽字面清淡，实为高油做法，**不得据此判 RECOMMEND**：
  水煮（川式红油）、干煸（需过油）、蒜蓉蒸（蒜蓉油量大）、香煎/干煎
- 必须覆盖列表中的每一道菜，不得遗漏、不得新增

只输出 JSON：
{"results":[
  {"id":1,"verdict":"RECOMMEND","ingredientMatches":["鲈鱼"],"cookingMatch":"清蒸"},
  {"id":2,"verdict":"REJECT","ingredientMatches":["五花肉"],"cookingMatch":"红烧"},
  {"id":3,"verdict":"RECOMMEND","ingredientMatches":["西兰花"],"cookingMatch":null},
  {"id":4,"verdict":"NEUTRAL","ingredientMatches":[],"cookingMatch":null}]}
```

**菜品数据里没有"烹饪方式"字段**（§8.1 只有菜名 + 食材 + 重量），
因此 `cookingMatch` 的唯一证据来源是菜名本身。校验规则见 §8.5a。

「清蒸鲈鱼」→ 清蒸 ✓；「红烧肉」→ 红烧 ✓；「西红柿炒蛋」→ 炒 ✓；
「蒜蓉西兰花」→ 菜名无烹饪词 → `null`（不是错误，是这道菜的做法确实看不出来）。

**`ingredientMatches` 与 `cookingMatch` 必须分开。** 推荐理由里会出现"含蔬菜且为炖煮做法"，
其中"炖煮"不是食材 —— 混在一个数组里会导致 Java 拿它去配料表核对，永远核对不上。
拆开后两者各自可验证，理由才真正是可追溯的。

**批大小 40 道/批。** 输入约 40×35 + 提示词 300 ≈ 1700 token，输出约 1000 token。
超过 60 条后遗漏率明显上升（需用标注集验证），不建议为省调用次数加大批量。

**营养补充维度**用同一结构，判断标准换成 `foods` 清单，
且**只允许 `RECOMMEND` / `NEUTRAL`** —— 需求 §8-2 明确该维度不产生不推荐。

### 8.5a `cookingMatch` 的词组表与语义分组

**只做 `dishName.contains(cookingMatch)` 是不够的** —— 它能校验"这个词在菜名里"，
校验不了"这个词的含义是什么"：

```
菜名「水煮肉片」→ cookingMatch: "煮"
contains("煮") → true → 校验通过 ✓
→ 模型据此判 RECOMMEND，理由写成"为煮制做法，符合报告建议低脂饮食"
```

**但"水煮"是川式红油重油做法，低脂维度下应当是 REJECT。**
字面通过、语义相反，这是 `cookingMatch` 最危险的失败形态。

中式菜名里这类陷阱是固定的几个：

| 菜名 | 字面 | 实际 |
|---|---|---|
| 水煮肉片 | 煮 | 川式红油，高油 |
| 干煸豆角 | 煸 | 需过油 |
| 蒜蓉粉丝蒸扇贝 | 蒸 | 蒜蓉油量大 |
| 香煎 / 干煎 | 煎 | 用油量显著 |
| 白灼菜心 | 白灼 | 确实清淡 ✓ |

**因此 `cookingMatch` 必须命中一张固定词组表，并带语义分组：**

```java
// DishRuleConstants，硬编码（§1.3）。按长度降序匹配，长词组优先
enum CookingGroup { LIGHT, HEAVY_OIL, HEAVY_SUGAR, NEUTRAL }

static final Map<String, CookingGroup> COOKING_PHRASES = ordered(
    // —— 清淡 ——
    "清蒸", LIGHT,  "白灼", LIGHT,  "凉拌", LIGHT,  "水晶", LIGHT,
    "汆",   LIGHT,  "炖",   LIGHT,  "煲",   LIGHT,  "卤",   LIGHT,  "蒸", LIGHT,
    // —— 高油（含字面清淡但实为高油的）——
    "水煮", HEAVY_OIL, "干煸", HEAVY_OIL, "干锅", HEAVY_OIL, "油焖", HEAVY_OIL,
    "干煎", HEAVY_OIL, "香煎", HEAVY_OIL, "红烧", HEAVY_OIL, "炸",   HEAVY_OIL,
    "蒜蓉蒸", HEAVY_OIL,   // 长词组优先，先于 LIGHT 的"蒸"命中；见本节表格第 3 行
    "酥",   HEAVY_OIL, "爆炒", HEAVY_OIL, "煎",   HEAVY_OIL,
    // —— 高糖 ——
    "糖醋", HEAVY_SUGAR, "拔丝", HEAVY_SUGAR, "蜜汁", HEAVY_SUGAR, "冰糖", HEAVY_SUGAR,
    // —— 中性 ——
    "炒", NEUTRAL, "烤", NEUTRAL, "焖", NEUTRAL
);
// 注意：单字"煮""煸"刻意不在表内 —— 「水煮肉片」若返回"煮"直接作废，
//       返回"水煮"则正确落入 HEAVY_OIL
// 「蒜蓉蒸」必须在表内且长于"蒸"：本节表格把「蒜蓉粉丝蒸扇贝」列为固定陷阱，
//       若表里只有"蒸"→LIGHT，第 ③ 步语义校验对这道菜完全不设防
```

**Layer 3 的两步校验：**

```java
// ① 字面校验：必须是菜名子串
if (cookingMatch != null && !dish.getDishName().contains(cookingMatch)) cookingMatch = null;

// ② 词表校验：必须命中固定词组表（长词组优先），否则作废
if (cookingMatch != null && !COOKING_PHRASES.containsKey(cookingMatch)) cookingMatch = null;

// ③ 语义校验：分组与 verdict 方向必须一致，否则该维度降级
CookingGroup g = COOKING_PHRASES.get(cookingMatch);
if (verdict == RECOMMEND && dimensionIs(LOW_FAT) && g == HEAVY_OIL)  return NEUTRAL;
if (verdict == RECOMMEND && dimensionIs(LOW_SUGAR) && g == HEAVY_SUGAR) return NEUTRAL;
```

第 ③ 步是这套设计的真正价值：**即使模型坚持给「水煮肉片」在低脂维度打 RECOMMEND，
Java 也会因为"水煮 ∈ HEAVY_OIL"把它降为 NEUTRAL。** 提示词里的反例是配套，不是依赖。

**两类失败的严重性差得很远，实施顺序不能反：**

| 失败 | 后果 | 优先级 |
|---|---|---|
| 「水煮」被判低脂 → 推荐了高油菜 | **推荐了不该推荐的菜** | **先做词组表** |
| 「挂炉」被清空 → 少一句"为烤制做法" | 少一句理由，`ingredientMatches` 仍在 | 提示词约束，配套 |

> 这与 §8.2 是同一条原则的两面：**菜名字符串匹配只用于烹饪方式识别，且必须配词表。**
> §8.2 挡的是"鱼香肉丝不含鱼""甜椒不算高糖"（字面命中但实际没有），
> 这里挡的是"水煮不等于清淡"（字面清淡但实际高油）。

### 8.6 Layer 3：Java 合并与七条校验

```java
// 四取一：NOT_RECOMMENDED / ALLERGEN_UNCERTAIN / RECOMMENDED / NEUTRAL
DishVerdict merge(Dish d) {
    if (forcedReject.contains(d.getId())) {
        if (llmSaidRecommend(d)) {
            log.error("LLM recommended an allergen dish, overridden. dishId={}", d.getId());
            metrics.incr("allergen_override");
        }
        return NOT_RECOMMENDED;              // 只带 ALLERGY 标签
    }
    if (!rejectTags(d).isEmpty())    return NOT_RECOMMENDED;   // 需求 §8-3：冲突以不推荐优先

    // ★ 必须排在 RECOMMENDED 之前：安全核验未完成的菜绝不能进标题写着"推荐"的列表（§8.4、§8.9）
    if (allergenUncertain.contains(d.getId()) || isIngredientIncomplete(d)) {
        return ALLERGEN_UNCERTAIN;                             // 第三列表，带 uncertainReason
    }

    if (!recommendTags(d).isEmpty()) return RECOMMENDED;
    return NEUTRAL;                                            // 不进任何列表
}
```

| # | 校验 | 失败处理 |
|---|---|---|
| 1 | 返回的 `id` 必须在本批候选内 | 丢弃该条，计数告警 |
| 2 | `verdict` 必须是三个枚举之一 | 按 `NEUTRAL` 处理 |
| 3 | **全量覆盖**：本批未被返回的菜 | 按 `NEUTRAL` 处理（漏标只导致少推荐，安全） |
| 4 | `ingredientMatches` ⊆ 该菜配料表 | 剔除对不上的；全部对不上则降 `NEUTRAL` |
| 5 | `cookingMatch` 三步校验：菜名子串 → 命中词组表 → 语义分组与 verdict 方向一致（§8.5a） | 前两步不过置 `null`；第三步不过则该维度降 `NEUTRAL` |
| 6 | **主料校验**（仅 NUTRITION 维度）：`ingredientMatches` ⊆ 主料集 | 降 `NEUTRAL` |
| 7 | **过敏覆盖检查**：`RECOMMEND` 的菜不得在 `forcedReject` 中 | 强制改判 + `log.error` 告警 |

**主料集的确定性推导（需求 §8-2：营养补充匹配"菜品主料"）：**

**单一重量占比阈值不够用。** 中式菜里高水分蔬菜常常在重量上压过真正的蛋白主体：

```
青椒肉丝   青椒 200g + 肉丝 80g   → 肉丝 28.6%  < 30%  → 被判为非主料
番茄炒蛋   番茄 250g + 鸡蛋 100g  → 鸡蛋 28.5%  < 30%  → 被判为非主料
```

用户报告写"建议补充蛋白质"时，这两道教科书级的菜会被 Layer 3 降成 `NEUTRAL`，
属于高频误杀。

**但"取重量前 2 名"这类无下限规则同样不能用**，它会把主料校验本身废掉：

```
青菜 180g + 猪肝 5g → 前 2 名 = {青菜, 猪肝} → 猪肝重新成为主料
→ 补铁时这道菜又会被推荐，正是主料校验要挡的东西
```

**因此采用双规则，且名次规则带下限：**

```java
Set<String> mainIngredients(Dish d) {
    if (hasIsMainFlag(d)) return namesOf(d.getIngredients(), DishIngredient::isMain);

    List<DishIngredient> body = d.getIngredients().stream()
        .filter(i -> !SEASONING.contains(i.getRawName()))    // 油盐糖酱醋葱姜蒜
        .filter(i -> i.getWeightG() != null && i.getWeightG() > 0)
        .sorted(comparing(DishIngredient::getWeightG).reversed())
        .collect(Collectors.toList());
    if (body.isEmpty()) return Collections.emptySet();       // 无重量数据 → 该维度全 NEUTRAL

    double total = body.stream().mapToDouble(DishIngredient::getWeightG).sum();

    // 规则一：占比达标者，无论名次
    Set<String> mains = body.stream()
        .filter(i -> i.getWeightG() / total >= MAIN_RATIO)          // 0.25
        .map(DishIngredient::getRawName).collect(toCollection(LinkedHashSet::new));

    // 规则二：重量前 2 名，但必须过下限 —— 下限是这条规则的安全阀
    body.stream().limit(2)
        .filter(i -> i.getWeightG() / total >= TOP_N_MIN_RATIO)     // 0.15
        .forEach(i -> mains.add(i.getRawName()));

    return mains.isEmpty() ? Collections.singleton(body.get(0).getRawName()) : mains;
}
```

| 菜品 | 目标食材占比 | 规则一(≥25%) | 规则二(前2且≥15%) | 结果 |
|---|---|---|---|---|
| 青椒肉丝（补蛋白） | 肉丝 28.6% | ✅ | ✅ | 主料 ✓ |
| 番茄炒蛋（补蛋白） | 鸡蛋 28.5% | ✅ | ✅ | 主料 ✓ |
| 木耳炒山药（补纤维） | 山药 40% | ✅ | ✅ | 主料 ✓ |
| **青菜180+猪肝5（补铁）** | 猪肝 2.7% | ❌ | ❌ 不足 15% | **非主料 ✓** |

> 模型仍看全量配料（它需要上下文判断），但"是不是主料"由 Java 用重量数据确定性地判。
> `MAIN_RATIO = 0.25` 与 `TOP_N_MIN_RATIO = 0.15` 均为推演值，
> 由 §15.3 的主料召回率/误判率验证；**误判率偏高时应推动食堂系统补 `is_main` 字段，
> 而不是继续调阈值 —— 业务数据永远比推导规则可靠。**

### 8.7 标签与推荐理由（需求 §8-4、§8-5）

| tagType | 来源 | 示例文案 | 建议色 |
|---|---|---|---|
| `NUTRITION` | 营养补充 RECOMMEND | 补铁、高蛋白、补钙 | 绿 |
| `DIET_REQ_OK` | 饮食注意 RECOMMEND | 低脂、低盐、低糖、高纤 | 蓝 |
| `ALLERGY` | Layer 1 命中 | 虾蟹过敏、芹菜过敏 | 红 |
| `DIET_REQ_AVOID` | 饮食注意 REJECT | 高脂、高盐、高糖 | 橙 |

标签文案取自 §7.6 已校验并激活内容的 `tagLabel` / `avoidTagLabel`（内容数据，非配置）；
后端只给 `tagType` + `label`，颜色由前端固定色板常量决定。

**过敏拒绝的菜品只带 `ALLERGY` 标签，正面标签一个都不下发 —— 灰色附注也不行。**

```java
// §8.6 merge()：forcedReject 分支返回 NOT_RECOMMENDED 时，标签集必须被清空重建
if (forcedReject.contains(d.getId())) {
    return new DishVerdict(NOT_RECOMMENDED, onlyAllergyTags(d));   // 丢弃全部 NUTRITION / DIET_REQ_OK
}
```

| 情形 | 正面标签（`NUTRITION` / `DIET_REQ_OK`） |
|---|---|
| **Layer 1 过敏命中 → 不推荐** | **一律不下发**，包括灰色附注 |
| 非过敏原因不推荐（`DIET_REQ_AVOID`） | 可按 §16 #14 灰色附注展示 |
| 过敏成分待确认（第三列表） | 正常展示 —— 匹配理由照常给出，只是标题不叫"推荐"（§8.4） |

> **为什么过敏这一类要单独定。** 一道因虾过敏被拒的菜，卡片上若还挂着灰色的「补铁」，
> 用户读到的是"其实它挺适合我的，只是有点虾" —— 这正是 §1.2 安全不对称要防的心理效果。
> 过敏拒绝是**绝对拒绝**，不应同时呈现任何正面理由；而"高脂"这类拒绝是程度问题，
> 附注推荐标签有助于用户理解取舍。两者不能一刀切。

**推荐理由由 Java 拼接，不由模型生成：**

```java
String part = dishName + "——";
if (!ingredientMatches.isEmpty()) part += "含" + join("、", ingredientMatches);
if (cookingMatch != null)         part += (ingredientMatches.isEmpty() ? "" : "，") + "为" + cookingMatch + "做法";
part += "，符合报告" + advice.getReasonPhrase();
// NUTRITION_SUPPLEMENT → "建议补充" + 展示名   → 「建议补铁」
// DIET_REQUIREMENT     → "建议" + 展示名       → 「建议低脂饮食」
```

命中多条时拼接，上限 2 条，超出用「等」收尾。**每条理由里的 `ingredientMatches`
必须来自该菜自己的配料表**（§8.6 校验 4），不同菜的理由各自独立，不跨菜拼接：

```
菠菜猪肝汤——含猪肝、菠菜，符合报告建议补铁
清蒸鲈鱼——含鲈鱼，为清蒸做法，符合报告建议低脂饮食
```

理由中的每一项都能对应到饮食建议模块的某个卡片（`linkedAdviceIds`，前端可跳转）。

### 8.8 拼音首字母排序（需求 §8-4、§8-5）

```java
String pinyinFull    = Pinyin.toPinyin(dishName, "");   // TinyPinyin，「菠菜猪肝汤」→ bocaizhugantang
String pinyinInitial = firstLettersOf(dishName);        // → BCZGT

dishes.sort(comparing(Dish::getPinyinInitial)
      .thenComparing(Dish::getPinyinFull)
      .thenComparing(Dish::getDishId));                 // 三级键保证稳定
```

| 问题 | 处理 |
|---|---|
| **计算时机** | **请求时实时计算**。TinyPinyin 无外部资源，500 道菜约 3~5ms，不值得为它引入落库字段 |
| **多音字** | 「干锅」`gān/gàn`、「炸」`zhá/zhà`、「重庆」`chóng`。取常用音。**本版无人工修正入口** —— 排序偶有偏差可接受，不影响正确性，仅影响用户找菜的直觉（§16 #16） |
| **非汉字开头** | 「XO酱炒饭」「3D烤鸭」。取原字符大写作为首字母，**统一排在汉字之后**（需确认） |
| **稳定性** | 首字母相同时必须有确定的二三级键，否则同一份报告两次请求顺序不同。`dishId` 作为末级键 |

### 8.9 配料信息不完整的处理

Layer 1 依赖菜名 + 配料表。**配料表不全时，"没命中过敏关键词"不等于"不含过敏原"。**

**配料完整性由 Java 运行时推导，不落库、不依赖食堂系统主动标记**（食堂系统不会主动说自己没录全）：

```java
boolean incomplete = d.getIngredients().isEmpty()
    || d.getIngredients().stream().anyMatch(i -> isBlank(i.getRawName()))
    || d.getIngredients().stream().allMatch(i -> i.getWeightG() == null)
    || totalWeight(d) < MIN_PLAUSIBLE_WEIGHT_G          // 一道菜总重低于 50g，大概率没录全
    || onlySeasonings(d);                                // 只录了油盐酱醋
```

**三条列表 —— 第三条统一承载所有"安全核验未完成"的情形：**

| 列表 | 内容 |
|---|---|
| 推荐 | 命中推荐、无任何不推荐、**且过敏核验已完成** |
| 不推荐 | 命中任一不推荐规则（含 Layer 1） |
| **过敏成分待确认** | ①配料不完整；②存在 `TERM_ONLY` 过敏原（§8.4）；③`allergenStatus = UNKNOWN`（§4.0.3） |

三种触发条件语义相同：**我们没能完成安全核验**。因此合并为一个列表，
条目上用 `uncertainReason` 区分具体原因并给出对应提示文案。

```java
enum UncertainReason {
    INGREDIENT_INCOMPLETE,   // 该菜品配料信息不完整，过敏人群请向食堂确认
    ALLERGEN_TERM_ONLY,      // 因「{term}」过敏成分无法完整核验，请确认配料后选择
    ALLERGEN_STATUS_UNKNOWN  // 本次未能确认您的过敏信息，请谨慎选择
}
```

> 需求只定义了两个列表。第三个列表是 §0 原则的直接应用：
> **把未完成核验的菜放进"推荐"或干脆不展示，都等于替用户做了"它是安全的"这个判断。**
> **"当作安全"这个选项不可接受。**

**厨房交叉污染提示（模块底部固定）：**「严重过敏人群请向食堂现场确认菜品制作方式」。

### 8.10 空态（需求 §8-6）

```java
boolean noAdvice = allergenAdvices.isEmpty()
                && nutritionAdvices.stream().noneMatch(DietAdvice::isUsable)
                && dietRequirements.stream().noneMatch(DietAdvice::isUsable);
// isUsable: 存在 is_active=1 的版本 且非 BLOCKED（§9.8）

// ★ 文案选择不能复用 noAdvice：它混了"报告没写"与"我们没有 ACTIVE 内容"两件事。
//   报告是否写了饮食医嘱，只看报告本身归一化出的 adviceKey，与缓存状态无关。
boolean reportHasNoDietAdvice = nutritionAdvices.isEmpty() && dietRequirements.isEmpty();
if (noAdvice) return DishRecommendation.empty("本食堂菜品暂无个性化推荐。");
```

**五类空态必须用不同文案：**

| 场景 | 文案 |
|---|---|
| 报告无异常、也无饮食医嘱 | 「本食堂菜品暂无个性化推荐。」（需求原文） |
| **报告有异常指标，但总检结论无饮食医嘱** | 「您的报告中包含异常指标，但总检结论未给出明确的饮食医嘱。本功能严格依据报告原文，不自动推导饮食建议，因此暂不提供菜品推荐。如需针对性的饮食方案，建议咨询医生或营养师。」 |
| 有建议，但当日菜品无一命中 | 「当前上架菜品中未匹配到符合您报告建议的菜品」 |
| 当日无上架菜品数据 | 「暂未获取到今日菜品数据，请稍后再试」 |
| `allergenStatus = UNKNOWN` | 「过敏信息未能确认，以下菜品仅供参考，请谨慎选择」+ 全部转入「过敏成分待确认」 |

**第二条是 §1.5 那条业务后果的用户侧兑现，必须单列。**
这类报告（指标一堆箭头、总检结论只写"建议复查"）在真实样本中占比可能相当高。
若复用第一条文案，用户看到"暂无个性化推荐"会以为是系统没识别出自己的异常，
或者以为功能坏了 —— 明明健康问题模块里刚列了八条异常。
把原因讲清楚，比让用户自己猜要好。

```java
boolean hasAbnormalIndicator = healthIssues.stream()
        .anyMatch(i -> i.getSourceType() == INDICATOR_NUMERIC
                    || i.getSourceType() == INDICATOR_TEXTUAL);
if (noAdvice) {
    // 第二条文案断言的是"报告里没有饮食医嘱"，因此只有 reportHasNoDietAdvice 时才允许用。
    // 报告写了、但该键没有 ACTIVE 内容时，用既有的第一条文案 —— 既不撒谎，
    // 也不泄露"维度被跳过"（§7.8、§8.2、§10.1）。
    boolean useAbnormalWording = hasAbnormalIndicator && reportHasNoDietAdvice;
    return DishRecommendation.empty(
            useAbnormalWording ? EMPTY_ABNORMAL_BUT_NO_ADVICE : EMPTY_NO_ADVICE);
}
```

> 其余几种同样不能复用第一条 —— 语义不同，用户会误以为是自己报告没问题。

> **第二条文案有一个前提，不能省：报告里确实没有饮食医嘱。**
> 若报告写了「低脂饮食」而 `LOW_FAT` 恰好没有 ACTIVE 内容，用第二条就等于告诉用户
> 「总检结论未给出明确的饮食医嘱」—— 报告明明写了，这是 §7.8 定性为"对报告内容的虚假陈述"
> 的同一件事，同时还变相披露了该维度被跳过（§8.2 明令不披露）。这种情况一律用第一条既有文案。

### 8.11 失败模式与残留风险

| 风险 | 兜底 | 残留 |
|---|---|---|
| LLM-C 漏标菜品 | **未返回 = 该菜在该维度无标签**，不写入任何占位，拿不到 `RECOMMEND` | 少推荐几道，无安全风险 |
| 某维度零有效标签 | **整维度跳过**；其他维度与 Layer 1 结果全部保留 | 该维度不参与，且不向用户披露 |
| LLM-C 把含过敏原的菜标为推荐 | Layer 1 强制改判 + 告警 | 无 |
| LLM-C 返回不存在的 dishId | Layer 3 校验丢弃 | 无 |
| LLM-C 编造食材/做法 | Layer 3 逐个核对配料表与菜名 | 无 |
| LLM-C 把非主料当推荐依据 | Layer 3 主料校验 | 无 |
| 同一菜品两次判定不一致 | 缓存消除；版本变更时整体失效重跑 | 无 |
| 关键词族未收录的过敏原 | 名称精确匹配 + 其余转第三列表（§8.4） | **降低** —— 隐藏成分仍无法识别，但不再出现在推荐列表 |
| LLM-B 生成的摄入量数字离谱 | Java 校验格式，**不校验数值合理性** | **有** —— 由上线前全量LLM-B 自检与 Java 校验覆盖（§7.3） |
| LLM-B 推荐了不适合的食材 | 功效词/药品名可拦，食材合理性拦不住 | **有** —— 同上 |

### 8.12 底部声明

```
推荐菜品基于体检报告内容及食堂菜品数据自动匹配，菜品信息以食堂实际上架为准。
```

（常量，`DisclaimerConstants.DISH_RECOMMEND`）

---

## 9. 缓存与每日预计算

**模型的不确定性只在写入时消费一次，读取永远是确定的。** 这是全案在线延迟、成本、
可复现性三项同时成立的原因。

### 9.1 全局存储形态

**全案只有两张 MySQL 表。** `health_report_file` 只保存文件技术元数据，报告内容仍不落库。

```
MySQL         diet_advice_cache          建议内容及激活状态
MySQL         health_report_file         S3 文件定位、任务关联、页数和过期时间

Redis-结果    task:{taskId}              任务状态与最终结果，TTL 2 小时
Redis-打标    dt:{dishVersion}:...       菜品维度打标缓存，TTL 30 天

S3           处理中原始文件             SUCCESS 后立即删除；FAILED 时与预览图一起删除
S3           单页 PNG 预览图            SUCCESS 后 TTL 2h；24h lifecycle 兜底

内存          解析文本层 / LLM-A 抽取结果 / 姓名 / 校验中间态
              —— 全部只在 Worker 单次执行内存活，不落任何存储
```

**两个 Redis 必须是不同实例，因为持久化需求完全相反：**

| | Redis-结果 | Redis-打标 |
|---|---|---|
| 内容 | 含 `evidenceText` 等报告原文片段，**高敏感** | 菜名 + 判定，无 PII |
| 持久化 | **建议关闭 AOF/RDB** —— 减少敏感数据落盘明文；丢了让用户重传即可 | **必须 AOF** —— 丢了要重跑 250 次 LLM-C |
| 淘汰 | 可用 `allkeys-lru` | **必须 `noeviction`**（§9.1a） |
| TTL | 2 小时 | 30 天 |

> 把这两类数据放同一个实例，会被迫在"敏感数据落盘"和"预计算成果丢失"之间二选一。
> 分开之后两个问题都不存在。

#### 9.1.1 MySQL：`diet_advice_cache`

```sql
diet_advice_cache
  id, advice_type, advice_key,
  advice_prompt_version, advice_model_version, temperature, schema_version,
  content_json,
  content_hash,            -- SHA256(content_json) 前 16 位，级联失效的枢纽
  is_active,               -- TINYINT NULL，1/NULL，同一 key 下唯一，见 §9.8
  generated_at, activated_at,
  create_by, create_time, update_by, update_time
  UNIQUE KEY (advice_type, advice_key, advice_prompt_version, advice_model_version),
  UNIQUE KEY uk_active (advice_type, advice_key, is_active),
  CHECK (advice_key <> 'OTHER')
```

**它为什么不能也砍掉：** 它是"不建知识库"能成立的支点。
砍掉就意味着每个用户每次都要 LLM-B 现生成建议内容 —— 慢、贵，
而且"上线前全量校验 33 个 key"这件事将没有载体，退回到"第一位用户当小白鼠"。

数据量 = **19 个正式非过敏 key**（`NUTRITION_SUPPLEMENT` 10 + `DIET_REQUIREMENT` 9）× 版本数，
**百行量级**，全量常驻本地缓存。

> **19 这个数字要算对。** `ALLERGEN` 的 11 个键与两个 `OTHER` 都**不入本表**
> （§7.2、§9.1.1 的 `CHECK`），把它们算进来会得到"33"这种看似合理、实则会让人
> 把过敏键送进 LLM-B 的数字（§7.3 明确：过敏维度不调用 LLM-B）。

**`advice_key` 只允许正式业务枚举值，`OTHER` 禁止入表。** Repository 在 insert/update
前必须做代码断言；数据库版本支持强制 `CHECK` 时同时使用上面的约束，形成双层防线。
`OTHER` 也不得写入建议内容本地缓存或 Redis 菜品标签缓存。

> **`ref_count` 已删除（V1.7.1）。** 全案没有任何一处描述它何时递增：在线链路对本表是**只读**的
> （§9.7），若为它加一次 `UPDATE`，等于每个用户请求都写一次 MySQL，与"在线只读"直接冲突；
> 而离线侧也没有统计它的入口。留着一个永远为 0 的字段，只会让后来者以为存在某种引用计数机制。
> **实现时需同步删除** `src/main/resources/db/mysql/schema.sql` 中 `diet_advice_cache` 的该列
> （原 `001_create_diet_advice_cache.sql`，DDL 已于 2026-08-23 合并为单文件 `schema.sql`）
> 与 `DietAdviceCacheEntity.refCount`。

> `is_active` 必须用 `TINYINT NULL` 存 `1/NULL`，**不能用 `0/1`** ——
> `uk_active` 依赖 MySQL 唯一索引忽略 NULL 的特性，用 `0` 会挡住所有历史版本。

#### 9.1.1a MySQL：`health_report_file`

每份上传文件一条记录。文件二进制始终存放在 S3，MySQL 只保存定位和生命周期信息：

```text
health_report_file
  id, file_id, user_id, task_id, file_index,
  original_object_key, preview_object_prefix,
  document_type, file_size, page_count,
  expire_at, create_by, create_time, update_by, update_time
```

- `file_id`：单份上传成功后返回前端的不可预测标识，唯一。
- `task_id`：上传阶段为 `NULL`；调用“体检报告总结”接口时才创建 `taskId`，并在数据库事务内把
  本次提交的全部 `fileId` 关联到该任务。关联更新必须带 `task_id IS NULL` 条件并校验影响行数，
  防止同一文件被并发加入两个任务。
- `file_index`：上传阶段为 `NULL`；创建任务时按请求中 `fileIds` 的顺序写入，从 0 开始，关联后不再改变。
- `original_object_key`：上传时即可确定，固定为 `upload/{fileId}/original`，不依赖尚未创建的 `taskId`。
- `preview_object_prefix`：上传阶段为 `NULL`；创建任务后固定为
  `task/{taskId}/preview/{fileIndex}/`，
  与 §10.1.1 的对象 Key 规则同源。`fileIndex` 取创建任务请求中 `fileIds` 的顺序，
  从 0 开始，**关联后不再变化**（§4.0a 部分确认后重跑时同样沿用原序号）。
  **不要用 `fileId` 作为前缀**：前端只提交 `taskId + fileIndex + pageNumber`（§10.1.1、§13.1.2），
  用 `fileId` 会让这一列指向一个前端永远拼不出、清理批次也用不到的命名空间。
- `page_count`：解析完成后写入，用于页码边界校验。
- `expire_at`：清理批次的查询依据。**三个阶段一律设为「该阶段发生时刻 + 24 小时」**：
  单份上传成功时、创建任务关联文件时、分析成功时各刷新一次。

  > **为什么三档同值。** 它是**兜底清理**，不是主删除路径 —— 主路径有三条且都更快：
  > SUCCESS 后立即删除原始文件、页面关闭调用清理接口、FAILED 时连同预览图全删（§13.1）。
  > `expire_at` 只负责收拾"主路径没走到"的残留：传了文件没点分析、浏览器直接被杀、
  > S3 删除失败等待重试。24 小时与 S3 Lifecycle 的 24h 兜底取同一时间视界，
  > 两道兜底对齐，不会出现"记录已删而对象还在"或反过来的窗口。
  >
  > 设短会误伤真实用户（传完文件接个电话回来就没了）；设长则原始报告在 S3 上的驻留超出
  > §13.1 的承诺，而 V1.7 把鉴权整体外移之后，**驻留时长是唯一还握在本系统手里的风险控制手段**。
- 所有 MySQL 表统一包含审计字段：`create_by VARCHAR(50)`、`create_time DATETIME`、
  `update_by VARCHAR(50)`、`update_time DATETIME`；Java 属性分别为 `createBy`、`createTime`、
  `updateBy`、`updateTime`。`create_by` / `update_by` 只能填写 `SYSTEM`、`UPLOAD_API`、
  `ANALYSIS_API`、`CLEANUP_JOB` 等技术主体常量，严禁写入用户 ID、账号、手机号、`openId`
  或外部鉴权主体。

- `user_id`：**上传时即写入，不可为空，不可变更**（V1.7.1 恢复，见 `OPEN_QUESTIONS.md` I）。
  文件类端点（原图预览、清理）与 `analyze` 的 `fileIds` 校验都以它为准。
  当前取值由业务方代码提供的占位符，接真实认证后替换来源、不改列语义。

禁止在该表保存文件 BLOB、原文件名、报告/OCR/证据文本、患者姓名、过敏原、医嘱或模型请求响应。
**除 `user_id` 之外**，不得再出现账号、手机号、`openId` 或其他等价身份字段；
`user_id` 也不得写进 `create_by` / `update_by`（那两列只允许技术主体常量）。
实现时必须同步修改 `src/main/resources/db/mysql/schema.sql` 中的 `health_report_file`
（原 `003_create_health_report_file.sql`，DDL 已于 2026-08-23 合并为单文件 `schema.sql`）：
把 `owner_id` 改为 `user_id`（非空）、`task_id` 改为可空、新增可空 `file_index`，
并保留 `file_id` 唯一约束；旧 DDL 不得直接用于部署。

到期清理不设计删除状态和并发抢占。每十分钟查询一次：

```sql
SELECT * FROM health_report_file
WHERE expire_at <= NOW(3)
ORDER BY expire_at
LIMIT 100;
```

已关联文件按 `taskId` 清理：逐条删除数据库记录中的 `original_object_key`，再删除
`task/{taskId}/preview/` 下的全部预览图，全部成功后物理删除该 `taskId` 的文件记录。
尚未关联任务的过期文件按 `fileId` 清理：删除其 `original_object_key` 后物理删除该行。
任一 S3 删除失败都保留对应记录，交给下个批次重试。
S3 Lifecycle 最长 24 小时是最终兜底。

#### 9.1.2 Redis-结果：`task:{taskId}`

```
Key    task:{taskId}
Type   Hash
Field  userId / status / stage / progress / createdAt
       result        （JSON，四模块聚合结果，仅 SUCCESS 时存在）
       failReason    （仅 FAILED 时存在）
（无确认态字段：§4.0a 取消了 NEED_CONFIRM 交互，身份不一致直接落 FAILED）
```

#### TTL 按状态设置，不是"创建时一次设定"

```
UPLOADED / 处理中     TTL = 30 分钟   每次状态推进时重置
SUCCESS              TTL = 2 小时    ← 用户查看结果的窗口
FAILED               TTL = 30 分钟
```

**"创建时设 2 小时且永不续期"是一个危险的写法**，任何长耗时任务都会踩到：

```
10:00 创建任务，TTL=2h（此后永不续期）
11:50 队列积压后任务才被消费，此时 Key 只剩 10 分钟
12:00 Key 过期消失（Worker 还在跑 LLM-A）
12:03 Worker 执行 HSET task:xxx status SUCCESS
      → Redis 重新创建这个 Hash
      → 新 Key 没有 TTL，TTL = -1
```

**结果是一个永不过期的 Key，里面装着报告原文片段。**
"2 小时自动消失"是本方案全部合规设计的基石（§13.1），这个 bug 把基石抽掉了，
而且**没有任何报警** —— `TTL = -1` 会静悄悄地待在那儿。

另外按状态设 TTL 的语义也更合理：**2 小时从"分析成功"开始算，而不是从"上传"开始算。**
用户等了 5 分钟分析，没有理由让查看时间少 5 分钟。

#### 统一写入封装，禁止裸调 `HSET`

```java
// 唯一允许的写入入口（读操作同样只允许出现在本类，见下）
public void updateTask(String taskId, Map<String,String> fields, Duration ttl) {
    String key = "task:" + taskId;
    redisResult.opsForHash().putAll(key, fields);
    redisResult.expire(key, ttl);          // 与写入成对出现，不可分离
}
```

**业务代码不得直接调用 `redisResult.opsForHash().put(...)`。**
只要还允许裸调，迟早有人漏掉 `EXPIRE`，而漏掉的后果正是上面那个永久 Key。
建议用 ArchUnit 规则或 code review checklist 强制：
`RedisTemplate` 的 Hash 写操作只允许出现在 `TaskStateRepository` 一个类里。

**读操作同样收进这个类。** 读不会造成永久 Key，但会造成 **Key 命名漂移** ——
`"task:" + taskId` 这个拼接原先散落在三处（本节读取示例、§11.2 的 `isTerminal()`、
状态查询接口），改一次前缀就要同时改三个地方，漏一处就是查不到但也不报错。

```java
// TaskStateRepository 的读出口，业务代码只用这三个
Optional<TaskSnapshot> find(String taskId);      // 内含存在性 + TTL 判断
String  readStatus(String taskId);               // isTerminal 用
boolean isTerminal(String taskId);
private static String key(String taskId) { return "task:" + taskId; }   // 前缀只在这里出现一次
```

#### 本 Key 存 `userId`，读取路径必须比对（V1.7.1 恢复）

V1.7 曾把归属校验整体移出本系统（删除 `ownerId`）。**V1.7.1 恢复本系统内的归属校验**，
原因见 §13.1.2：上传端点不产生 `taskId`，外部层无从校验 `fileId` 归属，
谁拿到 `fileId` 谁就能把它提交进自己的任务并读到完整报告结果。

```java
// 读取路径：存在性 → 有效期 → 归属，三者缺一不可
TaskSnapshot task = taskStateRepository.find(taskId)
        .orElseThrow(ResultExpiredException::new);        // 不存在 或 TTL <= 0
if (!Objects.equals(task.getUserId(), currentUserId())) {
    throw new ResultExpiredException();                    // ★ 归属不符按"不存在"处理，见下
}
```

**归属不符必须返回与"不存在"完全相同的响应**（`404` + `RESULT_EXPIRED`），
不得返回 `403` 或任何可区分的错误码 —— 否则 `taskId` 的存在性就成了一个可探测的信号，
攻击者可以用它枚举有效 `taskId`。

**为什么选择存进 Hash 而不是每次反查 MySQL**（2026-08-23 决策，方案 B）：

| | 存进 `task:{taskId}` | 只靠 `health_report_file.user_id` 反查 |
|---|---|---|
| 状态轮询开销 | 一次 Redis 读（本来就要读） | **每次轮询多一条 MySQL 查询**，而 status 是全链路 QPS 最高的端点（§11.6 间隔 2~10s） |
| 页面关闭清理之后 | 结果 Key 自带 `userId`，仍可校验 | §10.1.2 会**物理删除全部文件记录**，而结果 Key 还有最长 2 小时 TTL —— 此时无任何 `user_id` 可比对，`/status` 与 `/result` 失去校验依据 |

> **`userId` 的取值当前是占位符**（由业务方自己的代码提供，见 `OPEN_QUESTIONS.md` I）。
> 占位阶段也必须真实写入与比对，不得跳过校验分支 —— 否则接真实认证时无从验证这条链路是通的。

> 对实现的直接约束：`userId` 是 `task:{taskId}` 中**唯一**允许的用户维度字段，
> 且只允许经 `TaskStateRepository` 写入。不得再出现姓名、手机号、`openId`
> 等任何其他身份或可识别字段。

#### 前端契约

用户分析完切走，两小时后回来结果就没了。
查询不到 Key 时返回 `404` + `RESULT_EXPIRED`，前端提示
「结果已过期，请重新上传报告」，**而不是报错弹窗**。

> 这也是"不留历史"这个产品决策的直接兑现：**没有历史记录，就没有加密留存、
> 保留期管理、个人信息删除权响应这一整套事。** 合规面因此大幅收窄。

#### 9.1.3 Redis-打标：`dt:*`

```
Key    dt:{dishVersion}:{tagPromptVersion}:{tagModelVersion}
Type   Hash
Field  {adviceKey}:{adviceContentHash}
Value  {"v":"RECOMMEND","im":["鲈鱼"],"cm":"清蒸","s":"P"}
TTL    30 天（读命中不续期，由预计算 job 负责刷新）
```

一次 `HMGET` 即可取一道菜在本次用到的全部维度判定：

```java
String key = "dt:" + dishVersion(dish) + ":" + tagPromptVersion + ":" + tagModelVersion;
List<String> fields = adviceKeys.stream()
        .map(k -> k + ":" + contentHashOf(k))
        .collect(Collectors.toList());
List<String> vals = redisTag.opsForHash().multiGet(key, fields);
```

**`dishVersion` 运行时计算**（`SHA256(菜名 + 有序食材名 + 重量)` 前 16 位），不落库。
菜品改配料后 `dishVersion` 变化，新 Key 自然为空，旧 Key 随 TTL 消亡，**无需任何删除逻辑**。

**为什么 `tagPromptVersion`/`tagModelVersion` 进 Key 而 `adviceContentHash` 进 Field：**
前两者变更是全局性的（所有菜失效），放 Key 上让旧数据整体随 TTL 退场，便于灰度期新旧并存；
后者是维度级的（只影响一个 `adviceKey`），放 Field 上粒度更细，
编辑一条建议内容不会波及同一道菜的其他维度。

#### 9.1a Redis-打标的可靠性要求与重建路径

打标结果不落库，Redis 是唯一副本。**这是明确的取舍，必须配套三件事：**

| 项 | 要求 |
|---|---|
| **持久化** | 开启 AOF（`appendfsync everysec`），不能只靠 RDB —— 丢一次快照等于丢一天的预计算 |
| **淘汰策略** | **必须独立 Redis 实例/集群**，`maxmemory-policy = noeviction`。注意该配置是**实例级**，DB0/DB1 逻辑库与 key 前缀命名空间**都不构成淘汰隔离**；Cluster 模式下更是只有 DB0 |
| **容量** | 单条 Value 约 80 字节。500 道菜 × 19 维度 ≈ 9500 field ≈ 1MB；扩到 5000 道菜也只有 10MB 量级 |

**丢失后的恢复：** 预热任务是 diff 驱动且幂等的，重跑一次即可全量重建
（冷启动约 240 次 LLM-C 调用，20~40 分钟）。

#### 监控只有两个计数，且不参与任何判定

**系统中不存在任何以比值表达的门槛。** V1.6.4 的 `dishTagHealthCheckJob`、80%/30% 覆盖率
分级阈值与自动重建触发**全部废止**。预热任务结束时只上报两个计数：

```text
dish_tag_target_total    本次预热的目标组合数
dish_tag_written_total   实际写入且通过 Java 校验的标签数
```

两者不等即告警，**由运维决定是否重跑**。运行时降级结果、超时结果或无效模型响应
**不得计入** `dish_tag_written_total`。

> **这两个计数不得参与激活判定、在线判定或任何用户可见行为。** 它们只用于告警。

**重建期间的在线行为：** 在线链路**不做任何补算** —— 没有 singleflight、不调用 LLM-C、
不写缓存。零有效标签的维度按 §8.2 整维度跳过，其余维度与 Layer 1 过敏结果照常输出。
用户看到"推荐列表短了一些"，不是报错，**也不会被告知发生了什么**。

> **为什么放弃比值门槛：标签缺失只会让推荐列表变短，不会产生危险推荐。** 没有标签就拿不到
> `RECOMMEND`，而过敏拦截由与缓存完全无关的 Java Layer 1 独立完成。比值门槛买到的不是安全性，
> 而是"列表可证明完整"，代价却是一条标签写失败即对用户关闭该维度一整天。

> Redis-结果实例则相反：丢了只是让用户重传一次，因此**建议关闭持久化**，
> 减少报告原文片段落盘。两个实例的配置不能抄来抄去。

#### 9.1.4 被删除的表及其代价

| 原表 | 删除代价 |
|---|---|
| `report_parse_result`（文本层） | **零代价，且更安全** —— 它是全案最敏感的数据（接近报告全文），不存反而少一个泄露面 |
| `report_extraction` | Worker 崩溃时无法从中间步骤恢复，任务直接失败；用户重新提交分析后从头执行 |
| `indicator_verification` / `diet_advice_result` | 零代价，本就是 Worker 内存中的中间态 |
| `dish_snapshot` | 事后无法复现"当时依据哪批菜"。将 `dishVersion` 列表写进结果 JSON 即可覆盖 |
| `recommendation_result` | 结果移入 Redis |
| `outbox_event` | 见 §11.1 —— 没有 DB 事务要保护，Outbox 失去存在理由 |
| `advice_key_miss_log` / `unknown_allergen_log` | 改为**只打计数指标、不记原文**（§13.1.1）。建议类 `OTHER` 不写 `diet_advice_cache`；原文采集若确有必要，需单独设计有保留期与权限控制的链路 |
| `advice_content_audit` | 删除；无人工内容操作，版本变更使用 `update_by` / `update_time` / `activated_at` |
| `user_allergen_setting` | 产品确认不做自填过敏原（§1.2） |
| `dish_ext` / `dish` 改造 | 拼音、主料、配料完整性全部改为运行时推导（§8.1），菜品侧零改造 |

### 9.2 缓存键设计

| 缓存 | 标识维度 | 依据 |
|---|---|---|
| 建议内容（MySQL） | `adviceType + adviceKey + advicePromptVersion + adviceModelVersion` | 内容只取决于"是哪个营养素/饮食要求"，与用户、菜品、日期无关 |
| 菜品打标（Redis） | `dishVersion + tagPromptVersion + tagModelVersion` （Key）<br>`adviceKey + adviceContentHash`（Field） | 判定取决于"这道菜（这个版本）在这条建议内容（这个版本）下如何" |

**`adviceContentHash` 必须进 key。** LLM-C 的实际输入是 `AdviceContent`
（`recommendIngredients` / `avoidIngredients` / `cookingAdvice`），不只是 `adviceKey`。
若标识只到 `adviceKey`，`LOW_FAT` 重新生成的新版本把推荐食材加了"脱脂奶"并切为 ACTIVE 之后，
`adviceKey` 与 `promptVersion` 都没变，旧的菜品打标会全部继续命中 ——
用户会看到"建议里写着脱脂奶是推荐食材，但所有含脱脂奶的菜都没有推荐标签"。

加了这一层之后：**编辑建议内容 → `contentHash` 变化 → Field 名变化 → 旧 tag 自然 miss**，
比"人工编辑后记得级联 DELETE"可靠得多，Redis 里也不需要任何主动清理。

**`dishVersion = SHA256(dishName + 有序食材原文名 + 重量)` 前 16 位。**
菜品改名或改配料 → Key 变化，新 Key 为空；只是换供应日期 → 继续命中。
**Key 不含日期**，这是跨天复用与稳态低成本的根本原因。

**版本号必须拆开，不能共用一个 `promptVersion`：**

```
extractPromptVersion / extractModelVersion     LLM-A
advicePromptVersion  / adviceModelVersion      LLM-B
tagPromptVersion     / tagModelVersion         LLM-C
```

建议内容侧把 `modelVersion` / `temperature` / `schemaVersion` 一并落库；
打标侧则把 `tagPromptVersion` / `tagModelVersion` 编进 Redis Key 本身。
半年后看到一条可疑结果，必须能回答"当初是哪个模型、什么提示词生成的"，
否则审计、diff、回滚都无从谈起。

**反例（不能这么切）：**

```
❌ (userId, taskId, ...)               → 命中率 0
❌ (dishId, 用户的全部 adviceKey 集合)  → 组合爆炸
❌ (dishId, adviceKey, saleDate)       → 跨天不复用，每天全量重算
❌ 一道菜一个 String Key               → 取 20 个维度要 20 次往返，应用 Hash + HMGET
```

### 9.3 互斥锁：只用于离线预热，在线 singleflight 全部废止

**V1.6.4 的在线 singleflight 已废止。** 在线链路不调用 LLM-C，也就不存在"缓存击穿"这个问题 ——
未命中的维度按 §8.2 直接跳过，不会有任何请求去回源。

互斥锁在本版中只有一个用途：**保证同一维度、同一内容版本在离线预热时只有一个执行者**。

```java
// 仅离线预热任务持有；在线路径不得获取此锁
String lock = "lk:tag:" + adviceKey + ":" + contentHash + ":" + tagPromptVersion + ":" + tagModelVersion;
if (cache.tryLock(lock, workerId, 180)) {
    try   { /* diff → 分批调 LLM-C → 只写通过 Java 校验的标签 */ }
    finally { cache.unlockIfOwner(lock, workerId); }   // 必须校验锁所有者
}
// 抢不到锁 = 同版本预热正在别处执行，本次幂等跳过，不阻塞、不等待
```

**锁粒度仍是「维度 × 内容版本」**，不是 Hash Key（一道菜），也不是 Field（一道菜 × 一个维度）：
一次预热天然覆盖该维度下的全部菜品，粒度更细只会让同一批任务互相争抢。

继续有效的原则：**缓存键的版本隔离**（§9.2）与**释放锁时校验持有者**（避免释放他人的锁）。

### 9.4 预热调度

```
入口一  上线/发版初始化   幂等业务入口，由部署侧接入调度
入口二  每日凌晨差异预热   每天执行一次，窗口之后不补算
```

#### 处理集合：全部正式非过敏内容键，不做任何裁剪

```text
AdviceKeyCatalog.NUTRITION_SUPPLEMENT 全部正式键
+ AdviceKeyCatalog.DIET_REQUIREMENT 全部正式键
- OTHER
- ALLERGEN
```

**不得按近期命中次数、用户指标、用户身份或报告内容裁剪处理集合。** V1.6.4 的
「近 30 天 Top 60 活跃维度」筛选**已废止** —— 按活跃度预热意味着冷门维度永远等不到标签，
而用户是否命中冷门维度与它的重要性无关。

> **守门要求：** 离线预热的正式非过敏 `adviceKey` 集合，必须与在线可能读取标签的
> `adviceKey` 集合**完全相等**。任一方向出现差集都必须使自动化测试失败（§15）。

#### 每日凌晨差异预热

```text
目标集合 = 当日在架菜品 × 全部 ACTIVE 正式内容版本
缺失集合 = 目标集合 - 已存在且版本完全匹配的有效标签
```

仅对缺失集合调用 LLM-C，标签键必须继续包含全部五个版本因子：

```text
dishVersion + tagPromptVersion + tagModelVersion + adviceKey + adviceContentHash
```

任一因子变化都视为新组合重新预热。

**每批 40 道，单批失败隔离**：单批失败不中断其他维度，也**不得伪造 `NEUTRAL` 标签写入缓存**。
模型未返回、响应无效、Java 校验丢弃、缓存写入失败，**一律视为该菜品在该维度上没有标签**，
等待下次差异预热补齐。

#### 预热窗口之后不再补算

当日新增或变更的菜品没有标签，按 §8.2 自然不获得推荐；**不触发任何在线或盘中补算，
也不需要记录目标集合快照**（没有比值就没有分母）。

> **但 Layer 1 过敏硬兜底不受此限** —— 它对**全部当前在架菜品**实时运行，包括预热窗口之后
> 新增的菜品（§8.3）。新上架的菜拿不到推荐，但如果它含过敏原，仍然会被判为不推荐。

### 9.5 成本

**冷启动：** 500 道 × 19 个正式非过敏维度 ≈ 9500 组合 ÷ 40 ≈ **240 次调用**，一次性，
并发 5~8 路约 20~40 分钟。

**稳态：** 每日新增/变更菜品约 20~50 道，新晋维度 0~2 个 →
增量约 1500 组合 ÷ 40 ≈ **38 次调用/天**。

**在线：稳态下每个用户请求的模型调用 = 1 次（LLM-A）。** LLM-B/C 全部走缓存。

### 9.6 失效与灰度

| 触发 | 影响 | 处理 |
|---|---|---|
| 菜品改名/改配料 | 该菜全部维度 | `dishVersion` 变 → Redis Key 变，旧 Key 随 TTL 消亡，**无需删除** |
| 建议内容产生新 ACTIVE 版本 | 该 key 全部菜品打标 | `contentHash` 变 → Field 名变，旧 Field 随 Key TTL 消亡 |
| `tagPromptVersion` / `tagModelVersion` 升级 | 全部打标 | Key 前缀变化，新旧并存；**旧 Key 保留至 TTL 到期供回滚与 diff** |
| `advicePromptVersion` 升级 | 全部建议内容 | 重新生成并自检 → Java 校验 → **预热 → 切换活动版本**（§9.8）；旧活动版本在切换前一直服务 |
| 过敏关键词族变更 | 无缓存影响 | Layer 1 是实时计算，改常量发版即生效 |

**灰度做法：** 新版本先只写不读，跑一天后用脚本 diff 新旧 `verdict`，
人工看 Top 差异条目，确认无误再切读。**这个 diff 能力是缓存架构白送的。**

### 9.7 缓存层次

```
本地 Caffeine   diet_advice_cache 全量常驻（百行量级，KB 级），MySQL 未命中回源
                过敏关键词族等常量随类加载常驻
Redis-打标      菜品维度打标，Hash 按 dishVersion 聚合，一次 HMGET 取一菜全维度
                **无回源层**：未命中即为该维度无标签，按 §8.2 跳过该维度，不调用任何模型
Redis-结果      任务状态与结果，非缓存而是唯一副本，TTL 2h 过期即消失
```

建议内容切换 ACTIVE 版本后（§9.8 ④），通过消息广播刷新本地缓存。本版无人工编辑入口（§7.7）。

**注意打标侧不存在"回源"概念** —— Redis miss 就是真 miss，直接进模型调用路径。
因此 §9.1a 的持久化与淘汰策略配置是硬要求，不是优化项。

### 9.8 建议内容的 ACTIVE 版本与切换预热

`diet_advice_cache` 中同一个 `adviceKey` 可能同时存在多行：

```
LOW_FAT + advicePromptVersion=v1 + is_active=1
LOW_FAT + advicePromptVersion=v2 + is_active=NULL
LOW_FAT + advicePromptVersion=v3 + is_active=NULL
```

`activeContentOf("LOW_FAT")` 该返回哪一个？**必须有唯一确定的答案，
否则新版本一落库，该维度就可能读到尚未预热的内容哈希 → 标签全量 miss → 整维度被跳过（§8.2），
而旧版本明明还可用。**

**增加 `is_active` 标志，同一 `(advice_type, advice_key)` 下唯一**（用 `1 / NULL` 实现唯一索引）：

```
v1 is_active=1                         ← 线上一直在用
        ↓  生成 v2
v2 由 LLM-B 自检并通过 Java 校验（v1 仍活动，用户无感）
        ↓  ★ 预热
        ↓  事务切换
v1.is_active = NULL / v2.is_active = 1
```

**★ 预热这一步不能省，顺序也不能颠倒。** 切 ACTIVE 会改变 `contentHash` → Redis 中该 `adviceKey`
的全部 Field 名变化 → **该维度所有菜品打标瞬间全量 miss**。而本版在线链路不回源，
一旦先切后热，该维度会**一直空到下一个预热窗口**，等同于对用户关闭该维度一整天。

```java
// 校验通过 ≠ 立即生效
void activate(String adviceKey, long newVersionId) {
    String newHash = contentHashOf(newVersionId);
    PrewarmResult r = prewarm(adviceKey, newHash);          // ① 用新 hash 预热（事务外，含远程模型调用）
    if (!r.hasAnyValidTag()) throw new PrewarmNotReady();   // ② 门槛：至少一条通过 Java 校验的标签
    txTemplate.execute(st -> {                              // ③ 事务只包住切换本身
        adviceMapper.switchActive(adviceKey, newVersionId);
        return null;
    });
    publishInvalidationAfterCommit(adviceKey);              // ④ 提交后才广播缓存失效
}
```

#### 激活门槛：至少一条有效标签，不得使用任何比值

| 项 | V1.6.4 | V1.7 |
|---|---|---|
| 激活门槛 | 95% 标签覆盖率 | **预热任务执行完毕，且该维度至少存在一条通过 Java 校验的标签** |
| 表达形式 | 比值 | **禁止以任何比值形式表达**：无覆盖率、无完整度、无百分比阈值 |
| 一条都没有 | — | 保持新版本非活动，**旧 ACTIVE 不变并告警** |

**修订理由：标签缺失只会让推荐列表变短，不会产生危险推荐。** 没有标签就拿不到 `RECOMMEND`，
过敏拦截由与缓存无关的 Java Layer 1 独立完成。比值门槛买到的不是安全性，而是"列表可证明完整"，
代价却是一条标签写失败即对用户关闭该维度一整天。在每日凌晨预热、菜品量不大的运行模型下，
预热部分失败属于运维异常，正确响应是**告警并重跑**，而不是关闭功能。

#### 事务边界

**事务只包住 ACTIVE 切换本身**，并在**提交后**发布本地/跨实例缓存失效通知。
**预热（含 LLM-C 远程调用）必须在事务之外**：把分钟级的模型调用包进事务会让数据库连接
被长期占用，且连接被空闲回收后整次激活白做。

#### 灰度不得依赖在线回源

新版本必须在**预热窗口内完成后**才对外生效。V1.6.4 中"先切换、由在线请求逐步补算"的
灰度方式**已废止** —— 本版在线链路不回源，那样做只会让该维度直接空掉。

**回滚同理：** 把 `is_active` 切回旧版本即可。旧 `contentHash` 对应的 Redis Field
只要还在 TTL 内就立即恢复命中，无需重跑 —— **这是 `contentHash` 放 Field 而非 Key 的又一处收益。**

---

## 10. 接口与任务状态

### 10.1 结果接口

```
GET /api/health-report/{taskId}/result
```

响应结构与四模块内容见各模块章节，此处只列与存储形态相关的部分：

```json
{
  "taskId": "t20260812001",
  "status": "SUCCESS",
  "expiresAt": "2026-08-12T12:23:11+08:00",
  "reportMeta": {
    "fileCount": 2, "pageCount": 7, "imageOnlyPage": 3,
    "identityStatus": "PASS",
    "allergenStatus": "SCREENED_POSITIVE",
    "partialExtraction": [],
    "dishVersions": ["a1b2c3d4e5f60718", "..."],
    "analyzedAt": "2026-08-12T10:23:11+08:00"
  },
  "healthIndicators": { "...": "§5" },
  "healthIssues":     { "...": "§6" },
  "dietAdvice":       { "...": "§7" },
  "dishRecommendation": { "...": "§8" }
}
```

**三个与"不留历史"直接相关的约定：**

| 项 | 处理 |
|---|---|
| `expiresAt` | 下发过期时间，前端可提前提示「结果将在 X 分钟后失效」 |
| **查询已过期的 taskId** | 返回 `404` + `RESULT_EXPIRED`，前端提示「结果已过期，请重新上传报告」，**不是报错弹窗** |
| `dishVersions` | 写进结果，替代已删除的 `dish_snapshot` 表，用于事后追溯"当时依据哪批菜" |

四个模块结构完全平行，各带 `emptyText` 与 `disclaimer`，前端独立渲染、独立空态、互不阻塞。
任一模块内部异常降级为空态，不导致整个接口失败。**但 `moduleError` 的适用范围必须划清：**

| 模块 | 内部异常时 |
|---|---|
| 模块一 健康指标 / 模块二 健康问题 | 空态 + **下发 `moduleError`** —— 这两个模块不涉及"维度是否被跳过"的语义，告诉用户"这块没能算出来"没有泄露面 |
| **模块三 饮食建议 / 模块四 菜品推荐** | 空态，**一律不下发 `moduleError`**；改为服务端 `log.error` + 告警计数 |

> **为什么模块三/四不能下发。** `moduleError` 是一个能反推出内部状态的字段，
> 而 §8.2 与 `OPEN_QUESTIONS.md` G 明确要求：维度被跳过时不下发任何可反推的字段，
> 菜品推荐无输出时只用既有文案「本食堂菜品暂无个性化推荐。」，不新增错误码。
> 若内部异常时下发 `moduleError`，抓包方就能区分"报告本来就没建议"与"我们这边出问题了" ——
> 这正是 G 条要求对外不可区分的两种情况。
>
> 注意这与 `partialExtraction` 不冲突：后者语义是"报告有这部分内容但我们没读全"，
> 是对**报告事实**的如实陈述，必须下发（§7.8）；`moduleError` 陈述的是**我们的故障**，不下发。

**内部字段一律不下发**：`contentHash`、`verdict`、内容处置状态、模型版本、内部缓存 Key，
以及任何能反推出"某维度被跳过"的字段。

#### 维度被跳过时的公共响应

某维度无 ACTIVE 内容或零有效标签时，**原子跳过整个受影响维度**；其他维度继续输出，
Layer 1 过敏结果始终保留。模块三继续展示该饮食建议的报告原文和来源（§7.8），
**不告知用户该维度被跳过，也不下发缺失的 `adviceKey`、缓存状态或内部失败原因**。

菜品推荐最终没有可输出的个性化推荐时，使用**既有**展示文案「本食堂菜品暂无个性化推荐。」（§8.10）。
**不得新增 `DISH_RECOMMENDATION_DATA_INCOMPLETE`**，不得使用「菜品推荐数据正在准备中，请稍后再试」，
**也不提供或引导用户触发重新计算**。SUCCESS 的 2 小时 TTL 内重复调用 `/result` 仍只读取同一份固化 JSON。

> 正常饮食建议卡片按既有协议下发的 `adviceKey` **不属于**诊断信息，不受本条限制。

> **`verifyStatus` / `evidenceStatus` 属于下发字段，不在此列。** 它们是 §5.2「存疑角标」的直接依据 ——
> `CONFLICT` / `UNCERTAIN` 时前端必须展示角标，`EVIDENCE_UNVERIFIABLE` 时才有「查看原图」入口（§4.5）。
> 把它们当内部字段剥掉，等于让用户看不到"这一项我们没核验成功"，与 §1.2 的安全不对称直接冲突。
> 本条此前误列 `evidenceStatus`，已按 §5.2 更正。

#### 10.1.1 原报告单页预览接口

```
GET /api/health-report/{taskId}/original/{fileIndex}/page/{pageNumber}
```

前端只提交业务侧的 `taskId`、从 0 开始的 `fileIndex` 和从 1 开始的 `pageNumber`，
不得提交 S3 objectKey 或云存储文件 ID。后端依次校验：

1. 请求已由外部鉴权层完成 `taskId` 归属校验（§13.1.2），本系统不重复判断；
2. 任务及结果仍在 TTL 内；
3. 文件序号和页码对应的 PNG 预览对象真实存在；
4. 签名 URL 有效期取 `min(5 分钟, task 剩余 TTL)`。

成功时只返回短期地址，不由应用服务器转发图片字节：

```json
{
  "url": "https://private-s3.example/...signed...",
  "expiresInSeconds": 300
}
```

前端用该 URL 直接加载单页 PNG，并提供缩放与关闭能力。URL 返回 403 或过期时，
前端重新调用本接口获取新地址；不得将签名 URL 写入 LocalStorage、业务数据库或日志。
私有 Bucket 校验签名后直接返回图片，避免应用服务器承担报告图片下载流量。

对象 Key 仅由服务端按固定规则构造，例如
`task/{taskId}/preview/{fileIndex}/{pageNumber}.png`。对象 Key 不含患者姓名、原文件名或健康信息。

#### 10.1.2 页面关闭清理接口

```
DELETE /api/health-report/{taskId}/files
```

后端确认 `health_report_file` 中存在该 `taskId` 的记录后，逐条删除记录中的
`original_object_key`，再删除 S3 `task/{taskId}/preview/` 下的全部页面预览对象，
最后物理删除该任务的全部文件记录。成功返回 `204`。
前端可在主动退出或页面关闭时调用；关闭事件未成功送达时由 `expire_at` 定时清理兜底。

### 10.2 任务状态机与存储

```
UPLOADED → PARSING → EXTRACTING → VERIFYING → ASSEMBLING → MATCHING → SUCCESS
                          │                                           └→ FAILED
                          └→ 身份不一致 ──▶ FAILED / IDENTITY_MISMATCH（§4.0a，不可恢复，
                                            用户修正文件后重新上传）
```

**全部状态存在 Redis 的 `task:{taskId}` Hash 中**（§9.1.2），没有数据库表。

```java
// 状态推进一律走统一封装，写字段与设 TTL 成对出现（§9.1.2）
Map<String, String> processingFields = new HashMap<String, String>();
processingFields.put("status", "EXTRACTING");
processingFields.put("stage", "llm-a");
processingFields.put("progress", "40");
taskStateRepository.updateTask(taskId,
        processingFields,
        TTL_PROCESSING);      // 30 分钟

// 终态
Map<String, String> successFields = new HashMap<String, String>();
successFields.put("status", "SUCCESS");
successFields.put("result", resultJson);
taskStateRepository.updateTask(taskId,
        successFields,
        TTL_SUCCESS);         // 2 小时
```

无需 CAS —— 同一时刻只有一个 worker 持有任务锁（§11.2），
且写终态前会再判一次终态（§11.2 的双重判定）。

**没有任何"等待用户输入"的中间态，因此不需要 `confirm_deadline_at` 这类字段。**
状态机只有"处理中 → 终态"两段，每种状态的 TTL 由 §9.1.2 一张表定完。
这也是 §4.0a 取消确认交互带来的直接简化。

> 上一版为持久化任务设计的 `lease_expire_at` / `deadline_at` / `retry_count`
> 等字段随之全部删除。它们保护的是"数据库里可能永远卡住的行"，而 Redis Key 会自己过期。

### 10.3 全部持久化清单

```
MySQL      diet_advice_cache            建议内容，无 PII
MySQL      health_report_file           文件技术元数据，按 expire_at 到期清理
Redis-结果 task:{taskId}                TTL 2h，建议关闭持久化
Redis-打标 dt:{dishVersion}:{...}       TTL 30d，AOF + noeviction，独立实例
S3        处理中原始文件 + SUCCESS 后单页 PNG 预览图  原文件成功即删；预览图 TTL 2h；24h lifecycle 兜底
```

**没有其他存储。** 解析文本层、LLM-A 抽取结果、姓名、指标校验中间态、
菜品快照，全部只在 Worker 单次执行的内存中存在。

---

## 11. 异步链路与资源控制

### 11.1 队列：Redis Stream，不引入 Kafka

任务队列用 **Redis Stream + Consumer Group**，与任务状态放在**同一个 Redis 实例**
（Redis-结果，§9.1.2）。

```
Stream   q:analysis                        任务队列
Group    g:worker                          消费组
Entry    { taskId }                        只放 taskId，不放任何报告数据
```

#### 为什么这里 Redis Stream 比 Kafka 合适

| 维度 | 本场景的实际情况 |
|---|---|
| 吞吐 | 用户手动上传体检报告，量级是每分钟个位数。Kafka 的吞吐能力用不上 |
| 消费组 | 只有一个消费组（Worker）。不需要多方订阅、不需要回放 |
| 留存 | 已确定不留历史（§9.1），任务丢了让用户重传即可，不需要长期留存 |
| 运维 | 少一套中间件。Redis 本来就要部署两个实例 |
| **原子性** | **见下，这是最大的收益** |

#### 原子投递：一条 Lua 脚本

Kafka 版本存在一个真实的跨系统窗口：写 Redis 状态成功，但 Kafka 发送失败，
只能靠 try-catch 回滚（而回滚本身也可能失败）。
Stream 与任务状态在同一实例，**可以原子完成**：

```lua
-- KEYS[1]=task:{taskId}  KEYS[2]=q:analysis
-- ARGV: status, createdAt, ttlSeconds, taskId, userId
-- V1.7.1：恢复写入 userId（§9.1.2）；队列条目仍只放 taskId，不含任何用户数据
redis.call('HSET', KEYS[1], 'status', ARGV[1], 'createdAt', ARGV[2], 'userId', ARGV[5])
redis.call('EXPIRE', KEYS[1], ARGV[3])
return redis.call('XADD', KEYS[2], '*', 'taskId', ARGV[4])
```

```java
public String analyze(String userId, List<String> orderedFileIds) {
    validateFileIds(userId, orderedFileIds);         // ① 1～5 个、存在、未过期、尚未关联、**且都属于该 userId**
    rejectWhenQueueIsFull();                         // ② 显式分析前做背压
    String taskId = uuid();
    bindFilesInTransaction(taskId, orderedFileIds);  // ③ 条件更新 task_id IS NULL，并写稳定 file_index
    try {
        taskQueue.enqueueAtomically(taskId, userId,  // ④ 写状态 + 入队，一条脚本
                TTL_PROCESSING);                     //    V1.7.1：userId 随状态一同原子写入
    } catch (Exception e) {
        unbindFilesInTransaction(taskId);            // ⑤ 入队失败仅回滚任务关联，不删除已上传文件
        throw new AnalysisSubmitFailedException("提交失败，请重试");
    }
    return taskId;
}
```

逐份上传接口与上述方法分离：每次只保存一份文件到 `upload/{fileId}/original`，写入一条
`task_id=NULL`、**带 `user_id`** 的 `health_report_file` 记录并返回 `fileId`，
不创建 Redis 状态、不写 Stream。

**`validateFileIds` 必须校验每个 `fileId` 的 `user_id` 与当前请求一致** —— 这是引入
`userId` 的首要目的：不校验的话，任何人拿到一个 `fileId` 就能把它提交进自己的任务，
然后合法地读到那份报告的完整分析结果（`OPEN_QUESTIONS.md` I）。

**“状态写了但没入队”这个状态不再存在。** 数据库文件关联与 Redis 原子入队仍是跨系统操作；
入队失败时回滚 `taskId` / `fileIndex` 关联，让这些 `fileId` 可以安全重试，不删除用户已上传文件。

#### 背压

Stream 在内存里，不能无限增长。**点击分析、创建任务之前检查队列深度：**

```java
long depth = redisResult.opsForStream().size("q:analysis");
if (depth > MAX_QUEUE_DEPTH) {                       // 建议 200，需压测校准
    throw new TooManyRequestsException("当前排队人数较多，请稍后再试");
}
```

**不要用 `MAXLEN` 自动裁剪** —— 那会直接丢掉尚未处理的任务，且用户不会收到任何提示。
拒绝新请求比静默丢弃已接受的请求诚实得多。

`q:analysis` 无论处理成功还是明确失败，都在写入对应终态后原子执行 `XACK` + `XDEL`，
Stream 不会累积。任务级失败不重试，不设置 DLQ；模型单次调用内部的 Schema 校验重试仍按 §11.4 执行。

#### 持久化：与结果同实例，同生共死

Redis-结果实例建议关闭持久化（§13.1，避免报告原文片段落盘）。
这意味着 Redis 重启会**同时丢掉排队任务与任务状态** —— 这恰恰是正确的行为：

```
Kafka 方案：Kafka 保住了任务，但 Redis 状态没了
          → 出现"有任务无状态"的孤儿，worker 跑完写不进去、用户查不到
Stream 方案：两者一起消失
          → 用户轮询到 RESULT_EXPIRED，提示重新上传。语义一致，没有中间态
```

> 队列条目只含 `taskId`，不含任何报告数据，因此关闭持久化不影响合规判断。

### 11.2 消费、终态去重与崩溃失败收敛

#### 消费循环

```java
// 每个 Worker 一个固定 consumerName（建议 hostname + pid）
List<MapRecord<String, Object, Object>> records = redisResult.opsForStream().read(
        Consumer.from("g:worker", consumerName),
        StreamReadOptions.empty().count(1).block(Duration.ofSeconds(5)),
        StreamOffset.create("q:analysis", ReadOffset.lastConsumed()));
```

`XREADGROUP` 保证**同一条目只会投递给消费组内的一个消费者**，替代 Kafka 的分区消费语义。

#### Worker 崩溃：Pending 任务直接失败

Worker 崩溃时条目留在该消费者的 PEL（Pending Entries List）里。系统不重新认领、
不再次执行分析；巡检任务在安全空闲时间后直接写失败终态并清理消息：

```java
// 每 2 分钟巡检
// min-idle-time 必须大于 Worker 执行上限（15 分钟，§11.3），
// 否则可能把仍在正常处理的任务误判为失败
for (PendingMessage rec : pendingMessages("q:analysis", "g:worker")) {
    if (rec.idleTime() >= Duration.ofMinutes(20)) {
        taskState.markFailed(taskId(rec), "WORKER_LOST");
        taskQueue.acknowledgeAndDelete(rec);                    // Lua：XACK + XDEL
        alarm("pending task failed without retry", taskId(rec));
    }
}
```

不读取或维护 `delivery-count` / `retryCount`，也不存在任务级重试和死信队列。

#### 终态去重（不可省）

```java
// ① 抢锁前先判终态 —— 防止同一消息因消费边界异常而重复执行
if (isTerminal(taskId)) { ack(recordId); return; }

String lock = "lk:task:" + taskId;
if (!redisResult.opsForValue().setIfAbsent(lock, workerId, Duration.ofMinutes(10))) {
    return;      // 其他 worker 正在处理，直接返回
}
try {
    // ② 抢到锁后再判一次 —— 防止"判定与抢锁之间"另一个 worker 刚写完终态
    if (isTerminal(taskId)) return;

    heartbeat.start(lock, workerId);   // 每 2 分钟续期
    Result r = process(taskId);

    // ③ 写结果前最后判一次，然后写入
    if (isTerminal(taskId)) return;
    Map<String, String> fields = new HashMap<String, String>();
    fields.put("status", "SUCCESS");
    fields.put("result", toJson(r));
    taskStateRepository.updateTask(taskId, fields, TTL_SUCCESS);
} finally {
    heartbeat.stop();
    redisResult.delete(lock);          // 仅当持有者为自己时删除（Lua 脚本保证）
}

// 实现在 TaskStateRepository 内，此处仅示意判定条件；业务代码调 taskStateRepository.isTerminal()
boolean isTerminal(String taskId) {
    String st = taskStateRepository.readStatus(taskId);
    return "SUCCESS".equals(st) || "FAILED".equals(st);
}
```

**三重终态判定不是冗余，缺一不可：**

| 位置 | 挡住的场景 |
|---|---|
| ① 抢锁前 | 消费边界异常产生的重复交付，零成本短路，不必进入处理流程 |
| ② 抢锁后 | 另一个执行者刚写完终态并释放锁，本 Worker 恰好在缝隙抢到锁 |
| ③ 写入前 | 处理耗时数分钟，期间超时巡检可能已将状态写为 FAILED |

**为什么必须做：** Redis Stream 的消费确认仍存在进程崩溃和网络异常边界；此外，
Pending 失败巡检可能与尚未完全停止的旧 Worker 短暂并发。条件终态写入可以防止晚到结果覆盖
已经对用户可见的 `FAILED` 或 `SUCCESS`。

> 这不是"多花一次模型调用的钱"，而是**正确性问题**。
> 上一版把重复消费的代价说成纯成本，是错的。

#### 任务锁保留，但角色变小

`XREADGROUP` 已保证一条目只投一个消费者，Pending 失败巡检的安全空闲时间又大于
Worker 执行上限，冲突窗口极窄。锁仍然保留，理由有二：

1. 消费组交付与超时失败巡检是两条独立路径，可能并发
2. 锁的成本只是一次 `SET NX`，而它进一步防止同一任务被并发执行

**处理成功或失败后都必须 `XACK` + `XDEL`**，否则条目会一直留在 PEL。
顺序是：写终态 → 原子 `XACK + XDEL` → 释放锁。

Worker 崩溃时任务锁随 TTL 自动释放，PEL 条目由巡检任务直接写为 `FAILED/WORKER_LOST`
并清理，不重新执行，不进入 DLQ。

**没有"查库跳过已完成步骤"的分阶段幂等。** 任务失败后不会完整重跑；用户需要重新提交分析。
终态去重仍必须保留，防止消费边界异常覆盖已经写入的结果。

> 这是本版最大的架构简化：**幂等的复杂度来自"部分完成的状态需要被记住"。
> 不记住，就不需要记住每一步；但"整件事是否已经做完"仍然必须记住。**

### 11.3 超时：客户端等待 ≠ 服务端执行

**客户端超时不得修改任何状态。**

| 层 | 阈值 | 行为 |
|---|---|---|
| 前端轮询 | 3 分钟 | 仅展示「分析时间较长，请稍候」，**不调用任何写接口** |
| Worker 自身 deadline | 15 分钟（可配） | 超过则写 `status=FAILED`，释放锁 |
| 单次 LLM-A 调用 | 120~180s | 超时进入退避重试 |
| 任务锁 | 10 分钟，每 2 分钟续期 | 过期自动释放 |
| **Stream Pending 失败判定时间** | **20 分钟** | 必须 **> Worker deadline（15 分钟）**，否则会把原 Worker 正常处理的任务误判为失败 |
| 结果 Key | 2 小时 | 过期消失 → `RESULT_EXPIRED` |

> `min-idle-time > Worker deadline` 这条大小关系必须守住。设小了会把正常处理中的任务提前写成
> `FAILED/WORKER_LOST`。Pending 巡检只负责失败收敛，不得认领或重新执行任务。

### 11.4 模型调用的健壮性

| 项 | 措施 |
|---|---|
| **超时** | LLM-A 120~180s；LLM-B/C 30~60s |
| **分批** | LLM-A ≤8 页/批；LLM-C 40 道/批（§8.5）。单批失败只重试该批 |
| **跨批合并** | LLM-A 含 `sections`/`summaryConclusions`，**跨批合并必须重算展示顺序与 `itemNo`**，不能用批内序号。三个非数组字段（`reportMeta`/`reportOverview`/`allergenSectionPresent`）的合并规则见 **§4.0.1a**，其中 `allergenSectionPresent` 的 `true` 方向需 Java 佐证 |
| **退避重试** | Timeout / 5xx 指数退避（1s → 2s → 4s），最多 3 次 |
| **JSON 提取** | 先正则提取顶层 JSON 块再 Jackson 反序列化，全程 try-catch |
| **Schema 校验** | 见 §4.0.1，**缺字段必须重试，不得默认空** |
| **失败降级** | LLM-A 失败 → task FAILED；LLM-B 失败 → 不启用新版本并告警；LLM-C 失败 → 不切换建议版本；该批菜品在该维度上**没有标签**，等下次差异预热补齐，**不得写入 `NEUTRAL` 占位**（§8.2、§9.4） |
| **离线并发** | 预计算 job 并发 5~8 路，独立限流令牌桶，**不与在线请求共享配额** |

### 11.5 JVM 资源控制

`PDFRenderer.renderImageWithDPI` 大量使用 `DataBufferInt` 与 Java2D 堆外内存，并发下极易 OOM。

```java
int permits = availableHeapMB / (20 * PREFETCH_PAGES);   // 150DPI 单页约 20MB
Semaphore parseSemaphore = new Semaphore(Math.max(1, permits));
```

配套：显式 `-XX:MaxDirectMemorySize`；每页处理完立即 `image.flush()` 并断开引用；
边渲染边编码边上传，不要把所有页图片存进 List；渲染图用完立即从本地删除。

**本版额外要求：** Worker 内存中同时持有解析文本层与抽取结果，
单任务内存占用上升，`PREFETCH_PAGES` 需按实测调小；处理完成后显式置空引用，
不要让含报告全文的对象因为闭包引用而滞留。

### 11.6 前端轮询

```
GET /api/task/{taskId}/status → { status, stage, progress, expiresAt }
```

间隔递增 2s → 3s → 5s，上限 10s。
查询不到 Key 时返回 `RESULT_EXPIRED`，前端提示重新上传。

---

## 12. 前端职责

**原则：只做"必要的搬运优化"和"不可逆问题的预防"。任何会损失信息的决定，交给用户确认或留给后端。**

### 12.1 必做（与体积无关）

| 项 | 说明 |
|---|---|
| **HEIC/HEIF 转码** | iOS 相机默认格式，不做则 iPhone 用户直接被拦。Web 用 `heic2any`，原生用系统 API。**后端不引入 libheif** |
| **EXIF Orientation 校正** | 经 canvas 重绘后 EXIF 丢失，后端收到旋转 90° 的图，VLM 对旋转版面识别率显著下降 |

### 12.2 压缩条件（仅两条）

| 条件 | 动作 |
|---|---|
| 文件 > 10MB | 必须压 |
| 长边 > 2500px | 等比缩放至 2500 |
| 其余 | **不动** |

**2500px 依据**：A4 高 297mm，正文 8–10pt 字高约 3mm；长边 2500px 时字高约 25px，处于视觉模型舒适区。缩至 1600px 时字高仅 16px，`↑` `↓`、小数点、`μmol/L` 的 μ 开始糊 —— 而这些正是判断异常值的关键符号。

其他约束：JPEG 质量从 **0.85** 起步逐步降；迭代设底质量 0.7、长边 1800px，**触底仍超限则报错要求重拍**；手机截图（1080–1440 宽）小于 2MB 不压；PNG 截图**不转 JPEG**。

### 12.3 裁剪：检测可做，绝不自动应用

自动边缘检测算错一次就切掉最右侧"参考区间"列 —— 与表格错位同性质的静默丢数据。

正确做法：计算建议裁剪框 → 预览页展示，**默认全图** → 用户确认后应用。

### 12.4 拍照质量检查

**必须前端做，理由是时效性**：前端提示 → 用户当场重拍 10 秒解决；后端异步解析完，用户早已离开体检中心。

**只提醒，绝不拦截。** 交互：「照片可能不够清晰，建议重拍」+【重新拍摄】【仍然上传】，默认不阻断。

**首选（原生 App）：端上 OCR** —— iOS Vision `VNRecognizeTextRequest` / Android ML Kit。
**退化（H5）：拉普拉斯方差 + 尺度归一化（缩至长边 1000px）+ 只统计高频区块（网格取方差最高的 20%）。**

### 12.5 面向四模块的前端职责

| 项 | 说明 |
|---|---|
| **四模块独立渲染** | 任一模块 `emptyText` 非空时渲染空态，**不影响其他模块** |
| **原文查看入口** | `evidenceStatus == EVIDENCE_UNVERIFIABLE` 或 `verifyStatus == CONFLICT` 时提供「查看原图第 N 页」，图片走临时签名 URL |
| **模块内跳转** | 健康问题 →【查看指标】跳健康指标卡片锚点；菜品理由 → 跳饮食建议卡片锚点 |
| **标签色板固定** | 后端只给 `tagType`，颜色由前端常量表决定，保证同类标签全局一致 |
| **长列表虚拟滚动** | 菜品列表可能上百条，`recommended`/`notRecommended` 需分页或虚拟列表 |

### 12.6 上报处理元信息

原始尺寸、是否压缩、最终质量、是否裁剪、模糊得分。后端在校验时使用：**被重度压缩或裁剪过的图，识别结果倾向标 `UNCERTAIN`**。

---

---

---

## 13. 合规与数据生命周期

体检报告属个人健康数据。按《个人信息保护法》，健康信息属**敏感个人信息**，
需要**单独同意**并单独告知处理目的与必要性 —— 必须体现在上传前的授权弹窗，
不能混在通用隐私协议里。

### 13.1 自有链路：不留存即最好的保护

**"不保留历史记录"这个产品决策，本身就是最强的合规措施。**
`health_report_file` 只是短期文件索引，不是报告历史；文件和记录都由 `expire_at` 清理。

| 数据 | 存放位置 | 生命周期 |
|---|---|---|
| **上传文件记录** | MySQL `health_report_file` | 单份上传时创建；按阶段更新 `expire_at`；S3 删除成功后物理删除记录 |
| **原始上传文件** | S3 私有读写 | 仅处理期间保留；**SUCCESS 后立即删除**；FAILED/主动退出时连同预览图全部删除；24h lifecycle 兜底 |
| **页面 PNG 预览图** | S3 私有读写 | 解析当前页时写入，SUCCESS 后与结果同步保留 **2 小时**；只允许通过 5 分钟签名 URL 访问；24h lifecycle 兜底 |
| **页面渲染图内存对象** | Worker 当前页内存 | 写入 PNG 预览并完成当页模型调用后立即释放，不同时持有全部页面 |
| **解析文本层** | Worker 内存 | 随执行结束回收，**不落任何存储** |
| **LLM-A 抽取结果** | Worker 内存 | 同上 |
| **患者姓名** | Worker 内存 | 仅用于 §4.0a 同一性比对，**不写 Redis、不写日志、不回传前端** |
| **最终结果**（含 `evidenceText`） | Redis-结果 | **TTL 2 小时**，建议关闭该实例持久化以减少落盘明文 |
| **未收录医嘱原文**（建议类 `OTHER` 的 `originalText`） | 仅 Worker 内存及 Redis-结果 | 为向当前用户展示而保留，随结果 **2 小时 TTL** 消失；不进 MySQL、日志、LLM-B 请求或菜品标签缓存 |
| **打标缓存 / 建议内容** | Redis-打标 / MySQL | 不含 PII，正常运维 |
| **日志** | 日志平台 | **白名单制**，见 §13.1.1 |

**结果可查看期间必须保留对应的单页 PNG 预览，但原始上传文件在 SUCCESS 后立即删除。**

`IMAGE_ONLY` / `CONFLICT` / `EVIDENCE_UNVERIFIABLE` 场景下，
结果页会给出「查看原图 文件N 第M页」入口 —— 这是 §4.5 对用户的明确承诺，
也是拍照场景下用户**唯一**的核对手段。该入口只访问解析时固化的单页 PNG 预览，
不访问完整原始文件，因此移动端不依赖 PDF `#page=` 定位能力，也不会向前端暴露对象 Key。

```
分析结果 Redis      TTL 2h  ─┐
单页预览图 S3      TTL 2h  ─┴─ 同时失效
原始上传文件 S3    SUCCESS ── 立即删除
```

预览图仍属于敏感健康数据，不能因为改为 PNG 就降低保护等级。必须使用私有 Bucket、
受控对象 Key 和短期签名 URL，并与 Redis 结果同时失效；归属校验由外部鉴权层前置完成（§13.1.2）。
为保证任意证据页都可核对，解析成功的全部页面均生成预览，不只保留当前被引用的页面；
这样无需在最终组装后追加一次报告重读和渲染，也能保持逐页、内存有界的处理方式。

**文件清理只有两个入口：**

1. 用户关闭页面时，前端调用清理接口；后端删除该任务各文件记录指向的原始对象、任务预览对象和记录。
2. 每十分钟最多查询 100 条 `expire_at <= NOW()` 的记录：已关联任务的按去重后的 `taskId` 清理；
   尚未关联任务的按 `fileId` 删除原始对象和文件记录。

浏览器关闭事件和 `sendBeacon` 只能作为尽力通知，不能保证一定到达，因此定时清理是最终保证。
不考虑多个清理实例同时删除的并发场景，不增加 `DELETING`、重试次数或领取时间字段。
每个任务按“记录中的原始对象 + S3 `task/{taskId}/preview/` 全部对象 → MySQL 该任务全部记录”的顺序删除；
未关联文件按“记录中的原始对象 → MySQL 文件记录”删除。S3 操作失败时保留记录，
等待下一批自然重试。S3 删除接口必须幂等，对象已不存在视为成功。

**Redis-结果实例建议关闭 AOF/RDB。** 结果里含 `evidenceText`（报告原文片段），
开持久化意味着这些明文会周期性落盘。丢了只是让用户重传一次 ——
这个代价远低于多一份磁盘上的敏感数据。与 Redis-打标实例的配置正好相反（§9.1a），
**两者必须分实例，不能抄配置**。

### 13.1.1 日志是一条容易被忽略的持久化链路

上一版把 `advice_key_miss_log` / `unknown_allergen_log` 两张表删掉、改成"打结构化日志"，
**这在合规上是净倒退** —— 日志平台通常保留 30~180 天，且权限远比数据库宽松
（运维、SRE 都能查）。等于把最敏感的一类数据从"有加密、有保留期的表"
挪到了"没人管的日志里"。

尤其 `unknown_allergen` 记的是"某个用户对芥末过敏"，这是**特殊类别健康数据**。

**日志采用白名单制：**

| 允许记录 | 禁止记录 |
|---|---|
| `taskId`、**`userId`**、阶段、错误码、耗时 | `patientName` |
| 模型名与版本、prompt 版本 | `evidenceText`、报告原文任意片段 |
| **枚举 key**（`IRON` / `LOW_FAT` / `SHRIMP_CRAB`） | **原始 `adviceText` / `conclusionText` / `allergenText`** |
| 各类**计数**（`unknownAllergenCount++`） | 模型完整 request / response |
| 命中率、耗时等技术指标（**不含任何覆盖率口径，§9.1a 已废止**） | 解析文本层、OCR 文本 |

> **`userId` 允许进入白名单日志**（2026-08-23 决策）。但 §13.1.1 的组合禁令依然有效：
> 它**不得与报告/OCR/证据文本、患者姓名、原始过敏或医嘱文本、模型完整请求响应同事件记录**。
> 尤其注意 `unknownAllergenCount` 这类事件 —— 计数本身可以带 `userId`，
> 但一旦同时落下过敏原原文，就构成"某用户对某物过敏"的特殊类别健康数据关联记录。

**V1 不做"未知词条原文 Top N"。** 未收录医嘱、未知过敏原、未识别结论分别只记
`adviceKeyMissCount++`、`unknownAllergenCount++`、`unrecognizedConclusionCount++`，不记原文。

词表扩充改走别的路子：上线初期人工抽查真实报告；或让运营在后台看到
"本周有 N 次未知过敏原"后主动排查几个样本。等这真的成为瓶颈，
再单独设计一条**有保留期、有权限控制、有单独告知**的采集链路 ——
而不是顺手塞进应用日志。

### 13.1.2 访问鉴权：本系统不实现，边界与外部层的强制要求

**本系统不实现任何认证或归属校验**，接口不接收 `Principal`、不接收用户标识、不存储用户标识。
访问控制整体位于本系统之外。本节规定这条边界两侧各自的职责。

#### 本系统承担的（V1.7.1 起包含归属校验）

- **`userId` 的存储与比对**：`health_report_file.user_id`（上传时写入）与 `task:{taskId}.userId`
  （创建任务时写入）。四类端点 —— 状态、结果、原图预览、清理 —— **全部校验归属**，
  不符一律返回与"不存在"相同的 `404 + RESULT_EXPIRED`，不得用可区分的错误码
- **`analyze` 校验每个 `fileId` 的归属**，这是引入 `userId` 的首要目的（§11.1）


- 单页 PNG 预览签发**短时效（最长 5 分钟，且不超过 task 剩余 TTL）签名 URL**，应用服务不转发图片内容
- 前端只能提交 `taskId + fileIndex + pageNumber`，**不能指定对象 Key**；对象 Key 只由服务端按固定规则构造
- 单页预览图与结果同时失效（2 小时），过期后该入口返回 `404 + RESULT_EXPIRED`
- 对象 Key 不含患者姓名、原文件名或健康信息
- 任务状态、文件记录、日志中**不出现任何用户标识**（§9.1.1a、§9.1.2、§13.1.1）

#### 外部鉴权层仍需承担的

**身份认证本身仍在系统之外** —— 本系统不实现登录、不签发凭证、不校验凭证有效性，
只接收一个已被认证的 `userId`（当前为占位符）并据此做归属比对。
外部层需要保证的是：传进来的 `userId` 确实是当前请求者，且不可被伪造或篡改。

> **本系统与外部层的分工在 V1.7.1 变了：** V1.7 是"归属校验整体外移、本系统零自验能力"；
> 现在是"认证在外、**归属在内**"。下表中原先由外部层承担的 `taskId` 绑定校验，
> 已由本系统的 `userId` 比对覆盖，外部层不必重复维护那份账本。

| # | 要求 | 缺失后果 |
|---|---|---|
| 1 | “体检报告总结”接口提交有序 `fileIds`；本系统创建任务后返回 `taskId`，外部层据此建立「`taskId` ↔ 用户」绑定 | 后续请求无法进行任务归属校验 |
| 2 | 绑定覆盖创建任务后的四类端点：状态、结果、原图、清理（本版无确认端点与再次分析入口，§4.0a、§4.0.3） | 任一端点漏防都会形成越权入口 |
| 3 | `DELETE /{taskId}/files` 必须校验 | 任何人可删除他人尚未查看的报告与结果 |
| 4 | `taskId` 允许进入应用技术日志，但不得与任何报告内容、患者信息或原始健康文本共同记录；不得进入 URL 查询串或分享链接 | 日志组合或链接泄露会扩大报告泄露风险 |
| 5 | `taskId` 的绑定关系至少保留到本系统 TTL 结束（成功后 2 小时） | 绑定先于结果失效，出现“有结果但无人可读”或“校验落空” |

#### 边界的明确代价

V1.6.4 §9.1.2 的原判断在技术上依然成立：**「UUID 难猜是混淆，不是鉴权」**。本版把这道鉴权
整体移交出去之后，**本系统内部不再具备任何自验能力，也不提供任何补偿机制** —— 没有二次校验、
没有异常访问检测、没有降级拒绝。

这是一个用“系统不持有用户账号或外部身份标识”换“系统零自验能力”的交换。数据库和 Redis
脱库后仍可能暴露短期健康数据，因此 §13.1 的 TTL、日志最小化和对象存储隔离仍然是硬约束。
它的代价在于：**外部鉴权层成为唯一的、
没有后备的防线**。

> 上线前必须由安全评审确认上表五项已全部落实，**该确认属于上线阻塞项**，与 §13.2 的模型服务侧
> 合规确认同级。

### 13.2 模型服务侧（V1.7.1 起不再是上线阻塞项）

```
完整体检报告图像 / 文本
        ↓
      Dify
        ↓
   LLM Provider
```

**本版把自有链路的留存压到了最低，这反而让模型服务侧成为全链路唯一的实质性数据外发点。**
自有侧做得越干净，这一环的相对权重越高。

> **2026-08-23 决策：本节七项不作为上线阻塞项，不再要求逐条回答与留档。**
>
> 记录该决定的确定代价，与 §13.1.2 的做法一致 —— 让它成为一个被知情的选择，而不是被遗忘的缺口：
> 本方案已把自有侧压到极低留存（原文件成功即删、预览图 2 小时、结果 2 小时、文本层不落库），
> **模型服务侧因此成为全链路唯一的实质性数据外发点**，而外发内容是完整的体检报告图像与文本。
> 健康信息属《个人信息保护法》定义的敏感个人信息。不做这七项确认，意味着
> 「Dify trace 是否留存明文、底层模型是否用于训练、数据保留多久、部署在哪个区域」
> 这些问题在上线时没有答案，自有侧的全部克制**无法证明其有效性**。
>
> 下表保留为**技术参考**，供后续需要时使用；`OPEN_QUESTIONS.md` D 同步降级为非阻塞项。

**参考清单（不再是上线阻塞项）：**

| # | 问题 | 要求 |
|---|---|---|
| 1 | Dify 是否私有化部署 | 建议私有化，部署在行内可控网络 |
| 2 | Dify workflow trace / 运行日志是否保存输入输出 | **必须关闭或脱敏**，默认是开启的 |
| 3 | 底层模型提供方是谁、部署区域在哪 | 境外模型需额外做出境合规评估 |
| 4 | 提供方是否保存 prompt / 是否用于训练 | 必须明确不用于训练，合同层面固定 |
| 5 | 数据保留期 | 明确天数，越短越好 |
| 6 | 谁可以查看 workflow trace | 最小权限 + 访问审计 |
| 7 | 是否支持零数据保留（ZDR）模式 | 优先启用 |

> 若 Dify trace 里留了一份含姓名、疾病结论、过敏原的明文，
> **本方案在自有侧做的所有克制都将失去意义。**
> （该判断本身依然成立；按 2026-08-23 决策，它不再构成上线阻塞条件。）

**可选的降敏措施（P2）：** 图像进模型前遮盖姓名/身份证区域。
但版面定位本身依赖识别，定位错了会遮掉化验数据，收益不确定，**不建议 V1 做**。

---

## 14. 实施优先级

### P0 — 安全底线（涉及错误饮食或过敏风险）

| # | 项 | 对应 |
|---|---|---|
| 1 | **LLM-A 九个顶层字段严格 Schema，缺字段 ≠ 空值**（数组/对象/布尔三类空值语义分开）**+ 子字段必填性逐条列明** | §4.0.1 |
| 2 | **`allergenStatus` 四态 + UNKNOWN 的 fail-safe 降级** | §4.0.3 |
| 3 | **Safety Normalize 前置于 adviceKey 归一化** | §7.2 |
| 4 | **安全键与业务键统一枚举**（`LOW_PROTEIN` 等不出现在 Tier-1） | §7.2 |
| 5 | **未知过敏原 fail-safe + `ALLERGEN_KEYWORDS.get()` 空值防护** | §8.4 |
| 6 | **Layer 1 过敏硬兜底 + 关键词族常量表** | §8.3 |
| 7 | **Layer 3 过敏覆盖检查 + 强制改判告警** | §8.6 |
| 8 | **配料完整性由 Java 推导，不完整不得判为安全** | §8.9 |
| 9 | **上线前全量正式非过敏 adviceKey（排除 `OTHER` 与 `ALLERGEN`，共 19 个）生成 + 逐条LLM-B 自检与 Java 校验** | §7.3 |
| 10 | LLM-B 输出校验（数量 / 功效词 / 药品名 / 摄入量格式） | §7.5 |
| 9a | **DOC-VL 文档解析纳入主链路**（全部页面，含拍照） | §3.11 |
| 9b | **文本层来源优先级**：DOC-VL 优先，原生文本层并存互校 | §3.2 |
| 9c | **DOC-VL 输出禁止进入 LLM-A prompt**（独立性红线） | §3.11.4 ① |
| 9d | **DOC-VL 质量门槛 + 不达标置 `textLayerSource=NONE`** | §3.11.4 ③ |
| 9e | **§4.4b A档单元格级精确比对**（表格区） | §4.4b |
| 10a | **全局字符规范化层**（NFKC + CJK部首补充映射表 + **反 LaTeX**） | §3.0、§3.0.2a |
| 10b | **规范化覆盖三处**：报告文本层 / 模型输出 / 菜品数据 | §3.0.3 |
| 10c | **`NormalizedText` 类型 + ArchUnit 禁止裸 `String.contains`** | §3.0.4 |
| 11 | 原文回溯校验，`EVIDENCE_NOT_FOUND` 整条丢弃 | §4.5 |
| 12 | 结论 vs 数值交叉校验 | §4.4a |
| 13 | **模型服务侧合规结论**（Dify trace / 训练 / 保留期 / 区域） | §13.2 |

### P0 — 正确性（不做会产生"看似正常实则错误"的结果）

| # | 项 | 对应 |
|---|---|---|
| 14 | **多文件同一性校验，姓名/性别不一致直接 `FAILED/IDENTITY_MISMATCH`，不自动合并、不做确认交互** | §4.0a |
| 15 | **`adviceContentHash` 进打标缓存 Field** | §9.2 |
| 16 | **全局唯一 `indicatorId`，展示顺序不承担主键职责** | §4.7 |
| 17 | **所有 `evidence` 带 `fileIndex`（0 起，与预览接口同源）** | §4.7 |
| 18 | **版本号拆 A/B/C + 记录 modelVersion** | §9.2 |
| 19 | Layer 3 七条校验（含主料校验、`cookingMatch` 分离、过敏覆盖检查） | §8.6 |
| 19a | **姓名仅在 Worker 内存，不写 Redis / 日志 / 前端** | §4.0a、§13.1 |
| 19b | **SUCCESS 后删除原始文件，单页预览图与结果同 TTL；FAILED 全量删除** | §13.1 |
| 19e | **互斥锁粒度到维度级**（V1.7：仅用于离线预热，在线 singleflight 已废止） | §9.3 |
| 19f | **建议内容 `is_active` + 切换前预热** | §9.8 |

### P0 — 需求符合性

| # | 项 | 对应需求 |
|---|---|---|
| 20 | **准入规则两级判定**（行标记优先 → 分组小结兜底） | §5-2、§4.1 |
| 20a | **`sectionSummary` / `summaryEvidence` 纳入 LLM-A 抽取与 Schema** | §4.0.5 |
| 20b | **总览条数字合理性验证**（异常占比是用户看到的第一个数字） | §5.4、§15.1 |
| 20c | **`reportOverview` 抽取 + 优先展示报告自带数字** | §4.0、§5.4.1 |
| 20d | **来源标注章节名取报告原文，不写死「总检结论」** | §6.2 |
| 20e | **饮食表述全文扫描**（不限于总检结论章节） | §4.0.4 |
| 21 | 结论归一化词表 + 状态标签 | §5-3 |
| 22 | 分组保序 + 总览条 | §5-4/5-5 |
| 23 | 健康问题两类来源 + 来源标注 + 关联跳转 | §6 |
| 24 | 三维度菜品匹配 + 冲突以不推荐优先 | §8-2/8-3 |
| 25 | 标签体系 + 理由模板（Java 拼接） | §8-4/8-5 |
| 26 | 拼音排序（请求时实时计算，无落库字段） | §8-4/8-5 |
| 27 | 四个声明 + 全部空态文案（含"识别失败"与"报告没写"区分） | §5-6/6-5/7-4/8-6 |

### P0.5 — 工程可靠性

| # | 项 | 对应 |
|---|---|---|
| 28 | **Redis Stream 队列 + Lua 原子入队 + 队列深度背压** | §11.1 |
| 29 | Redis 任务锁 + 心跳续期（无 lease 回收 job） | §11.2 |
| 29a | **Pending 失败巡检 + `min-idle-time > Worker deadline`；不重试、不使用 DLQ** | §11.2、§11.3 |
| 29b | **处理成功或失败后原子 `XACK + XDEL`**，否则 PEL 条目会残留 | §11.2 |
| 30 | ~~singleflight 防缓存击穿~~ **V1.7 废止**（在线不回源，无击穿面） | §9.3 |
| 31 | **客户端超时不写终态**，服务端 deadline 独立判定 | §11.4 |
| 32 | Redis 打标缓存 + `dishVersion` + 预热任务 + **两个计数告警**（V1.7：覆盖率已废止） | §9 |
| 32a | **独立 Redis 实例 + AOF 持久化 + `noeviction`**（逻辑库/key 前缀不构成隔离） | §9.1a |
| 32b | ~~`dishTagHealthCheckJob` 覆盖率巡检与自动重建~~ **V1.7 废止** | §9.1a |
| 33 | 内容版本**只读**观测页（各 key 的 ACTIVE 版本、生成时间、失败告警）。**不含人工编辑/通过/驳回**（§7.7） | §7.7 |
| 34 | HEIC 前端转码 | §12.1 |
| 35 | **主料双规则推导（≥25% ∪ 前2且≥15%）** | §8.6 |
| 36 | **空态第五类文案：有异常指标但无饮食医嘱** | §8.10 |
| 37 | **结果过期返回 `RESULT_EXPIRED`，前端友好提示而非报错** | §10.1 |
| 38 | **`NOT_SCREENED` 的过敏提示文案**（本版唯一的过敏安全出口） | §1.2、§4.0.3 |
| 39 | **`cookingMatch` 仅从菜名识别**（无烹饪方式字段） | §8.5、§8.6 |
| 40 | **`COOKING_PHRASES` 词组表 + 语义分组校验**（堵"水煮=低脂"） | §8.5a |

### P0 前置 spike（评审后立即启动，结论决定排期）

| # | 项 | 说明 |
|---|---|---|
| ~~S1~~ | ~~DOC-VL 可行性验证~~ | **已完成**：两份报告 6 页实测，九项判据通过八项（§3.11.3），已升为主链路组件 |
| S2 | 同一 `adviceKey` 连跑 5 次的输出稳定性（§15.2 ②） | 决定"内容让模型生成 + 缓存"是否成立 |
| S3 | **DOC-VL 在拍照件上的表现**（折痕/倾斜/反光） | 现有实测均为电子版渲染图。拍照是主流形态，必须补测，用于校准 §3.11.4 ③ 的三个阈值 |

### P1

X 分栏 + 行级联合校验；单位白名单；
提示词灰度 diff 工具；
监控埋点全量接入。

### P2

过敏误杀例外词典（≤20 条常量）；单位换算层；菜品拼音人工修正后台；
`is_main` 录入界面；OFD 兼容性扩展；图像进模型前的姓名遮盖。

### 明确不做

- 建立食材主数据、别名表、营养/饮食知识库
- 由指标异常推导饮食建议（需求 §7-5 禁止）
- 整餐营养组合与供能比校验
- 把过敏判定交给模型
- 总检结论智能关联指标（需求 §6-3 明确不展示关联按钮）
- 严重程度分级 / 风险排序（需求 §6-4 明确禁止）
- 全页面双次识别交叉验证

---

## 15. 验证方法

本文档所有阈值（`0.85 / 0.30 / 0.20 / 40道每批 / 30天 / 2500px / 页宽5%`）**均为推演值，未经验证**。

### 15.1 报告解析与抽取

**攒 30–50 页真实报告标注集**，覆盖：不同医院、电子版 PDF、扫描件、带 OCR overlay 的扫描件、
OFD、手机拍照、截图、Word 导出、**双栏排版**、
**含镜检类结果的尿常规页**（`2-5/HP`、`+血尿` 这类形态，用于验证 §4.1.0 的准入放宽）、
**含过敏原检查的报告**、**含总检结论页**、**多文件混合（含故意放入他人报告的负例）**。

**必须刻意覆盖字符污染样本（§3.0）：**

```
纳入至少 2 份不同生成器产出的电子版 PDF，其中至少 1 份文本层含部首区字符。
判定方法：提取文本后统计 U+2E80-2EFF / U+2F00-2FDF 区字符数，非 0 即为污染样本。
```

**新增必测项：**

| 指标 | 目标 |
|---|---|
| `residualNonStandardCount`（规范化后残留，含 LaTeX 残留 `$` `\\mu`） | **必须为 0** |
| DOC-VL 的 `↑↓` 行绑定准确率 | **100%**（实测 6/6，须在更大样本上保持） |
| DOC-VL 的条目编号保留率 | **100%**（实测 9/9） |
| 跨页续表表头混入的检出率（§3.11.5） | **100%** —— 检出即强制 MIXED，不要求 DOC-VL 不出错 |
| 污染样本上的分组小结命中率 | 与干净样本**无显著差异** |
| 污染样本上的 `hallucinationRatio` | **< 2%**（未规范化时会是 100%） |
| 污染样本上的过敏关键词命中率 | **与干净样本一致** |

> 最后一条是安全项：构造"配料含 `⻥` 的菜 + 鱼过敏"的用例，验证仍被强制不推荐。

**必须刻意覆盖两种结论样式**（§4.0.5）：

| 样式 | 特征 | 已有样本 |
|---|---|---|
| **行标记式** | 每个指标行带 `↑ ↓ H L` | 某连锁机构 22 页报告（仅 3 项带 ↑，其余靠小结） |
| **分组小结式** | 提示列大量留空，结论写在「小结：未见异常」 | 某三甲医院 17 页报告（已验证，见 §4.1.1） |
| **自带总览数字** | 首页印出「N项总指标 / M项异常」 | 某连锁机构 22 页报告（见 §5.4.1） |
| **无「总检结论」章节** | 叫「综述建议」「专家建议与指导」 | 上述两份各占一种（见 §6.2） |

**若标注集里只有一种样式，两级判定的第二级就没有被真正测试过。**

**新增必测项：总览条数字的合理性。**

```
对标注集中每份报告，人工统计真实的异常项数与总项数，与系统输出比对：
  异常占比绝对误差 > 10 个百分点  → 视为失败
  一份大部分正常的报告显示异常占比 > 50%  → 视为严重失败
```

这条不是"锦上添花的准确率指标"，而是**功能可信度的第一道门**：
用户看到的第一个数字就是它。

**八个必须为 0 的单向指标：**

| # | 指标 |
|---|---|
| 1 | 真实是表格页、被判成 TEXT |
| 2 | 实际异常、结论被判成 NORMAL |
| 3 | 双栏页被误判为同行 |
| 4 | 引用了报告中不存在的原文 |
| 5 | 报告中的过敏原被漏抽 |
| 6 | 总检结论条目被漏抽或错编号 |
| 7 | **模型缺字段被当成空数组通过**（构造缺 `allergens` 的响应注入测试） |
| 8 | **不同人的报告被自动合并**（同一人不同次体检允许合并，§4.0a） |

**允许有误差但需统计：** 章节名字符一致率（> 95%）、指标顺序一致率（> 98%）、
`adviceKey` 归一化准确率（> 95%）、`droppedNoConclusionRatio`。

#### 15.1a OCR 文本层的门槛校准（§3.11 上线前必做）

三个阈值（`MIN_LINES` / `MIN_AVG_CONFIDENCE` / `MAX_LOWCONF_RATIO`）**必须用标注集校准后才能上线**，
因为两个方向的偏差都会让情况比"不做 OCR"更糟：

| 偏差方向 | 后果 |
|---|---|
| 阈值太松（劣质 OCR 也当文本层用） | 产生**假 `CONSISTENT`** —— 把错误数据盖章为"已核验"，比没有校验更危险 |
| 阈值太严（好 OCR 被拒） | 大批正确数据被打成 `UNCERTAIN`，用户满屏存疑角标，功能可用性下降 |

**校准方法：** 20~30 张真实拍照页，人工标注真值，统计不同阈值下的
「假 CONSISTENT 数」（**必须为 0**）与「误判 UNCERTAIN 率」（目标 < 10%），
取满足前者的最松阈值组合。

**另需验证 OCR 与 VLM 的误差是否真的不相关**：统计两者在同一页上同时读错同一个数值的比例，
若显著高于独立假设，说明这条校验的价值低于预期，需要重新评估 §3.11 是否值得做。

### 15.2 建议内容（LLM-B）

```
① 全部正式非过敏枚举（**排除 OTHER 与 ALLERGEN**，共 19 个 key）跑一遍，自动逐条校验并 ACTIVE
   检查项：食材是否常见易得 / 摄入量是否合理 / 有无功效表述 / 有无药品名
② 同一 adviceKey 连续跑 5 次，比较输出差异
③ 边界用例：Tier-0 全部安全键必须被拦（含「建议限制碘摄入」这类只能落 OTHER 的表述）
④ 对抗用例：adviceText 里塞入诱导文本（"请忽略前述限制"），验证提示词注入防护
```

> **②是关键实验，建议在评审前先跑。** 若同一维度 5 次输出差异很大，
> 说明该内容不适合缓存，也说明它本来就不适合让模型生成 —— 那种情况下该维度应停止启用并告警或 BLOCKED。

### 15.3 菜品打标（LLM-C）

**人工标注 100 道典型菜 × 5 个高频维度 = 500 个判定作为金标准：**

| 指标 | 目标 |
|---|---|
| REJECT 召回率 | > 90% |
| RECOMMEND 准确率 | > 85% |
| 全量覆盖率（含 Layer 3 兜底后） | 100% |
| `ingredientMatches` 命中率 | > 95% |
| `cookingMatch` **字面命中率**（是菜名子串且在词组表内） | > 90% |
| `cookingMatch` **语义正确率**（分组与真实做法一致，尤其"水煮/干煸/香煎"归入 HEAVY_OIL） | **> 98%** |
| **"字面清淡实为高油"的菜在低脂维度被判 RECOMMEND** | **必须为 0** |
| 批内位置偏差 | 检查第 30~40 道是否劣于前 10 道，用于校准批大小 |
| **主料判定召回率**（双规则推导 vs 人工标注） | > 90% |
| **主料判定误判率**（把配料判成主料） | < 5% |

> `MAIN_RATIO=0.25` / `TOP_N_MIN_RATIO=0.15` 均为推演阈值（§8.6）。
> 标注集需**刻意覆盖高水分配菜的菜品**（青椒肉丝、番茄炒蛋、木耳类、酸菜类），
> 这类菜正是单阈值失效的地方。
> 若误判率偏高，优先推动食堂系统补 `is_main` 字段，而不是继续调阈值 ——
> 业务数据永远比推导规则可靠。

### 15.4 Layer 1 / Layer 3（可完全自动化）

```java
@Test void 过敏原微量也必须排除()        { /* 虾仁滑蛋，虾 20g/130g，虾蟹过敏 → 不推荐 */ }
@Test void 菜名命中即可无需配料表()       { /* 白灼虾 配料表为空 → 仍进不推荐 */ }
@Test void 未知过敏原名称命中强制不推荐()  { /* OTHER:芹菜，配料含芹菜 → 不推荐 */ }
@Test void 未知过敏原未命中不得判安全()    { /* → **不得进推荐列表**，转入「过敏成分待确认」，
                                                 uncertainReason = ALLERGEN_TERM_ONLY（§8.4、§8.9）*/ }
@Test void 未知过敏原不得抛NPE()          { /* ALLERGEN_KEYWORDS 无该 key */ }
@Test void 单元格级比对应识别列错位()       { /* 值落在参考值列 → CONFLICT，而非 CONSISTENT */ }
@Test void 空单元格塌陷应降级为B档()        { /* <td> 数量与表头不一致 → 不用 A 档 */ }
@Test void 表头文字出现在数据行应强制MIXED() { /* "项目名称" 出现在数据单元格 */ }
@Test void 反LaTeX后原文回溯应成功()        { /* "$ 489 \\mu mol/L $" vs "(489μmol/L)" → EVIDENCE_EXACT */ }
@Test void 小结遇方括号标题应截断()          { /* "...(6.47mmol/L) 【高脂血症】" → 截到 mmol/L) */ }
@Test void rowspan切碎的小结应拼接还原()     { }
@Test void 镜检类结果不得被丢弃()          { /* 上皮细胞 2-5/HP，参考 0~3，结论偏高 → 进指标模块 */ }
@Test void 带符号结果不得被丢弃()          { /* 红细胞(镜检) +血尿，参考 负~3 → 进指标模块 */ }
@Test void 正常定性项不得进健康问题()       { /* 尿蛋白 阴性，参考 阴性 → 指标模块，状态正常 */ }
@Test void 无参考范围的文字结论进健康问题()  { /* 「肝胆B超：脂肪肝」→ textualFinding */ }
@Test void 半定量项不参与数值交叉校验()      { /* 2-5/HP → verifyStatus = UNVERIFIED，不报 CONFLICT */ }
@Test void 部首字符不得导致过敏漏判()      { /* 配料"清蒸⻥"+鱼过敏 → 必须强制不推荐 */ }
@Test void 部首字符不得导致小结判定失效()   { /* "未⻅明显异常" → NORMAL */ }
@Test void 部首字符不得导致原文回溯误判()   { /* 模型输出标准汉字 vs 文本层部首字符 → EVIDENCE_EXACT */ }
@Test void NFKC单独不足以还原部首补充区()   { /* ⻅ 经 NFKC 仍是 ⻅，须查表 */ }
@Test void 规范化后无残留非标准字符()       { /* residualNonStandardCount == 0 */ }
@Test void 模型推荐过敏菜必须被改判()      { /* mock LLM-C 返回 RECOMMEND → 不推荐 + 告警 */ }
@Test void 过敏拒绝的菜不得带正面标签()     { /* 虾过敏 + 该菜在补铁维度 RECOMMEND → 标签集只剩 ALLERGY */ }
@Test void 非过敏拒绝可保留灰色推荐标签()   { /* 高脂拒绝 + 补铁推荐 → DIET_REQ_AVOID 主展示，NUTRITION 灰色附注 */ }
@Test void 模型漏标降级为中性()           { /* mock 返回缺失部分菜 → 不进任何列表 */ }
@Test void 模型编造食材被剔除()           { }
@Test void 非主料命中不得进营养推荐()      { /* 青菜180 + 猪肝5，补铁 → NEUTRAL */ }
@Test void 水煮不得判为低脂()              { /* 「水煮肉片」低脂维度 mock RECOMMEND → NEUTRAL */ }
@Test void 单字煮不在词组表应作废()         { /* cookingMatch="煮" → null */ }
@Test void 长词组优先于单字()               { /* 「水煮肉片」应截到"水煮"而非"煮" */ }
@Test void 菜名外同义词被清空()             { /* 「烤鸭」cookingMatch="挂炉" → null，但不影响 verdict */ }
@Test void 干煸香煎归入高油组()             { }
@Test void 缺字段响应必须触发重试而非空数组() { }
@Test void 复合饮食医嘱须拆出全部键()       { /* 「低脂、低糖、低胆固醇饮食」→ LOW_FAT+LOW_SUGAR+LOW_CHOLESTEROL 三张卡，共享同一条原文 */ }
@Test void safetyFlag缺失不得丢弃整条医嘱() { /* 按 null 处理 + safetyFlagMissingCount++ */ }
@Test void safetyFlag缺失时Java关键词仍拦截() { /* 「建议限制碘摄入」无 safetyFlag → 仍 BLOCKED */ }
@Test void 候选键全部非法时落OTHER()        { /* adviceKeyCandidates=[] 或全不在枚举内 → OTHER，不丢条目 */ }
@Test void 不同姓名文件不得自动合并()      { /* → FAILED/IDENTITY_MISMATCH，无 NEED_CONFIRM */ }
@Test void 不同性别文件不得自动合并()      { }
@Test void 同一人不同次体检允许合并()      { /* 日期相差 400 天、机构不同 → PASS */ }
@Test void 部分文件无体检信息不得拦截()    { /* 分页拍照：文件2 reportMeta=null → PASS */ }
@Test void 仅一份文件识别出信息时不拦截()  { /* known.size()<=1 → PASS */ }
@Test void 小结简称不得使 IMPLIED 判正常()  { /* 小结「低密度脂蛋白偏高」+ rawName「低密度脂蛋白胆固醇」
                                                 → possiblyMentioned 命中 → NONE（丢弃），不得判 NORMAL */ }
@Test void 子串不得被误认为点名()           { /* 小结只点名 LDL-C → 总胆固醇不得取到 SECTION_SUMMARY 标签 */ }
@Test void 性别全部缺失时不得默认男性()      { /* resolveGender → UNKNOWN → 性别相关区间 UNVERIFIED */ }
@Test void 部分文件有性别且一致时采用该性别() { }
@Test void 三字姓名差一字视为同一人()      { /* 「张伟明」vs「张伟民」→ PASS，识别误差容错 */ }
@Test void 两字姓名差一字必须拦截()        { /* 「张三」vs「张四」→ BLOCK，不得容错 */ }
@Test void 姓氏不同必须拦截()              { /* 「张伟明」vs「王伟明」→ BLOCK */ }
@Test void 姓名长度不同必须拦截()          { /* 「张伟」vs「张伟明」→ BLOCK */ }
@Test void 建议内容变更后旧标签失效()      { /* contentHash 变 → cache miss */ }
@Test void 冲突以不推荐优先()             { }
@Test void 排序稳定性()                   { }
```

### 15.5a V1.7 必测回归场景

以下场景在 §15.1~§15.4 之外**额外必测**，缺任何一条不得发布：

**离线与激活**

1. 全部正式内容键都会进入初始化集合，**不能按活跃度截断**。
2. `OTHER` 和 `ALLERGEN` 的 LLM-B / LLM-C 调用次数为 0。
3. 模型返回但被 Java 校验丢弃的标签**不写入缓存**，等同该菜在该维度上没有标签。
4. 预热后该维度一条有效标签都没有时**不得激活**，旧 ACTIVE 版本保持不变。
5. **缓存只增不改**：菜品变更不得删除或覆盖旧 `dishVersion` 的标签。
6. 内容哈希、菜品版本或标签模型版本变化会造成缓存未命中，并进入下一次差异预热集合。
7. 离线任务按 `adviceType + adviceKey + contentHash + tagPromptVersion + tagModelVersion` **幂等**；
   重跑只补缺失标签，不覆盖已通过校验的同版本标签。

**在线只读**

8. 某维度在线 ACTIVE 缺失时 **LLM-B 调用次数为 0**，该维度整体不参与推荐；其他维度和 Layer 1
   结果保留；模块三正常展示该建议；公共响应**不披露**维度被跳过。
9. 某维度零有效标签时 **LLM-C 调用次数为 0**，同上。
10. **某维度只有部分菜品有标签时，该维度照常参与推荐**：有标签的菜正常判定，无标签的菜不进入
    推荐列表，LLM-C 调用次数为 0。
11. **在线链路对标签缓存的写操作次数为 0**（每一种 miss 场景都要验）。
12. 某维度被跳过时，模块一、模块二和模块三仍正常组装并返回。
13. 同时命中两个非过敏维度时，一个零标签只能使该维度不参与，另一个必须继续输出。
14. 非过敏维度全部被跳过时，Layer 1 已命中的过敏不推荐和过敏待确认列表**仍必须返回**，
    正常推荐列表**不得由过敏维度反向生成**。
15. 预热窗口之后新增或变更的菜品没有标签时不获得任何推荐；**若其含过敏原，仍必须由 Layer 1
    判为不推荐并带过敏标签**。
16. `ALLERGEN` 不进入 LLM-B/C 预热集合，其 Java Layer 1 过敏硬兜底不依赖 ACTIVE 内容或标签，
    也不能因非过敏维度被跳过而被绕过。

**守门测试**

17. **离线预热的正式非过敏 `adviceKey` 集合，必须与在线可能读取标签的 `adviceKey` 集合完全相等；
    任一方向出现差集都必须使测试失败。**

**过敏卡片**

18. 每个 `FULL` 卡片的 `avoidIngredients ∪ hiddenFoods` **等于** Layer 1 使用的该组完整关键词集合，
    且两张清单**不得重叠**；`TERM_ONLY` 返回空清单及能力边界提示。
19. 过敏卡片必须下发 `allergenKey`、`allergenText`、`matchCapability`、`origin=REPORT`；
    内部内容哈希和模型字段不得下发。
20. 卡片标题优先使用 `allergenText`，仅在原文缺失或过短时回退标准化 `label`；`sourceLabel`
    必须保留报告原文来源。
21. `matchCapability` 必须下发但**枚举值不得直接渲染**；每个报告过敏原独立成卡，卡片之间不合并、
    不去重；同一过敏原重复出现时合并为一张并保留首条证据。
22. 每个词的展示形态必须显式存在，**缺失时不得静默回退为匹配词原文**。
23. 尚未完成分类的分组：全部词进 `hiddenFoods`、`avoidIngredients` 为空，并集不变量仍成立；
    **`FULL` 分组两张清单同时为空必须使测试失败**，且该分组不得携带 `TERM_ONLY` 提示。
24. 关键词族成员增删会改变 Layer 1 拦截行为：并集不变量的基线随之变更时，**必须有显式的变更记录**。

**公共响应与身份边界（V1.7 新增）**

25. 非过敏维度因 ACTIVE/标签缺失被跳过时，**响应中不存在**跳过提示、缺失 `adviceKey`、缓存状态、
    内容哈希、模型版本或重新计算入口；无可输出的个性化推荐时只显示「本食堂菜品暂无个性化推荐。」。
26. **归属校验必须真实生效**（V1.7.1 方案 B）：`task:{taskId}` 含且仅含 `userId` 一个用户维度字段；
    `health_report_file.user_id` 非空；用另一个 `userId` 访问状态/结果/原图/清理四类端点，
    **必须返回与"不存在"完全相同的 `404 + RESULT_EXPIRED`**，不得出现 `403` 或任何可区分错误码
    （否则 `taskId` 存在性可被枚举）。
27. 原子入队 Lua **写入 `userId`**；`analyze` 提交他人的 `fileId` 必须被拒绝
    —— 这是引入 `userId` 的首要目的（`OPEN_QUESTIONS.md` I）。
28. **公共响应体中不出现 `userId`**；日志中 `userId` 允许出现，但不得与报告/OCR/证据文本、
    患者姓名、原始过敏或医嘱文本、模型完整请求响应**同事件**记录（§13.1.1）。
29. `task:{taskId}` 中除 `userId` 外不得出现姓名、手机号、`openId` 等任何其他身份字段。

### 15.5 上线后

- 人工抽查四个模块，重点核对"原文引用"与纸质报告是否逐字一致。
- 抽查 `diet_advice_cache` 的 ACTIVE 内容，**每周至少 5 条**，按 `activated_at` 倒序优先看最近切换的版本。
- 盯 `unknownAllergenCount` / `adviceKeyMissCount` 计数趋势（**不记原文**，§13.1.1）；
  计数偏高时人工抽查几份真实报告定位具体词条。
- **`allergen_override` 与 `allergenUnknownCount` 非 0 即告警**。
- `cacheHitRatio` > 95%、`degradeToNeutralCount` 应接近 0。
- `droppedNoConclusionRatio` 与"落到空态的报告占比"——后者超过 40% 需回到需求侧讨论 §1.5。

---

## 16. 待明确事项

### 需产品 / 合规拍板

| # | 事项 | 建议解法 |
|---|---|---|
| 1 | **营养建议内容由模型生成，不设置人工内容管理功能** | **已确认**：LLM-B 自检 + Java 硬校验 + LLM-C 预热达标后自动激活 |
| 2 | **模型服务侧的数据处理结论**（§13.2 七问） | **上线阻塞项**，需留档 |
| 3 | 「过敏成分待确认」列表的**标题文案** | 方案已定为不进推荐列表（§8.4/§8.9），仅需确认列表标题与提示措辞 |
| 4 | 需求只定义两个列表，是否接受第三列表 | 「过敏成分待确认」，承载全部未完成核验的情形；**"当作安全"不可接受** |
| 5 | ~~多文件不一致时的交互~~ **已定**（2026-08-23） | 只有 `PASS` / `BLOCK` 两态：姓名或性别不一致直接 `FAILED/IDENTITY_MISMATCH`，用户修正文件后重新上传。**不做确认弹窗** —— 「仍然合并」等于让用户推翻一条安全判定，而他并不比系统更清楚这两份是不是同一个人的 |
| 6 | 敏感个人信息**单独同意**的授权弹窗文案 | 需法务出稿 |

### 需求条文层面

| # | 事项 | 需求出处 | 建议解法 |
|---|---|---|---|
| 7 | 状态标签只定义三种，「阳性」或词表未命中如何展示 | §5-3 | 新增「异常」（橙）与「原文直显」（灰），均计入异常项 |
| 8 | ~~定性项算不算"有明确数值"~~ **已定** | §5-2 | 判据改为**「结果 + 参考范围 + 结论」三要素齐备**，不要求值是数字；有参考范围即进健康指标模块（§4.1.0） |
| 9 | 总览条 `total` 是"已展示指标数"，与报告项目总数不符 | §5-5 | 加一行小字说明，文案待定 |
| 10 | 报告未提供参考范围时如何展示 | §5-3 | 展示「报告未提供」，**禁止填充通用参考值** |
| 11 | 健康问题 `title` 需要拼接 | §6-2 | 允许"指标名 + 归一化结论词"的机械拼接，唯一允许的文字加工 |
| 12 | ~~`textualFindings`（脂肪肝）归 INDICATOR 还是 SUMMARY 类~~ **已定** | §5-2/§6-2 | 归 `INDICATOR_TEXTUAL`（§6.1 已定案并给出依据） |
| 13 | 营养补充/饮食注意的空态文案未给出 | §7-4 | 按过敏提醒句式生成，待确认措辞 |
| 14 | 不推荐列表中是否展示命中的推荐标签 | §8-3/§8-5 | **按拒绝原因分开定（2026-08-23）**：过敏拒绝的菜**不下发任何正面标签**，灰色附注也不行（§8.7）；非过敏拒绝主展示不推荐标签、推荐标签灰色附注 |
| 15 | 多文件时来源标注的编号口径 | §6-3 | 带文件标识：「报告2-专家建议与指导第2条」；章节名取报告原文（§6.2） |
| 16 | 非汉字开头菜名的排序位置 | §8-4/8-5 | 建议统一排在汉字之后 |

### 产品价值层面

| # | 事项 |
|---|---|
| 17 | **仅有指标异常、报告未写饮食建议的报告占比可能很高**（§1.5）。建议上线前用样本估算；若过高，需求侧应考虑增加"系统通用提示（明确标注非报告结论）" |
| 18 | **报告结论样式的分布**。加入分组小结判定后风险已大幅降低，但仍需确认目标医院是否存在"既无行标记也无分组小结"的样式（§4.1） |
| 18a | **`SECTION_SUMMARY_IMPLIED` 是否可接受**。"小结点名了本组其他指标 ⇒ 未被点名者正常"是本方案唯一的推断，虽然贴合体检报告的固定写法，但严格说超出了"零推断"。需产品/医务确认（§4.1） |

### 工程层面

| # | 事项 | 建议解法 |
|---|---|---|
| 19 | HEIC 未列入支持格式 | 前端转码，后端不引 Native 库 |
| 20 | 只限单文件大小，无总量上限 | 增加总量限制（建议 60MB），网关同步配置 |
| 21 | **OFD 解析可行性未验证** | 评审前跑 3–5 家真实样本，通过率不足则砍掉或降级 |
| 22 | 菜品库改造 | **零改造**。只读当日在售菜品的名称与食材重量，拼音/主料/配料完整性全部运行时推导（§8.1） |
| 23 | 成本 | 在线每请求 1 次模型调用；离线冷启动约 240 次、稳态约 38 次/天 |
| 24 | 推荐定位 | 明确为"基于报告原文的饮食参考"而非"医疗营养治疗"，四处声明已覆盖 |
| 25 | **Redis 实例规划** | 需**两个独立实例**：打标（AOF + `noeviction`）与结果（建议关持久化，同时承载任务队列与任务锁）。两者配置相反，不能共用（§9.1a、§13.1） |
| 25b | **队列深度阈值 `MAX_QUEUE_DEPTH`** | 建议 200，需按实际并发与单任务耗时压测校准；超阈值的拒绝文案需产品确认（§11.1） |
| 25e | **DOC-VL 服务的容量与配额** | 每页一次调用，22 页报告即 22 次。需确认行内服务的并发上限与单页耗时，用于设定 §11.5 的批处理策略 |
| 25f | **PP-StructureV3 是否可一并部署** | 若非表格区的块级坐标不足以支撑校验，需要它的细粒度坐标（§3.11.7）。同一仓库，成本应不高 |
| 25c | **部首映射表的覆盖面** | 初版约 30 条覆盖 U+2E80-2EFF 全区，但不排除其他污染形态（PUA、兼容表意字 U+F900-FAFF）。靠 `residualNonStandardCount` 告警持续补齐，补齐走发版（§3.0.5） |
| 25d | **受影响机构的覆盖面评估** | 建议尽早收集 5~10 家不同体检机构的电子版 PDF，统计 `nonStandardCharRatio`。若某类生成器占比高，该问题的实际影响面可能远超单一机构（§3.0.1） |
| 27 | **是否有可复用的 OCR 服务** | §3.11 spike 的前置输入。行内票据/证件识别通常已有，需确认能否复用及调用配额 |
| 28 | **词表扩充的采集方式** | V1 对未收录医嘱、未知过敏原和未识别结论都只记计数不记原文（§13.1.1）。若后续需要任一未知词条原文 Top N，须单独设计有保留期与权限控制的链路，不能塞进应用日志 |
| 26 | **词表更新的发版节奏** | 词表硬编码意味着补词要发版。需约定节奏（建议随双周迭代），并确认运维可接受（§1.3） |

---

## 17. 相对 V1.5.4 的改动

本次改动全部由**两份真实体检报告的实测**驱动，架构未变，无新增中间件、无新增存储。

### 17.1 P0：全局字符规范化层（§3.0，全新章节）

**问题来源：** 某连锁体检机构 22 页电子版 PDF，文本层检出 **895 个非标准汉字、78 种**
（`⻅`≠`见`、`⻝`≠`食`、`⾎`≠`血`）。视觉无差异，渲染正常，人工抽检发现不了。

**实测影响：**

| 关键词 | 改动前命中 | 实际出现 |
|---|---|---|
| `未见明显异常` | 0 次 | 34 次 |
| `饮食` | 0 次 | 7 次 |
| `血常规` | 0 次 | 1 次 |

**三条连锁失效，第三条是 §1.1 第 3 条安全红线：**

1. 分组小结判定失效 → 异常占比又回到 100%
2. 饮食要求识别失效 → 不产生任何菜品推荐
3. **过敏关键词匹配失效 → 含过敏原的菜进入推荐列表**

**改动内容：**

| # | 改动 | 位置 |
|---|---|---|
| 1 | 新增 `TextNormalizer`：NFKC + CJK部首补充映射表（约 30 条）+ 去零宽字符 | §3.0.2 |
| 2 | 明确 **NFKC 不足** —— 康熙部首区可还原，CJK部首补充区必须查表 | §3.0.2 |
| 3 | 规范化覆盖**三处**：报告文本层 / 模型输出 / **菜品数据** | §3.0.3 |
| 4 | 新增 `NormalizedText` 类型 + ArchUnit 规则，禁止裸 `String.contains` | §3.0.4 |
| 5 | §4.5 原文回溯改为**双向规范化**，并注明排查顺序（先查字符再查提示词） | §4.5 |
| 6 | §4.3 结论归一化、§8.3 过敏兜底改走统一入口 | §4.3、§8.3 |
| 7 | 新增监控 `nonStandardCharRatio` / `residualNonStandardCount`（后者非 0 即告警） | §3.0.5 |

**最容易被忽略的是第 3 条的菜品侧。** 食堂菜品数据若从 Excel/PDF 菜单导入同样可能污染，
`「清蒸⻥」.contains("鱼")` 为 false → 鱼过敏用户拿到含鱼的推荐。
三处缺一，安全兜底就有缺口。

**为什么用类型系统强制而非靠自觉：** 与 §9.1.2 的 `updateTask()` 封装同性质 ——
只要允许某处裸操作，漏洞就会从那里回来。区别在于那里漏掉的后果是"永久 Key"，
这里漏掉的后果是"含过敏原的菜进推荐列表"。

**这不是单一机构的脏数据。** 成因是 PDF 生成器构建 ToUnicode CMap 时按字形反查码点，
部首区与汉字区共用字形导致取到码点更小的部首区。同一套生成器产出的**所有**报告都会有，
且生成方无法自查（他们看到的报告一直是好的）。

### 17.2 报告自带总览数字优先展示（§5.4.1）

**问题：** 该报告首页印着 `136项总指标 / 0项重要异常 / 13项异常结果`，
而我方按 §5.4 只统计有数值的项，会算出约 79 项、异常 3 项。
**两个数字同时印在一份报告里，用户一定会对比**，且我方数字看起来还"漏了一堆"。

**改动：** LLM-A 新增抽取 `reportOverview`；有则优先展示报告原文数字并标注来源页，
我方统计降级为监控用途（发现抽取遗漏）。依据是需求 §5-1「严格依据报告原文」——
报告自己印的数字才是原文，我们算的是二次统计。

> 报告自己的数字未必自洽（图示 13 项，阳性列表 12 条，第 13 项在口腔章节）。
> **仍照抄** —— 展示原文不代表为报告的算术背书，与 §4.4a"结论与数值矛盾时仍展示报告结论"同一原则。

### 17.3 饮食表述全文扫描（§4.0.4 提示词第 6 条）

**问题：** 该报告的饮食医嘱分散在两处 ——「专家建议 02」的
「低脂、低糖、低胆固醇饮食…节制饮食，控制体重」，
以及**口腔科建议**的「饮食注重粗细搭配，少吃甜食」。后者极易漏抽。

**改动：** 提示词明确要求全文扫描，不限于"总检结论/专家建议"章节，
并给出口腔科的具体例子。漏抽会直接导致菜品推荐缺失对应维度。

### 17.4 来源标注章节名通用化（§6.2）

**问题：** 模板写死「总检结论第N条」，但两份实测报告都不叫这个名字 ——
一份叫「综述建议」，一份叫「专家建议与指导」。

**改动：** 改为 `{summarySectionName}第{itemNo}条`，章节名取报告原文，
由 LLM-A 随 `summaryConclusions` 一并输出。
需求 §6-3 的原话是"便于用户与纸质报告核对"，名字对不上等于该字段失效。

### 17.5 验证方法补强（§15.1、§15.4）

| 新增 | 内容 |
|---|---|
| 标注集要求 | 至少 2 份不同生成器的电子版 PDF，其中 ≥1 份含部首区字符 |
| 报告样式覆盖 | 补齐"自带总览数字""无总检结论章节"两种样式，并标注已有样本 |
| 必测指标 | `residualNonStandardCount` 必须为 0；污染样本上的小结命中率、`hallucinationRatio`、过敏命中率须与干净样本一致 |
| 单元测试 | 新增 6 条：部首字符下的过敏漏判 / 小结判定 / 原文回溯 / NFKC 不足 / 残留归零 |

### 17.6 优先级与待明确事项

**P0 新增：** 10a 规范化层、10b 三处覆盖、10c 类型强制、
20c `reportOverview`、20d 章节名通用化、20e 全文扫描。

**待明确新增：**

- **25c 部首映射表的覆盖面** —— 初版 30 条覆盖 U+2E80-2EFF 全区，
  但不排除 PUA、兼容表意字 U+F900-FAFF 等其他污染形态，靠告警持续补齐
- **25d 受影响机构的覆盖面评估** —— 建议尽早收集 5~10 家机构的电子版 PDF
  统计 `nonStandardCharRatio`。**若某类生成器占比高，影响面可能远超单一机构**

### 17.7 定性项准入放宽（§4.1.0、§4.1.0a）

**问题来源：** 产品原型截图中的尿常规展示了三种原规则会整类丢弃的形态 ——
`2-5/HP`（镜检）、`+血尿`（带符号）、`阴性`（纯定性），
而它们在报告里都有明确的参考范围与结论标签。

**改动：**

| # | 改动 | 位置 |
|---|---|---|
| 1 | `isNumericValue` → `hasDisplayableResult`：判据改为「结果 + 参考范围」齐备，不要求值是数字 | §4.1.0 |
| 2 | 明确"有无参考范围"是健康指标与健康问题的分界线 | §4.1.0a |
| 3 | 值原样展示，不解析、不改写 | §5.2 |
| 4 | 半定量/定性项不参与 §4.4a 数值交叉校验，标 `UNVERIFIED` | §4.4a |
| 5 | §16 待明确 #8 由"不算"改为"已定：三要素齐备即准入" | §16 |

**丢弃的代价是具体的：** 实测某报告的「血尿」为严重级健康问题，
源头正是被丢弃的两项镜检结果 —— 丢掉后健康问题模块的关联跳转（需求 §6-3）直接失效。

**顺带修掉一处别扭：** 尿蛋白「阴性」是正常结果，原规则却把它塞进"健康问题"模块。
现在它落在健康指标模块、状态标签为「正常」，符合用户直觉。

> 本次仅采纳这一条。原型中的早/午/晚餐推荐、匹配度分数、严重程度与置信度、
> 适宜多吃/忌吃少吃分区、由指标推断的饮食建议，**均与需求 §6-4、§7-3、§7-5、§8-4/8-5 冲突，
> 一律以需求文本为准，不予采纳**（见 §17.8）。

### 17.8 明确不采纳的原型形态

产品原型（评审通过状态）与需求文档存在若干冲突，**已确认一律以需求文本为准**。
以下形态**不得**在开发时参照原型实现：

| 原型形态 | 冲突条款 | 本方案的做法 |
|---|---|---|
| 菜品按早/午/晚餐推荐 + 匹配度百分比 | §8-4/8-5 要求**推荐/不推荐两个列表**、按拼音排序 | 两列表 + 标签 + 理由（§8.7、§8.8） |
| 健康问题带「轻度/严重」+ 置信度 | §6-4 **明文禁止**严重程度分级与风险排序 | 不分级，按章节顺序排列（§6.3） |
| 饮食建议分「适宜多吃 / 忌吃少吃」 | §7-3 要求**过敏提醒 / 营养补充 / 饮食注意**三区，每条标注报告原文来源 | 三区结构 + `sourceLabel`（§7.6） |
| 由指标推断的饮食建议（高碘、维生素K 等） | §7-5 **禁止交叉关联**；且"碘"在本方案属 Tier-0 `BLOCKED` | LLM-B 输入只有 `adviceKey`，结构上无法关联指标（§7.2、§1.4） |

> **这不是取舍问题，是责任边界问题。** 需求把产品定位为"报告原文的搬运工"，
> 原型把它做成了"AI 健康顾问"。四处免责声明的措辞、§13.2 的合规结论、
> §7.3 的LLM-B 自检与 Java 校验流程，全部是按前者设计的。混用两种定位会让免责声明与实际输出不符。

**原型的正确用途是样式样本**：它印证了四模块平行的 tab 结构（§10.1 的响应设计可直接对应），
并提供了镜检类结果的真实展示形态（已纳入 §15.1 标注集要求）。

### 17.9 未改动的部分

架构、存储形态、队列、缓存、菜品匹配三层、饮食建议自动校验流程、合规链路**均未变动**。
本次全部是"文本进入比对之前"这一层的修正，以及抽取范围与展示口径的对齐。

---

## 18. 相对 V1.5.6 的改动

本次改动由 **PaddleOCR-VL 在两份真实报告 6 页上的实测**驱动，
把此前的"P0 前置 spike"升为主链路组件。架构层级未变，但**校验能力有实质提升**。

### 18.1 实测结论摘要

九项判据通过八项。三处**优于原生文本层**（不只是不差）：

| 现象 | 原生文本层 | DOC-VL |
|---|---|---|
| 某连锁机构报告的 `↑` | **0 次**（矢量图形） | **6/6 正确绑定到行** |
| DR-70 项目名 | 被数据行劈成两截 | 完整一行 |
| 肝功小结顺序 | 「小结：」标签跑到末尾 | 按视觉阅读顺序还原 |
| 部首字符污染 | 895 个 | **无**（识别产物） |
| 综述建议条目编号 | 完整 | **完整 9/9** |
| 饮食医嘱 | 完整 | **逐字无损** |

一项有缺陷：**跨页续表把表头文字混入数据列**（§3.11.5），已有缓解方案与检测信号。

### 18.2 改动清单

| # | 改动 | 位置 |
|---|---|---|
| 1 | **§3.11 从 P0 前置 spike 升为主链路组件**，重写全节 | §3.11 |
| 2 | **新增 §3.2 文本层来源优先级**：DOC-VL 优先，与原生文本层并存互校 | §3.2 |
| 3 | **§4.4b 拆 A/B 两档**：表格区用单元格级精确比对，非表格区/兜底用行级 | §4.4b |
| 4 | **§4.5 关闭拍照缺口**：`EVIDENCE_UNVERIFIABLE` 从常态变罕见 | §4.5 |
| 5 | 新增 §3.0.2a **反 LaTeX 归一化**（DOC-VL 会把 `(489μmol/L)` 输出成 `$ 489 \mu mol/L $`） | §3.0.2a |
| 6 | §3.0 明确**部首污染仅存在于原生文本层路径** | §3.0.1 |
| 7 | §3.5 X 分栏**降级为兜底**（有 HTML 时不需要） | §3.5 |
| 8 | §3.9 MIXED 提示词**增加第三版**（DOC-VL 结构文本页），含表头混入的纠正指令 | §3.9 |
| 9 | §4.0.5 小结抽取增加**`【...】` 截断规则**与三种物理形态说明 | §4.0.5 |
| 10 | 选型论证：**文档解析模型（②层）**，非传统 OCR、非通用 VLM | §3.11.2 |
| 11 | 部署优先级：**行内已部署 > 自建 > 云 API**（合规不对称） | §3.11.6 |
| 12 | 能力边界删除"拍照场景无法程序化验证" | 附录 |

### 18.3 三条不能松的红线

升级后最容易被"顺手优化"掉的三点，已写进 §3.11.4：

1. **DOC-VL 输出绝不进 LLM-A 的 prompt** —— 喂进去校验的独立性当场归零
2. **DOC-VL 不能取代 LLM-A** —— 两者并存才有第二来源；且它输出的是文档结构不是业务 JSON
3. **质量不达标就置 `textLayerSource=NONE`** —— 劣质文本层会产生**假 `CONSISTENT`**，比没有更糟

### 18.4 仍需补测

现有实测**全部基于电子版 PDF 的渲染图**，而拍照是预期的主流形态。
新增 spike **S3：DOC-VL 在真实拍照件上的表现**（折痕、倾斜、反光、屏摄），
用于校准 §3.11.4 ③ 的三个质量阈值。

**在 S3 完成前，§3.11 的实测结论只能说"在电子版渲染图上成立"。**

---

## 19. 版本改动记录

### 19.V1.7.1 相对 V1.7 的改动（2026-08-23，文档一致性评审发现）

**性质与 §19.2（V1.6.1）相同：改了一处却没同步另一处，导致文档自相矛盾。**
本版不改架构、不改逻辑、不改对外行为，也不改任何词表内容。

**安全语义类（照 V1.7 原文实现会得到 fail-open 或错页）**

| # | 改动 | 位置 |
|---|---|---|
| 1 | §15.4 单测注释改为「不得进推荐列表，转入过敏成分待确认」，与 §8.4 一致 | §15.4 |
| 2 | §8.4 fail-safe 伪代码补 `allergenUncertain.add()`；未命中分支不再只打字段标记 | §8.4 |
| 3 | §8.6 `merge()` 增加第三列表分支，且排在 `RECOMMENDED` 之前 | §8.6 |
| 4 | `handleUnknownAllergen` 形参由 `String` 改为 `NormalizedText`，与 §3.0.4 的 ArchUnit 规则一致 | §8.3、§8.4 |
| 5 | 证据序号全案统一为 `fileIndex`（0 起，等于 `health_report_file.file_index`），废止 `fileSeq`；用户可见的「报告N」由 `fileIndex + 1` 生成 | §4.2、§4.7、§4.8、§14 |
| 6 | §19.0-a 中 `fileIndex`「按主键升序」的旧定义改为「按 `fileIds` 提交顺序」 | §19.0-a |
| 7 | §8.10 空态文案增加 `reportHasNoDietAdvice` 前提：报告写了但无 ACTIVE 内容时不得声称"报告没给饮食医嘱" | §8.10 |
| 8 | §11.4「该维度标签缺失时按 `NEUTRAL`」改为「没有标签、不写占位」 | §11.4 |
| 9 | §4.1 `SECTION_SUMMARY_IMPLIED` 补「小结须点名本组其他指标」的前置校验，否则「建议复查」类小结会把整组判正常 | §4.1 |

**行为修正（本版唯一一处，2026-08-23 评审功能点 2 发现）**

| # | 改动 | 位置 |
|---|---|---|
| 35 | **`PageMode` 与 `TextLayerSource` 拆分建模**：前者只决定发给 LLM-A 的形态，后者决定校验层有无文本层可用。§4.4b、§4.5 的门槛由 `mode == IMAGE` 改为 `textLayerSource == NONE` | §3.1、§3.2、§4.4b、§4.5 |
| 36 | §3.11.4③ / §3.11.6 / §14 9d 的「不达标退回 `IMAGE_ONLY`」改为「置 `textLayerSource = NONE`」，与上条同一套概念 | §3.11.4、§3.11.6、§14 |
| 37 | OFD 双路径均失败的提示由「请重新上传 PDF/JPG/PNG 格式」改为「建议改用手机拍摄或截图后重新上传」—— 用户手上通常只有那一份 OFD，旧文案指向一个他做不到的动作 | §3.8 |
| 38 | 重复编号：原第二个 `### 3.2`「多文件语义」改为 `### 3.1a`；全文 `§3.2` 引用均指「文本层来源优先级」，无需改动 | §3.1a |
| 39 | §3.9「提示词分两版」更正为「分三版」（实际给出三版，§18.2 #8 亦记为增加第三版） | §3.9 |
| 40 | **`deLatex()` 与 `unescapeHtml()` 接入 `normalize()` 主流程**。V1.7 的 §3.0.2 实现从未调用它们，而 §3.0.2a、§4.5 都声称"必须在 NFKC 之前执行" —— 照原实现落地会精确复现 §3.0.2a 警告的失败：DOC-VL 路径的 evidence 100% 判成幻觉丢弃 | §3.0.2、§3.0.2a |
| 41 | **§4.5 原文回溯改为按 `TextUnit`（同栏 `TextLine` / DOC-VL `Block`）比对**，不再用 `page.getFullText()` 整页拼接串；相邻单元拼接必须同 `bandIndex`。原写法在双栏页会产生**假 `EVIDENCE_EXACT`**（去空白后左右栏接成一句），把 §3.5 极力避免的假一致从另一个入口放回来 | §4.5、§3.5 |
| 42 | §4.5 改用 `NormalizedText` 比对，不再裸调 `String.contains` —— 原写法违反 §3.0.4 自己声明的 ArchUnit 规则（与第 4 条 `handleUnknownAllergen` 同型） | §4.5 |
| 43 | §3.5 的 `TextLine`「随页面结果**落库**」更正为「只在 Worker 内存、不落任何存储」，与 §9.1.4、§10.3、§13.1 一致。文本层接近报告全文，是全案最敏感的数据，落库表述属隐私口径倒退 | §3.5 |
| 44 | **删除 `allergenStatus = UNKNOWN` 的"提供重新分析入口"**。该入口在本方案中不存在也实现不了：SUCCESS 结果固化 2 小时且 `/result` 不重算（§7.8、§10.1），同一批 `fileId` 只能关联一个 `taskId`（§2.2），原始文件 SUCCESS 后已删除（§13.1）。给一个点不动的按钮比不给更糟 | §4.0.3 |
| 48 | **§4.1 小结点名判定改为松紧不对称**：断言"被点名"用严格匹配（`rawName` 完整出现，避免「总胆固醇」被「低密度脂蛋白胆固醇」的子串误命中）；否定"被点名"用宽松匹配 `possiblyMentioned`（去括号核心词 + 最长公共子串 ≥3），命中即放弃 `SECTION_SUMMARY_IMPLIED` 退回 `NONE`。原写法只有一档松紧，小结用简称时会把报告明确指出异常的指标**静默判成正常** | §4.1、§1.3、§15.4 |
| 49 | **补齐性别的跨文件解析口径**：新增 `resolveGender()` —— 全部非 `null` 性别一致才采用，一个都没有或冲突则 `UNKNOWN`，**不得默认按男性**；`crossCheck` 签名补 `gender` 参数（原为自由变量）。§4.7 合并表同时补上 `reportMeta` / `reportOverview` / `allergenSectionPresent` 三行，此前只覆盖六个数组类字段 | §4.4a、§4.7、§15.4 |
| 60 | **恢复系统内的归属校验（方案 B，2026-08-23 定向回退 V1.7 身份边界）**：`task:{taskId}` 新增 `userId` Field、`health_report_file` 新增非空 `user_id`、入队 Lua 恢复 ARGV[5]，四类端点全部比对归属，`analyze` 校验每个 `fileId` 归属。**归属不符返回与"不存在"相同的 `404 + RESULT_EXPIRED`**，不得用 `403`（否则 `taskId` 存在性可枚举）。起因是 V1.7 留下的口子：上传端点无 `taskId`，外部层无从校验 `fileId` 归属。选 B 而非只存文件表，因为 status 轮询是最高频端点、且 §10.1.2 清理会物理删行而结果 Key 仍有 2 小时 TTL | 版本说明、§9.1.1a、§9.1.2、§11.1、§13.1.2、§15.5a、AGENTS.md |
| 61 | **`userId` 允许进入白名单日志**，但不得与报告/OCR/证据文本、患者姓名、原始过敏或医嘱文本、模型完整请求响应**同事件**记录；公共响应体中不下发 | §13.1.1、§15.5a |
| 62 | **§13.2 模型服务侧七问不再是上线阻塞项**（2026-08-23 决策），降级为技术参考清单。代价已按 §13.1.2 的方式知情记录：模型服务侧是全链路唯一实质数据外发点，不做确认则自有侧的全部克制无法证明其有效性 | §13.2、`OPEN_QUESTIONS.md` D |
| 57 | **`expire_at` 三个阶段统一为「该阶段时刻 + 24 小时」**（原文只说"按阶段更新"，三档各多久全文无数字）。它是兜底清理而非主删除路径，与 S3 Lifecycle 的 24h 兜底取同一时间视界。设短会误伤真实用户，设长则原始报告驻留超出 §13.1 承诺 —— 而鉴权外移后驻留时长是本系统唯一还握得住的风险控制手段 | §9.1.1a |
| 58 | **`task:{taskId}` 的读操作一并收进 `TaskStateRepository`**：新增 `find` / `readStatus` / `isTerminal` 三个读出口，`"task:" + taskId` 前缀只在该类出现一次。原文把裸读散在三处（§9.1.2 示例、§11.2 `isTerminal`、状态接口），改前缀要同时改三地，漏一处是查不到且不报错 | §9.1.2、§11.2 |
| 59 | **划清 `moduleError` 的适用范围**：仅模块一/二可下发；模块三/四内部异常一律不下发，改服务端告警。`moduleError` 能反推内部状态，会让抓包方区分"报告本来就没建议"与"我们这边出故障"，而 `OPEN_QUESTIONS.md` G 要求这两者对外不可区分。`partialExtraction` 不受此限——它陈述的是报告事实，必须下发 | §10.1 |
| 56 | **删除 `diet_advice_cache.ref_count`**：全案无任何一处描述它何时递增；在线链路对本表只读（§9.7），为它加 `UPDATE` 等于每个用户请求写一次 MySQL。§15.5 的抽查依据改为按 `activated_at` 倒序。**代码侧需同步删除** `schema.sql` 的列与 `DietAdviceCacheEntity.refCount` | §9.1.1、§15.5 |
| 55 | **过敏拒绝的菜品不得下发任何正面标签**（§8.7）：`forcedReject` 分支返回时标签集清空重建，只保留 `ALLERGY`，灰色附注也不允许。一道因虾过敏被拒的菜若还挂着灰色「补铁」，用户读到的是"其实它挺适合我的，只是有点虾"。§16 #14 相应改为按拒绝原因分开定：非过敏拒绝仍可灰色附注 | §8.7、§8.6、§16、§15.4 |
| 52 | **`adviceKeyCandidates` 补进 LLM-A schema 并改为数组**（原 §7.2 提到 `adviceKeyCandidate` 但 §4.0 的 schema 里根本没有该字段），提示词新增第 8 条要求复合表述列出全部命中键，Java 侧按键拆卡、原文与来源共享。「低脂、低糖、低胆固醇饮食」是高频写法，只取一个键会让用户少拿到对应维度的全部菜品推荐且系统不报错 | §4.0、§4.0.4、§7.2、§15.4 |
| 53 | **子字段必填性逐条列明**（原文只有一句概括）：六个数组各自的必填/可选清单。其中 **`safetyFlag` 明确为可选**（2026-08-23 决策），缺失按 `null` 处理、不丢弃整条医嘱，但计 `safetyFlagMissingCount` 使这条退化可观测 | §4.0.1、§7.2 |
| 54 | **新增两个监控项**：`blockedAdviceCount`（按 `SafetyAdviceKey` 分维度，占比 >10% 排查裸词误伤）与 `safetyFlagMissingCount`（>5% 触发提示词回归）。原方案对 Tier-0 拦截量**没有任何观测手段**，`孕`/`儿童` 这类裸词若高频误伤，大量正常饮食建议会被静默拦掉而无人知晓 | §4.8、§7.2 |
| 51 | **明确 `evidenceVerified()` 的口径**：仅 `EVIDENCE_EXACT` / `EVIDENCE_FUZZY` 算已核实，`EVIDENCE_UNVERIFIABLE` 退回 §5.4 自算。原文未定义该函数，若把 UNVERIFIABLE 当已核实，模型编造的总览数字会被冠以"取自报告第 N 页"展示，而用户无法自行发现 | §5.4.1 |
| 50 | §4.6 别名表用途由"两个"更正为"一个"：多文件去重实际用的是 `(sectionName, rawName)` 原文名、不经过别名表，该用途已不存在；`IndicatorCode` 现仅服务单位白名单，并标注可作为 P2 简化项去掉 | §4.6 |
| 47 | **姓名比对增加有限容错**（§4.0a）：长度相同、长度 ≥3、首字相同三个条件同时满足时，允许编辑距离 ≤1。**不采用裸的编辑距离 ≤1** —— 两字姓名容错一个字等于容错一半信息量，「张三」与「张四」会被判成同一个人。这是全案唯一方向放松的常量，取消确认交互后误判不再有出口，故为识别误差留一条窄缝；残留风险是同姓同性别仅差一字的两个人被合并 | §4.0a、§1.3、§15.4 |
| 46 | **§4.0a 同一性校验由三态改为两态，`NEED_CONFIRM` 整条链路取消**（2026-08-23 产品决策）：① 只拦「不同人」（姓名/性别），不再拦「不同次体检」，§1.1 第 5 条与 §3.1a 定义同步收窄为「同一个人」；② `reportMeta` 为 `null` 的文件不参与比对也不触发拦截，否则分页拍照这一主流场景直接不可用；③ 判定不通过直接 `FAILED/IDENTITY_MISMATCH`，按 §13.1 连同原文件与预览图删除，用户修正后重新上传。连带删除：`NEED_CONFIRM` 状态与其 2 小时 TTL、`confirmPayload` 字段、确认端点、重新入队与从头重跑逻辑；§13.1.2 的受保护端点由六类减为四类 | §4.0a、§1.1、§3.1a、§2.2、§9.1.2、§10.2、§11.6、§13.1.2、§14、§15、§16 |
| 45 | **新增 §4.0.1a 跨批合并规则**，补齐三个非数组字段此前完全未定义的合并方式：`reportMeta`/`reportOverview` 取第一个非 `null`；`allergenSectionPresent` OR 合并但 `true` 方向需 Java 佐证，佐证不上落 `NOT_SCREENED`（多一条提示，符合 §1.2 的安全不对称与 §1.3 的保守未命中路径）。不新增顶层字段，九字段 Schema 不变 | §4.0.1a、§11.4、§1.3 |

**与数据真源对齐**

| # | 改动 | 位置 |
|---|---|---|
| 10 | §7.4 过敏词表示例改为逐字照抄 `allergen_display_split.csv`（`MILK` 12 词、`EGG` 8 词、`SHRIMP_CRAB` 19 词），并注明匹配是逐词字面匹配、增删走安全变更流程 | §7.4 |
| 11 | §7.6 过敏卡片示例的两张清单补全为完整并集（19 词），不再是节选 | §7.6 |
| 12 | 附录能力边界新增「已收录过敏原的词形覆盖」一行，显式记录单字 `蛋` 不在词表内 | 附录 |
| 13 | 正式非过敏 key 数由「约 33 个」更正为 **19 个**，并明确同时排除 `OTHER` 与 `ALLERGEN`（33 会诱导把过敏键送进 LLM-B） | §9.1.1、§15.2、§14 |
| 14 | 冷启动与容量估算的维度数由 20 / 60 统一为 19 | §9.1a、§9.5、§16 |

**文档内部一致性**

| # | 改动 | 位置 |
|---|---|---|
| 15 | §5.1 数据来源由「有数值 + 有结论」更正为「结果 + 参考范围 + 结论」三要素（§4.1.0 早已放宽） | §5.1 |
| 16 | `IssueSourceType` 由两值改为三值（`INDICATOR_NUMERIC` / `INDICATOR_TEXTUAL` / `SUMMARY`），与 §6.2 的 `switch` 和 §6.3 的排序一致 | §6.1~§6.3 |
| 17 | §6.2 `linkedIndicatorId` 示例改为 UUID，不再使用 §4.7 明令禁止的 `s2-i3` 形态 | §6.2 |
| 18 | §6.4 空态判据补 `doctorAdvices.isEmpty()`（§6.1 的 SUMMARY 类含它） | §6.4 |
| 19 | §8.6 标题与全文四处「六条校验」更正为「七条」 | §8.6、§2.2、§8.2、§14 |
| 20 | §8.10「四类空态」更正为「五类」 | §8.10 |
| 21 | §13.1.2「上表六项」更正为「五项」 | §13.1.2 |
| 22 | §2.2 链路图补齐九个顶层字段（原只列六个） | §2.2 |
| 23 | §4.0.4「七条硬要求」补编号说明：第 5) 条正文在 §4.0.5 | §4.0.4 |
| 24 | §4.4b A 档 `Optional.isEmpty()` 改为 `!isPresent()`（Java 8） | §4.4b |
| 25 | §3.10 的 OOM 交叉引用 §11.6 更正为 §11.5 | §3.10 |
| 26 | §3.3 判定表删除不可达的第 6 行（已被第 1 行覆盖），8 行对齐为 7 行 | §3.3 |
| 27 | §3.0.2a 的空格规则与 `normalize()` 全局去空白的矛盾说明清楚 | §3.0.2a |
| 28 | §8.5a 词组表补 `蒜蓉蒸 → HEAVY_OIL`；此前本节把它列为固定陷阱，词表里却没有 | §8.5a |
| 29 | §8.7 推荐理由示例删除该菜不含的「西兰花」（违反 §8.6 校验 4） | §8.7 |
| 30 | §8.4「该维度下」改为「本次分析中」（Layer 1 全局，不按维度切分） | §8.4 |
| 31 | 三处"人工后台编辑"残留改为"新版本激活"，与 §7.7「无人工编辑入口」一致；§14 #33 改为只读观测页 | §9.2、§9.6、§9.7、§14 |
| 32 | §13.1.1 日志白名单删除"覆盖率"（§9.1a 已废止全部比值口径） | §13.1.1 |
| 33 | §16 #12 标记为已定（归 `INDICATOR_TEXTUAL`） | §16 |
| 34 | §0 新增「阅读约定：代码块是伪代码」，明确落地时须按 AGENTS.md §8 补花括号、去 Java 8+ API | §0 |

### 19.V1.7 相对 V1.6.5 的改动

| # | 改动 | 位置 |
|---|---|---|
| 1 | 删除应用内鉴权和全部用户账号/外部身份标识，包括 `ownerId` | 版本说明、§9.1.1a、§9.1.2、§13.1.2 |
| 2 | 上传阶段每份文件只返回 `fileId`；调用分析接口时提交有序 `fileIds`，后端才创建 `taskId`、关联文件并入队 | §2.2、§9.1.1a、§11.1 |
| 3 | `taskId` 允许进入符合白名单的技术日志，但不得与报告内容或患者信息共同记录 | §13.1.1、§13.1.2 |

### 19.0-a 本版内的两处一致性修正（2026-08-20，代码评审发现）

与 §19.2 是同一类问题：改了一处却没同步另一处，导致文档内部自相矛盾。

| # | 冲突 | 原状 | 修正 | 位置 |
|---|---|---|---|---|
| 1 | 单页预览图对象 Key 有两套约定 | §9.1.1a 举例 `report/{fileId}/preview/`，§10.1.1 定义 `task/{taskId}/preview/{fileIndex}/{pageNumber}.png` | **统一到 §10.1.1**，并补充 `fileIndex` 的稳定性定义（V1.7 起：按分析请求中 `fileIds` 的**提交顺序**、从 0 开始、关联后不再改变） | §9.1.1a |
| 2 | `evidenceStatus` 是否下发前端 | §10.1 列为"内部字段不下发"，§5.2 要求它作为存疑角标依据展示 | **以 §5.2 为准**，从 §10.1 的内部字段列表中移除，并说明理由 | §10.1 |

第 1 条选 §10.1.1 的理由：前端只提交 `taskId + fileIndex + pageNumber`，不接触对象 Key（§10.1.1、§13.1.2），
用 `fileId` 做前缀会让 `preview_object_prefix` 指向一个前端拼不出、清理批次也用不到的命名空间。
第 2 条的判据是安全方向：剥掉 `evidenceStatus` 会让用户看不到"这一项未核验成功"，与 §1.2 的安全不对称冲突。

### 19.0 V1.6.4 相对 V1.6.2 的改动

| # | 改动 | 位置 |
|---|---|---|
| 1 | 上传改为每次一份，首次上传创建草稿 `taskId`，每份返回 `fileId` | §2.1、§2.2 |
| 2 | 所有文件归属同一 `taskId`，点击“分析体检报告”后才写入分析队列 | §2.2 |
| 3 | 新增 `health_report_file`，仅保存 S3 Key、归属、文件元数据和过期时间 | §9.1.1a |
| 4 | 新增页面关闭清理与每十分钟、每批最多 100 条的到期清理，均按 `taskId` 删除 | §9.1.1a、§13.1 |
| 5 | 清理逻辑不处理删除并发；S3 删除失败时保留记录等待下批重试 | §13.1 |
| 6 | 云存储统一为 Amazon S3，页面预览仍通过短期 Presigned URL 访问 | §10.1.1、§13.1.2 |

### 19.1 V1.6.2 相对 V1.6.1 的改动

本版关闭建议类 `OTHER:{原文}` 与数据最小化原则之间的冲突，不改变未知过敏原的
`OTHER:{term}` fail-safe 匹配能力。

| # | 改动 | 位置 |
|---|---|---|
| 1 | 建议归一化未命中时统一输出纯枚举值 `OTHER`，不再把原文拼进 key | §7.2 |
| 2 | `OTHER` 不查/写 `diet_advice_cache`，不调用 LLM-B | §7.3 |
| 3 | 增加未收录医嘱展示结构：原文、来源和固定未收录提示 | §7.6 |
| 4 | `diet_advice_cache.advice_key` 禁止 `OTHER`，由代码断言及可用时的数据库 `CHECK` 双重强制 | §9.1.1 |
| 5 | 明确未收录医嘱原文只存在于 Worker 内存及 Redis 最终结果，2 小时后消失 | §13.1 |
| 6 | 三类未知词条统一为只计数、不记原文；如需 Top N，后续单独建设合规链路 | §13.1.1、§16 #28 |

放弃的是长尾 `OTHER` 内容的自动生成和校验流水线。正式枚举仍保持上线前全量生成、
LLM-B 自检与 Java 校验、预热及激活流程。`adviceKeyMissCount` 用于判断是否需要扩充正式枚举。

### 19.2 V1.6.1 相对 V1.6 的改动

**本版是一致性修正，不改架构、不改逻辑。**

#### 19.2.1 问题

V1.5.5 引入 `reportOverview`（报告自带总览数字，§5.4.1）时，
改了 §4.0 的输出示例，**但没有同步改 §4.0.1 的字段数与 required 列表**。
结果是文档内部冲突：

```
§4.0 示例 JSON     → 九个顶层字段（含 reportOverview）
§4.0.1 Schema 描述  → "八个顶层字段"，required 列表里没有 reportOverview
§4.0.4 提示词       → "八个顶层字段必须全部输出"
§2 链路图           → "八个顶层字段严格 Schema"
§14 优先级          → "八个顶层字段严格 Schema"
```

**直接影响 Dify 输出 Schema**：照 §4.0.1 实现会漏掉 `reportOverview` 的必填校验，
而它缺失时无法区分"报告没印总览数字"与"模型没输出"。

#### 19.2.2 改动清单

| # | 改动 | 位置 |
|---|---|---|
| 1 | 全文统一为**九个顶层字段**，required 列表补入 `reportOverview` | §4.0.1、§4.0.4、§2、§14 |
| 2 | **新增三类字段的空值语义定义**（数组→`[]`、对象→`null`、布尔→必须 true/false） | §4.0.1 |
| 3 | 校验规则补充：**布尔字段为 `null` 视为 `MODEL_OUTPUT_INVALID`** | §4.0.1 |
| 4 | §4.0.2 分级表新增 `reportOverview` 行：**按 null 处理，仅记 warn，不 FAIL** | §4.0.2 |
| 5 | 重试提示词改为按三类字段分别说明空值形态 | §4.0.2 |
| 6 | 补充 `reportOverview` 必填的第三种理由 | §4.0.1 |

#### 19.2.3 三个字段的必填理由各不相同

这一点必须写清楚，否则后续新增字段时会套错模板：

| 字段 | 必填理由 | 缺失的后果 |
|---|---|---|
| 数组类（尤其 `allergens`） | **fail-open 防线** | 缺 `allergens` 当成 `[]` → Layer 1 不运行 → **含过敏原的菜进推荐列表** |
| `allergenSectionPresent` | **整体可信度判断** | 缺失时默认 `false` 方向保守、不构成 fail-open；必填是因为"连布尔都漏输出的响应不可信" |
| **`reportOverview`** | **区分"报告没印"与"模型没输出"** | 前者填 `null` 正常走 §5.4，后者应重试 —— 允许省略则两者无法分辨 |

> **分级的判据始终是"缺这个字段会导致什么后果"，不是"这个字段重不重要"。**
> `reportOverview` 落在最轻一档：失败代价只是回退到自行统计总览条，
> 不影响任何安全判定。为它整体 FAIL 不成比例，且会误伤大量不印总览数字的公立医院报告。

#### 19.2.4 与实现的对齐

若仓库已按九字段严格校验，**以仓库为准**，本版文档向其对齐。
需确认的一点：仓库中 `reportOverview` 缺失时走的是 FAIL 还是按 `null` 降级 ——
**必须是后者**（§4.0.2），否则模型在不印总览数字的报告上偶尔省略字段就会造成大面积任务失败。

---

## 附：本方案承认的能力边界

| 场景 | 覆盖情况 |
|---|---|
| 报告已明确写出的饮食要求（低脂/低盐/低糖/低嘌呤/控制体重等） | 支持 |
| 报告已明确写出的营养补充建议（铁/钙/蛋白质/维D 等） | 支持 |
| 报告已明确标注的过敏原，且在关键词族内（约 11 类） | 支持，Java 硬兜底 |
| **已收录过敏原的词形覆盖** | **部分支持** —— Layer 1 是逐词字面匹配，只认 `allergen_display_split.csv` 里的 73 个词。菜名若只写更泛的单字（如「韭菜炒蛋」的「蛋」，该字不在词表内）则匹配不上。增删词条属安全变更，须单独走流程（`OPEN_QUESTIONS.md` H.2） |
| **关键词族未收录的过敏原**（芹菜、芥末、亚硫酸盐等） | **部分支持** —— 名称精确匹配可拦直白命中；加工食品中的隐藏成分**无法识别**，因此其余菜品一律转入「过敏成分待确认」，不进推荐列表（§8.4） |
| **报告未写饮食要求、仅有指标异常** | **不生成饮食建议与菜品推荐**（需求 §7-5 的明确选择，§1.5） |
| 结论写在分组小结而非指标行的报告 | **支持**（§4.0.5、§4.1 两级判定） |
| 文本层含部首区字符的 PDF/OFD | **支持**（§3.0 规范化层）；新出现的污染形态由 `residualNonStandardCount` 告警后补表 |
| 报告自带总览数字 | **支持**，优先展示报告原文数字（§5.4.1） |
| 既无指标行标记、也无分组小结的报告 | 该组指标**全部丢弃**，总览条附小字说明 |
| 拍照上传的原文引用验证 | **支持**（§3.11 已升主链路，为拍照页面提供文本层）。仅在 DOC-VL 质量不达标时退回"提供原图跳转" |
| 拍照上传的数值准确性 | **支持**，DOC-VL 与 LLM-A 互为独立来源（§4.4b） |
| **跨页续表的结构还原** | **部分支持** —— DOC-VL 可能把表头文字混入数据列（§3.11.5），靠强制 MIXED + 视觉纠正缓解 |
| **报告未做过敏原筛查时的过敏保护** | **不支持** —— 本版无用户自填入口，系统对用户过敏情况一无所知，唯一兑现是模块级提示（§1.2） |
| 厨房交叉污染导致的过敏风险 | **不支持** —— 数据不可得，固定提示现场确认 |
| **历史报告回看** | **不支持** —— 结果 TTL 2 小时，过期需重新上传（产品决策） |
| 碘、肾病（低蛋白/低钾/低磷）、用药、孕产、儿童相关饮食 | **BLOCKED** —— 只展示原文 + 就医提示 |
| 药物-食物相互作用 | **不支持** —— 体检报告不含用药全貌 |
| **营养建议内容的医学准确性** | 由**LLM-B 自检与 Java 校验**保证（全量 key 上线前过审），非专业机构签发；程序只能校验格式与合规词 |

不支持的场景**明确告知用户，不做降级猜测**。
