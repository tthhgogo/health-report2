 2. 营养补充、饮食要求缺少章节和顺序字段

  LLM-A 输出中的 nutritionSupplements、dietRequirements 只有：

  enumKey、itemIndex、blockRefs

  见AI体检报告分析-精简设计方案V1.md:1112。

  但模块三要求展示“总检结论/专家建议”等来源，AI体检报告分析-精简设计方案V1.md:1840又没有说明来源如何得到。

  这样实现时只能让 Java：

  - 根据 blockRefs 查找相邻章节；
  - 与 summaryConclusions 做文本关联；
  - 或直接写死“总检结论”。

  前两种属于语义/版面推断，第三种会显示错误来源。

  建议让 LLM-A直接返回：

  {
    "enumKey": "LOW_SODIUM",
    "sectionIndex": 3,
    "sourceOrder": 2,
    "itemNo": 4,
    "blockRefs": [115]
  }

  Java只按字段查表和排序。

  3. 跨批章节合并仍依赖解析器顺序

  方案声称同一章节跨批时 sectionSegmentId 相同并“天然合并”，见AI体检报告分析-精简设计方案V1.md:778。但非重叠批次中，后一批通常看
  不到前一批的标题块，不可能再次返回同一个标题 segmentId。

  同时 groupOrder 使用“页码 + 最小 segmentId”，见AI体检报告分析-精简设计方案V1.md:1407。segmentId 中的 seq 是解析器产出顺序，不
  一定是双栏、表格页面的阅读顺序。

  最简单的处理是：

  - 章节顺序完全使用 LLM-A 的 sectionIndex/orderInSection。
  - 只在模型明确返回 CONTINUATION 时机械继承。
  - 删除“同一章节跨批自然获得相同 ID”的假设。
  - 如果还想更简单，可以不合并跨批章节，允许同一章节分成两个展示组。

  ## P0：会导致错误或数据不一致

  4. OCR 块数溢出的降级没有定义完整

  AI体检报告分析-精简设计方案V1.md:1007说 OCR 每页超过 400 块时“该页按截断处理”，但：

  - PAGE_TRUNCATED 原本只表示超过 30 页。
  - 没说明是否设置 partial。
  - 没说明 processedPages 如何计算。
  - 没说明是否关闭模块三、四。
  - partial_reason 枚举没有 OCR_BLOCK_OVERFLOW。

  如果被丢掉的正好是过敏原页，系统可能继续推荐菜品。

  建议简单处理：OCR 任一页超过上限，整任务直接失败为 UNREADABLE；不要再增加复杂的局部截断规则。

  5. MySQL成功状态与 Redis 结果无法原子写入

  AI体检报告分析-精简设计方案V1.md:410要求“置 SUCCEEDED 与写 Redis 结果在同一逻辑步骤”，但 MySQL 与 Redis 无法靠普通事务原子提
  交。

  需要明确固定顺序：

  写 Redis result，暂不对外可见
  → MySQL CAS：ASSEMBLING → SUCCEEDED
  → CAS 失败则删除 Redis result

  结果接口必须先检查 MySQL 为 SUCCEEDED，再读取 Redis。

  另外，任务行只保留 30 分钟，而结果保留 2 小时，AI体检报告分析-精简设计方案V1.md:449。任务行被删后，接口无法继续完成归属校验，
  Redis 的后 90 分钟实际不可访问。

  应在成功时把任务 expire_at 更新为与结果一致的 2 小时。

  6. deadline_at 写了但没有执行规则

  任务领取时设置了十分钟截止时间，AI体检报告分析-精简设计方案V1.md:330，但巡检只检查“心跳是否超过十五分钟未更新”。

  只要心跳线程持续运行，任务即使执行一小时也不会触发 EXECUTION_TIMEOUT。

  建议巡检增加简单时间比较：

  status 为执行中 且 now > deadline_at
  → FAILED / EXECUTION_TIMEOUT

  这是明确的数值比较，适合 Java。

  ## P1：建议简化

  - ConclusionLabelWords、NormalStatementWords、AllergenSectionWords 虽然只告警、不覆盖模型结果，但仍在 Java运行语义词表扫描。若
    严格追求简单，可移到离线评测，不进入生产请求链路。AI体检报告分析-精简设计方案V1.md:1296

  - allergenSectionBlockRefs 和 allergenDataBlockRefs 被加入契约，但正文没有真正定义两者如何参与校验，仅在示例注释中说“Java靠差
    集判断”。要么补成明确的集合规则，要么删除，保留原始关键词安全扫描。AI体检报告分析-精简设计方案V1.md:1130

  - 文档说 bbox 会传给 LLM-A，但批次渲染格式只有 [blockRef] 文本，没有规定坐标格式。必须明确每块是否包含 bbox，否则模型只能依赖
    解析器顺序理解版面。AI体检报告分析-精简设计方案V1.md:644

  - 顶层任务线程池和批次并发都存在，但没有说明是否使用同一个执行器。如果 W 个任务线程各自向同一个 W 大小线程池提交四个批次并等
    待，可能直接线程饥饿死锁。应明确使用独立的“任务执行池”和“LLM调用池”。

  ## 工程规范冲突

  设计方案还有两处直接违反工程约束：

  - 方案让 create_by/update_by 写 userId，AI体检报告分析-精简设计方案V1.md:2334；工程规范要求固定系统标识，绝不写用户标识。
    AGENTS.md:100

  - DDL 示例中的字符列没有逐列声明 CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci，但工程规范明确要求文档 DDL 示例也不能例外。
    AGENTS.md:83
