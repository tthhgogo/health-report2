# AI 体检报告分析精简设计方案 V1——开发可落地性评审

## 1. 评审范围

本次评审以“开发人员能否不依赖口头补充，直接依据文档完成实现、联调和验收”为判断标准，评审材料包括：

- 《体检报告分析需求.md》
- 《AI体检报告分析-精简设计方案V1.md》
- `schema/llm_a_output.schema.json`
- `schema/llm_b_output.schema.json`
- `prompt/llm_a_extract.md`
- `prompt/llm_b_dish_tag.md`
- `constants/README.md`

## 2. 评审结论

**当前方案暂不具备直接进入完整功能开发的条件。**

方案对文件安全、敏感信息生命周期、模型幻觉防护、过敏安全、任务幂等和菜品标签缺失等问题考虑较深入，可以开始部分技术预研和工程脚手架工作；但核心证据模型、跨批排序、正式 Schema、结果生命周期、队列可靠性、前后端接口契约和若干产品口径仍未闭环。

在这些问题解决前，不建议冻结接口、数据表和核心解析链路，也不建议前后端按当前文档独立并行开发，否则较大概率在联调阶段发生结构性返工。

## 3. 开发前必须解决的问题

### 3.1 表格 Segment 设计与指标证据校验互相冲突

**问题**

方案 §3.2.1 规定 PDF、Word 表格区“一个单元格生成一个 segment”；但 §3.2.3 和 §4.2 又要求指标的名称、检测值、单位、参考范围、结论五个字段必须全部能在同一个 segment 中找到。

真实体检报告通常是一行代表一个指标，各字段分布在不同单元格。按照当前规则，表格类报告中的大量指标会在 Java 包含性校验时被整条丢弃。

另外，PDFBox 本身不能直接稳定识别“表格单元格”，当前文档也没有定义表格检测、行列重建和跨页表头处理算法。

**影响**

- 健康指标召回率会大幅降低；
- 表格型报告可能出现指标模块为空；
- “原文证据可核对”的核心设计无法成立；
- PDF、OFD、Word 和 OCR 路径可能产生不同的证据语义。

**建议**

二选一并形成统一解析契约：

1. 表格按“逻辑行”生成 segment，使一项指标的全部字段处于同一 segment；
2. 改成字段级证据结构，例如 `nameSegmentId`、`valueSegmentId`、`unitSegmentId`、`refRangeSegmentId`、`conclusionSegmentId`，同时增加整行 `evidenceSegmentIds` 用于展示原文。

无论采用哪种方式，都需要补充各文件格式统一的 `ParsedDocument/ParsedPage/Segment` 数据结构和表格重建规则。

### 3.2 并行分批无法可靠生成文件级连续序号

**问题**

方案要求 LLM-A 的多个批次完全并行，但又要求模型返回文件内连续的：

- `sectionIndex`
- `sourceOrder`
- `orderInSection`

并行执行时，后续批次不知道前面批次已经识别出多少章节、结论或指标，因此不可能稳定生成文件级连续序号。章节跨批次时，模型也无法判断当前页面是在延续上一章节，还是开始一个新章节。

**影响**

- `groupKey = fileIndex + sectionIndex` 可能发生错误合并；
- 同一章节可能被拆成多个分组；
- 不同章节可能使用相同 `sectionIndex`；
- 健康问题和总检结论的排序不稳定；
- 并行完成顺序不同可能导致结果不同。

**建议**

- LLM 只返回证据位置和局部分类，不负责生成全局连续序号；
- Java 根据 `fileIndex → page → segmentSeq → itemSeq` 生成稳定排序；
- 章节识别应返回章节标题证据或章节起始 segment，由合并阶段生成稳定 `sectionId`；
- 若必须由模型识别章节延续关系，应提供批次重叠范围和明确的跨批合并算法。

### 3.3 去重规则在当前输出契约中无法实现

**问题**

方案规定跨批重复项通过 `segmentId + clauseIndex` 判定，但当前正式 Schema 中：

- `indicators` 没有 `clauseIndex`；
- `textualFindings` 没有 `clauseIndex`；
- `allergens` 没有 `clauseIndex`；
- 只有 `nutritionSupplements` 和 `dietRequirements` 包含 `clauseIndex`。

因此该规则无法统一应用于文档列举的指标、文字结论和过敏原等对象。

**建议**

为不同类型定义明确的去重键：

| 类型 | 建议去重键 |
| --- | --- |
| 健康指标 | `segmentId + name + value + conclusionText`，或新增稳定 `clauseIndex/itemIndex` |
| 文字结论 | `segmentId + title + conclusionText` |
| 总检结论 | `segmentId + clauseIndex/category` |
| 过敏原 | `segmentId + rawName + rawResult` |
| 营养补充/饮食要求 | `segmentId + clauseIndex + enumKey` |

同时需要明确批次是否存在重叠、重叠几页、在哪些场景启用去重。目前方案提到了“输入确有重叠时”，但未定义实际重叠策略。

### 3.4 正式 JSON Schema 与正文不一致

**问题**

正式 Schema 存在以下缺口：

1. `nutritionSupplements` 和 `dietRequirements` 都引用通用 `adviceItem`；
2. `adviceItem.enumKey` 允许任意字符串；
3. `$defs` 中已经定义的 `nutritionKey` 和 `dietRequirementKey` 没有被引用；
4. 没有实现正文要求的 `batchStatus != OK` 时其他字段必须为空的条件约束；
5. LLM-B Schema 对所有维度都允许 `RECOMMEND/REJECT/NEUTRAL`，没有从结构上禁止过敏原维度返回 `RECOMMEND`；
6. Schema 无法保证请求中的每道菜只返回一次并全部覆盖，需要明确 Java 校验；
7. 正文一处规定 Schema 校验失败直接失败且不重试，另一处又写“按 Schema 必填走重试”，口径冲突。

**影响**

开发人员若以正文为准和以 Schema 为准，会得到不同实现；错误枚举可能通过正式契约校验并进入业务逻辑。

**建议**

- 为 `nutritionSupplementItem` 和 `dietRequirementItem` 分别定义 `$defs`；
- 使用 `if/then` 或按类别拆 Schema，限制批次状态和 LLM-B verdict；
- 补充 Java 级语义校验清单；
- 用正例、反例建立契约测试；
- 明确全案到底是“零自动重试”还是允许结构化输出重试，并全文统一。

### 3.5 任务记录与结果数据生命周期冲突

**问题**

当前设计同时规定：

- `ct_health_report_task` 约 30 分钟后物理删除；
- Redis 分析结果 TTL 为 2 小时；
- 结果接口必须通过任务行校验 `taskId` 是否属于当前用户且未删除。

任务行被删除后，Redis 中剩余约 90 分钟的数据无法完成归属鉴权，因此“结果保留 2 小时”实际无法成立。

**建议**

- 任务归属记录至少保留到 Redis 结果过期之后；或
- 单独保存不含健康数据的结果授权记录，其 TTL 与结果一致；
- 明确 `expire_at` 在上传、任务创建、重试、成功时是否刷新；
- 把用户可访问时间、原始文件重试时间和后台墓碑保留时间拆成不同字段，不建议共用一个 `expire_at`。

### 3.6 MySQL 状态、Redis 结果与用户删除之间缺少可实现的竞态方案

**问题**

方案要求置 `SUCCEEDED` 与写 Redis 结果处于“同一逻辑步骤”，并以状态 CAS 成功为前提。但 MySQL 与 Redis 之间没有原子事务，仍可能发生：

```text
Worker CAS：任务状态变为 SUCCEEDED
用户 DELETE：设置 deleted_at，并删除 Redis 结果
Worker SET：将分析结果重新写入 Redis
```

结果会在用户确认删除后重新出现，正是方案希望避免的竞态。

S3 删除也存在类似问题：如果删除数据库文件行后 S3 删除失败，将失去对象定位信息，形成无法追踪和清理的敏感文件。

**建议**

定义明确、可测试的写入协议，例如：

1. 先写一个对 GET 接口不可见的结果；
2. CAS 将任务置为 `SUCCEEDED`；
3. GET 结果必须同时检查任务状态和 `deleted_at`；
4. CAS 失败时删除预写结果；
5. 删除接口先写持久化删除标志，再执行可重试的 Redis/S3 清理；
6. S3 对象定位信息必须保留到确认对象删除成功后；
7. 增加清理失败重试与遗留对象巡检。

需要用并发测试覆盖 Worker 完成、用户删除、超时巡检和清理任务同时发生的情况。

### 3.7 Redis 入队没有完整的投递恢复机制

**问题**

“XADD 在 MySQL 事务提交前执行”并不能让 MySQL 和 Redis 形成原子事务。“不可能出现有任务但没有消息”的结论依赖 Redis 写入绝不丢失、Worker CAS 必须访问主库并等待事务完成等隐含前提。

Redis 故障切换、异步持久化丢写、Stream 被错误裁剪或消费组 PEL 长期堆积时，仍可能出现 `QUEUED` 任务没有可处理消息。

当前方案也没有定义：

- Consumer Group 创建方式；
- Worker 崩溃后的 `XAUTOCLAIM`/PEL 清理；
- `XACK`、`XDEL` 的异常处理；
- Stream 最大长度；
- 非法消息和毒消息处理；
- 队列深度的具体阈值和返回错误码；
- 排队过久任务的处理方式。

**建议**

至少提供一种投递恢复保障：

- 恢复事务 outbox；或
- 定时扫描长时间处于 `QUEUED` 且没有有效消费记录的任务并补投消息。

补投消息属于投递恢复，不等于重新执行已经失败的分析任务，可以继续保持业务执行“零自动重试”。

### 3.8 上传进度与任务状态不对应

**问题**

当前流程是逐个文件上传完成后，用户再调用 `/analyze` 创建任务；但任务 `QUEUED` 状态被映射为 `UPLOADING / 0%~30%`。

任务创建时文件已经上传完成，因此任务轮询无法提供真实上传进度，页面也无法依据任务状态显示 0%~30%。

**建议**

- 0%～30% 由前端根据 multipart 上传字节数计算；
- 全部文件上传成功并创建任务后，服务端进度从 30% 开始；
- `QUEUED` 单独展示“等待处理”，不要映射为 `UPLOADING`；
- 明确多文件上传是串行还是并行，以及总进度如何加权；
- 明确单文件上传成功、其余文件失败时的清理和重传行为。

### 3.9 创建任务时无法直接获得 Word 等效页数

**问题**

`POST /analyze` 要求在创建任务前校验累计页数，但 DOC/DOCX 的等效页数依赖：

- 正文和表格 segment 数；
- 内嵌图片数量；
- 内嵌图片尺寸；
- OCR 后产生的 segment。

这些数据只有深度解析后才能得到，而方案又把文件解析放在 Worker 阶段。当前上传接口只要求 POI 能打开且正文非空，不能完成等效页数计算。

**建议**

明确以下口径之一：

1. 上传时同步进行结构预扫描，并持久化 `pageCount/equivalentPageCount/segmentCount/imageCount`；
2. 创建任务只校验文件数和体积，页数限制由 Worker 解析后裁决；
3. DOC/DOCX 使用不依赖 OCR 结果的预估规则，并明确误差处理。

还需要明确超过 60 页是在 `/analyze` 同步返回错误，还是先创建任务后异步进入 `FAILED`。

### 3.10 健康问题正常词兜底可能错误过滤真实问题

**问题**

方案一方面明确“阴性不一定正常”，另一方面 `NormalStatementWords` 又将“阴性”作为整段正常关键词。整段命中后会强制将 `includeInHealthProblems` 改为 `false`。

例如：

```text
乙肝表面抗体阴性，建议接种乙肝疫苗
```

该条可能被整段排除。

方向词表也存在子串冲突：

- `↑↑` 同时命中 `↑`；
- “未见明显异常”会命中“异常”；
- “不正常”会命中“正常”；
- 一个 segment 中同时出现正常和异常描述时，结果取决于遍历顺序。

**建议**

- 不要使用整段无优先级子串匹配直接推翻结构化结果；
- 词表匹配应针对抽取出的具体 `conclusionText` 或 clause；
- 定义最长短语优先、否定词优先和冲突裁决顺序；
- “阴性”不能作为通用正常词；
- 为“未见明显异常”“不正常”“阴性但需处置”等反例建立单元测试。

### 3.11 健康问题空态可能形成无依据的“全部正常”结论

**问题**

当前规则是三类健康问题来源全部为空时，直接展示：

> 本次体检各项指标均在正常范围内，请继续保持良好的生活习惯。

但结果为空还可能由以下情况造成：

- 报告只有数值，没有明确结论；
- 模型漏抽；
- Segment 回切失败后条目被丢弃；
- 某批次不可读；
- 报告被截断；
- 上传的有效报告部分没有覆盖结论页。

这些情况都不能证明“各项指标均正常”。特别是 `BATCH_UNREADABLE` 和 `PAGE_TRUNCATED` 状态下，继续展示肯定的正常结论存在明显误导。

**建议**

- 增加 `confirmedNoHealthProblems` 或等效的完整性判定；
- 只有报告明确给出全部正常结论，且不存在 partial/evidence miss 时，才使用需求中的正常空态；
- 其他情况使用中性文案，例如“本次报告未提取到明确的异常结论”；
- 中性文案属于需求变更，需要产品确认并回写需求。

### 3.12 过敏原交叉扫描无法发现“部分漏抽”

**问题**

当前高风险扫描只在命中过敏关键词且整个 `allergens` 数组为空时降级。

如果模型抽取了筛查表中 20 项里的 19 项，恰好漏掉唯一阳性项，因为数组不为空，安全降级不会触发。完整筛查表本来就会返回大量阴性条目，因此“数组为空”不是有效的阳性漏抽检测方式。

此外，`resultStatus = UNKNOWN` 目前只记录计数并继续生成推荐，也与“过敏是唯一安全红线”的原则存在张力。

**建议**

- 对每个过敏章节中出现阳性、弱阳性、可疑、临界标记的 segment 做逐条证据覆盖检查；
- 每个高风险 segment 必须至少关联一个准入过敏原条目；
- 未覆盖即设置 `ALLERGEN_SUSPECT_MISS` 并关闭菜品推荐；
- 明确 `UNKNOWN` 是否需要展示、阻断推荐或要求用户重新上传；
- 一个 segment 含多项过敏原时，需要增加稳定的 `itemIndex/clauseIndex`。

### 3.13 菜品打标证据校验前后冲突

**问题**

LLM-B Prompt 允许根据菜名、工艺或隐藏成分进行判断，此时 `matchedIngredients = []` 是合法输出；但方案 §8.10 又规定匹配食材全部对不上时降级为 `NEUTRAL`。

这会导致以下判断被错误清除：

- 菜名暗示含有过敏原；
- 挂糊、裹粉、油炸等烹饪工艺；
- 食材表漏录的复合调味料；
- 饮食注意维度中完全依据烹饪方式产生的 `REJECT`。

**建议**

增加结构化证据类型，例如：

```text
evidenceType = INGREDIENT | DISH_NAME | COOKING | INFERRED
```

只有 `INGREDIENT` 类型要求 `matchedIngredients` 非空且属于菜品食材表；其他类型要求 `reason` 非空，并进入单独的审核与统计口径。

### 3.14 菜品标签的陈旧数据清理规则不正确

**问题**

方案规定 `ct_dish_tag` 按 `create_time` 清理陈旧行。但一条标签只在菜品、模型、提示词或内容版本变化时重算。长期没有变化但仍然有效的标签，其 `create_time` 会越来越早，最终会被按时间误删。

如果预热任务先执行 diff、确认标签存在后跳过，再由清理任务删除，该菜会在当天变成 `TAG_MISSING`。

**建议**

清理依据应是：

- 标签对应的 `dishHash` 是否仍为菜品当前 hash；
- `tagPolicyVersion` 是否仍处于保留版本集合；
- 菜品是否已下架且超过保留期；
- 当前 Redis Key 是否仍可能回源该行。

不能仅凭 `create_time` 判断标签是否失效。

### 3.15 PDF 文本层判定需要改为逐页或混合模式

**问题**

当前通过“抽取字符数/页数”判断整个 PDF 是否有文本层。真实报告可能是混合 PDF：前几页有原生文本，后几页是扫描图，或者只有封面带文本。

使用文档级平均值可能导致：

- 扫描页未执行 OCR；
- 原生文本页被不必要地重复 OCR；
- 同一报告不同页面的证据质量不可控。

**建议**

按页判断文本层质量，并允许一份 PDF 同时存在 `NATIVE` 和 `OCR` 页面。判定指标和阈值也应在真实样本上验证。

### 3.16 图片和文档解析的资源安全限制仍不完整

**问题**

当前图片流程先“实际解码”，再校验总像素。恶意图片可能在完成像素检查前就触发巨额内存分配。PDF 超大页面渲染、复杂矢量内容、恶意字体和 OFD XML 也可能造成 CPU、内存或解析器风险。

**建议**

- 图片先读取尺寸元数据，再决定是否完整解码；
- 对单页渲染像素、DPI、耗时和任务总内存设硬限制；
- 对 XML 禁用外部实体和外部资源；
- OFD/DOCX 预扫描同时检查路径穿越、嵌套容器和压缩大小；
- 对解析器设置任务级超时和资源隔离；
- 用恶意样本和超大页面做安全测试。

## 4. 需要产品正式确认并回写需求的事项

方案 §12 已列出 14 项待产品确认。其中以下事项会直接改变代码结构、页面表现或验收结果，不能只保留在技术方案中：

### 4.1 是否新增第四种指标状态 `ABNORMAL`

原需求只定义正常、偏高、偏低三态。方案新增橙色“异常”，并允许模型理解阳性/阴性的医学含义，这与“只引用报告原文、不额外分析”的需求边界存在冲突。

需确认：

- 是否新增第四态；
- 哪些结论允许模型判断；
- 模型判断是否计入顶部异常占比；
- 验收时如何判断模型医学语义是否正确。

### 4.2 是否正式取消“体检报告不完整”失败类型

原需求明确要求缺页时返回“体检报告内容不完整”。方案决定 V1 不实现。必须修改需求或形成正式评审结论，否则测试仍可按原需求报缺陷。

### 4.3 是否新增累计 60MB 上限

需求只有单文件大小和最多五个文件的限制，理论最大为 100MB；方案新增累计 60MB 限制，但没有确定错误文案和 HTTP/业务错误码。

### 4.4 降级时隐藏模块还是显示原因

需求规定结果页包含四个模块；方案允许部分场景隐藏模块三、四，同时又规定全页面不增加任何提示文字。

需明确：

- 隐藏是否算满足“四模块”要求；
- 用户是否需要知道结果不完整；
- `PAGE_TRUNCATED`、`BATCH_UNREADABLE`、`ALLERGEN_SUSPECT_MISS` 分别显示什么；
- 静默隐藏是否通过医疗、安全和用户体验评审。

### 4.5 饮食建议来源是否扩大

需求限定饮食要求来自总检结论；方案扩大到总检结论和医生建议章节。需要正式回写需求，并明确“专家建议、健康指导、专科小结、既往医嘱”的边界。

### 4.6 是否限制一次只能分析一个人

需求支持多份报告，但没有规定必须属于同一人。方案新增姓名/性别冲突拒绝规则，并注明不支持代家人分析。

需要明确：

- 用户上传本人与家属报告是否允许；
- 姓名 OCR 错误造成误拦时如何处理；
- 姓名完全未识别时为何又允许继续；
- 是否需要用户确认而不是直接失败。

### 4.7 总览条使用报告汇总还是卡片计算结果

方案同一章节先定义“总指标数等于本模块卡片数”，随后又规定报告自带汇总数字优先，两者矛盾。

还需明确报告只提供总数和异常数时，正常项数如何展示，以及报告汇总数字与卡片数量不一致时的验收口径。

建议统一按展示卡片计算，报告原始汇总如需保留可单独展示。

### 4.8 健康问题名称是否允许系统生成

需求要求问题名称直接引用报告原文、不做改写；方案允许将“甘油三酯 + 偏高”拼接成问题名称。`displayNameGenerated` 只能标记差异，不能消除需求冲突。

### 4.9 `OTHER` 饮食建议是否允许字段不完整

需求要求每条饮食建议提供食材、摄入量或烹饪方式等内容；方案对 `OTHER` 只展示原文，不提供这些字段，也不参与菜品推荐。必须修改需求或扩充受审核枚举和内容常量。

### 4.10 过敏命中后是否隐藏全部正面标签

需求要求展示所有命中的标签；方案为了突出过敏风险，不下发任何正面标签。安全方向合理，但仍需成为正式产品规则。

### 4.11 `dish_id` 唯一性及新上架菜品策略

需要确认：

- `dish_id` 是全系统、租户内、食堂内还是日期内唯一；
- Redis Key 是否需要增加租户、食堂或档口维度；
- 预热后新上架菜品当天完全不展示是否可以接受；
- 临时加菜、菜品改配方和跨日复制菜品如何处理。

### 4.12 “关闭页面”的准确含义

浏览器关闭、App 被系统杀死、断网和页面崩溃时，前端无法保证成功调用 DELETE。

需要明确需求中的“关闭”是：

- 点击业务页面的关闭按钮；
- 返回上一页；
- 浏览器关闭标签页；
- App 进入后台或被杀死。

建议将主动点击确认关闭作为实时删除触发点，其他非可靠退出场景由短 TTL 和后台清理兜底，并在需求中写明。

## 5. 接口和数据契约缺失项

当前 §9.2 只有接口清单，没有达到前后端可独立开发所需的详细程度。至少需要补充 OpenAPI 或等价契约。

### 5.1 上传接口

需要定义：

- multipart 字段名；
- 文件名编码；
- 成功响应 DTO；
- 格式、大小、损坏、页数和服务异常对应的 HTTP 状态码与业务码；
- 上传超时和取消；
- 同一文件重复上传如何处理；
- S3 成功但数据库写入失败时如何补偿；
- 孤儿上传的查询和删除方式。

### 5.2 创建任务接口

需要定义：

- `fileIds` 是否禁止重复；
- 请求幂等键；
- `FILE_ALREADY_BOUND` 在多个文件分别绑定不同任务时如何返回；
- 累计体积、页数和队列已满的错误结构；
- 混入一个非体检报告文件时，是拒绝整个任务还是忽略该文件；
- `expire_at` 和文件重试窗口如何刷新。

当前规则只有“全部文件都不是报告时失败”，意味着一份有效报告加一份无关文件可能被静默接受，需要产品确认。

### 5.3 状态接口

需要定义：

- 完整响应 Schema；
- 状态、阶段、进度的允许组合；
- 任务不存在、已删除、已过期的区别；
- 轮询的 `Retry-After` 或建议间隔；
- 客户端五分钟停止轮询后如何恢复；
- `reanalyzable=true` 时如何取得原始 `fileIds` 或重分析令牌。

当前方案依赖前端保留原 `fileIds`。页面刷新后如果前端状态丢失，“重新解析”按钮可能无法工作。

### 5.4 结果接口

需要提供正式结果 JSON Schema，至少定义：

- 四模块对象结构；
- 模块被抑制时是字段缺失、`null` 还是带状态对象；
- `partialReason` 是否为数组；
- 指标卡片和健康问题的 ID、关联关系；
- 原文、来源和枚举展示字段；
- 推荐与不推荐菜品 DTO；
- 空态是后端文案还是前端根据空数组渲染；
- 结果尚未生成、已经过期、任务失败时的错误结构；
- 大量指标和菜品下是否分页。

`partial_reason` 当前只能保存一个值，但 `PAGE_TRUNCATED`、`BATCH_UNREADABLE` 和 `ALLERGEN_SUSPECT_MISS` 可能同时发生。建议改为原因集合，或定义明确的优先级并保留全部观测指标。

### 5.5 删除接口

需要定义：

- 是否幂等；
- 重复删除返回 200、204 还是 404；
- S3、Redis 清理部分失败时的响应；
- 清理是否异步；
- 前端何时可以认为数据已彻底删除；
- Worker 收到删除标志后是否停止继续向 OCR/Dify 发送数据。

## 6. 菜品数据与打标链路待明确事项

### 6.1 实际数据表尚未确认

方案中的 `ct_dish` 和 `ct_dish_ingredient` 是假定结构，实际表名、字段和数据质量尚未核对。开发前需要提供真实 DDL 或数据访问接口，并明确：

- 租户、食堂、档口、餐次和日期维度；
- 菜品上下架状态；
- 食材重量单位及空值比例；
- 调味料和复合配料完整性；
- 菜品名称和食材名称是否已标准化；
- 当前用户可见范围如何计算。

### 6.2 主料推导缺少异常数据规则

需补充：

- 负重量、零重量和异常大重量；
- 同名食材多行；
- 克、千克、份、勺等非统一单位；
- 全部食材被调味料表排除；
- 总重量为零；
- 并列前两名；
- 菜品无食材明细。

### 6.3 `dishHash` 和标签字段格式需要定稿

需要明确：

- 规范化算法版本；
- 食材别名是在算 hash 前还是匹配时应用；
- `matched_ingredients` 在 MySQL 中使用 JSON、分隔字符串还是其他格式；
- 食材名称包含逗号等字符时如何序列化；
- `tagPolicyVersion` 的 hash 拼接格式和算法；
- 模型实际版本变更如何自动触发版本升级。

### 6.4 标签清理与缓存回填

除修正按时间清理问题外，还需明确：

- Redis 未命中、MySQL 回源命中后是否回填 Redis；
- 某个维度部分字段缺失时的批量回源方式；
- 每日 Key 使用哪个时区和业务日期；
- 跨午夜请求如何选择 `bizDate`；
- 当日预热未完成时是否继续读取前一日标签。

## 7. 上线前置交付仍未完成

### 7.1 医疗和营养内容常量为空

`constants/README.md` 明确说明当前目录只定义结构，不含具体内容。以下核心常量尚未交付：

- `AllergenContent`
- `NutritionContent`
- `DietRequirementContent`
- `AllergenKeywords`
- `HighRiskAdvicePatterns`
- `IngredientAliasWords`
- `RadicalNormalizeMap`

其中前三类内容既决定页面展示，也直接影响菜品推荐；没有实际内容，模块三和模块四无法实现完整功能和验收。

### 7.2 医务审核未完成

常量审核记录目前为空。按照方案自己的 P0 要求，审核表未填满时模块三和模块四不得上线。

审核记录至少应包含：

- 审核人和资质；
- 审核日期；
- 内容版本；
- 审核范围；
- 修改意见及复核记录；
- 高风险内容的最终确认结论。

### 7.3 外部服务和隐私条件未确认

开发前应确认并形成配置清单：

- OCR 服务商、接口、超时、并发和数据留存；
- Dify 工作流 ID、模型版本、结构化输出能力和日志关闭方式；
- 模型输入的图片数量、大小和 token 限制；
- S3 Endpoint、Bucket、加密、签名和生命周期策略；
- Redis 部署方式、持久化策略和故障切换模型；
- 是否跨境传输；
- APM、异常追踪和网关是否记录请求体；
- 运维人员访问敏感数据的权限和审计。

## 8. 测试与验收缺口

方案列出了需要验证的假设，但还没有形成开发可执行的验收标准。建议在编码前建立脱敏或授权使用的黄金样本集，并为每份样本标注期望结果。

### 8.1 文件解析样本

至少覆盖：

- 原生文本 PDF；
- 全扫描 PDF；
- 原生文本与扫描页混合 PDF；
- 旋转、倾斜、模糊、反光图片；
- 表格跨页、重复表头和合并单元格；
- DOCX 表格；
- Word 内嵌扫描图片；
- 旧版 DOC；
- 不同医院 OFD；
- 损坏文件、加密 PDF、超大页面和解压炸弹样本。

### 8.2 抽取和证据验收

每种类型至少统计：

- 指标准确率和召回率；
- 异常指标召回率；
- 健康问题召回率；
- 过敏原阳性召回率；
- 饮食医嘱召回率；
- 证据回切成功率；
- OCR 模糊匹配使用率及错误率；
- 多文件排序和分组正确率；
- `OTHER` 占比。

过敏原阳性漏抽应单独设上线门槛，不能只观察整体准确率。

### 8.3 菜品推荐验收

应由营养师或医务人员对真实菜品人工标注：

- 每个过敏原维度的拒绝结果；
- 每个饮食注意维度的推荐、拒绝和中立；
- 主料推导结果；
- 营养补充匹配结果；
- 多维度冲突裁决；
- 标签缺失时的隐藏行为；
- 新菜、改配方和跨日缓存行为。

### 8.4 状态机和并发验收

至少覆盖：

- 重复点击创建任务；
- 创建成功但响应丢失；
- Worker 崩溃；
- Redis 重启或故障切换；
- 用户在 PARSING、EXTRACTING、ASSEMBLING、SUCCEEDED 各阶段删除；
- 清理任务与 Worker 同时执行；
- 结果写入和用户删除同时发生；
- 任务排队超过文件过期时间；
- 同一文件被两个请求并发绑定；
- 重分析时旧消息晚到。

## 9. 建议实施顺序

### 阶段一：需求与产品口径冻结

1. 对方案 §12 的全部待确认项逐项形成结论；
2. 所有与原需求不同的结论同步修改《体检报告分析需求.md》；
3. 明确降级展示、第四种状态、累计大小、缺页、同一人限制和 `OTHER` 规则；
4. 形成可追溯的产品、医疗和安全评审记录。

### 阶段二：修正核心技术契约

1. 重构 Segment 和证据模型；
2. 将全局排序从模型职责迁移到 Java；
3. 修正去重规则；
4. 修正 LLM-A、LLM-B Schema 和 Prompt；
5. 定稿任务、结果、删除和重试生命周期；
6. 补齐可靠投递和 Redis Stream 消费规则。

### 阶段三：补齐开发输入

1. 输出完整 OpenAPI；
2. 输出正式结果 Schema；
3. 提供最终 DDL 和索引；
4. 提供真实菜品数据模型；
5. 填充并审核全部内容常量；
6. 确认 OCR、Dify、S3、Redis 和 xxl-job 配置。

### 阶段四：技术验证和验收基线

1. 建立真实报告黄金样本集；
2. 验证 PDF、Word、OFD 和 OCR 可行性；
3. 验证模型调用耗时、并发配额和失败率；
4. 校准页数、主料、文本层和图片阈值；
5. 建立模型契约、解析、状态机、并发和安全测试；
6. 达到预先约定的准确率、召回率、耗时和安全门槛后再进入正式上线开发。

## 10. 当前可并行开展的工作

在上述问题尚未全部定稿时，可以先进行以下低返工风险工作：

- 文件真实格式识别和基础可读性验证的技术实验；
- PDFBox、POI、ofdrw 对真实样本的兼容性验证；
- OCR 和 Dify 的隐私、留存、并发和耗时验证；
- Redis Stream 消费组和 Worker 心跳的独立原型；
- S3 私有存储、加密和清理补偿机制验证；
- 黄金样本收集、脱敏和人工标注；
- 内容常量的营养师/医务审核；
- OpenAPI、结果 Schema 和契约测试框架搭建。

不建议在核心证据模型、产品口径和结果契约定稿前，直接完成四模块业务代码或前端结果页，否则后续返工面会较大。

## 11. 最终准入清单

以下条件全部满足后，方案才适合作为“直接开发基线”：

- [ ] 需求与方案差异已全部回写需求或形成正式评审记录；
- [ ] Segment 能正确表达表格一行及多证据字段；
- [ ] 跨批章节、顺序和去重规则可由确定性代码实现；
- [ ] LLM-A、LLM-B Schema 与正文完全一致并有契约测试；
- [ ] 任务、结果、文件和删除生命周期一致；
- [ ] MySQL、Redis、S3 之间的失败补偿和竞态行为已定义；
- [ ] Redis Stream 具备投递恢复和 PEL 清理机制；
- [ ] 上传、状态、结果、删除接口已有完整 OpenAPI；
- [ ] 实际菜品数据结构、租户范围和 ID 唯一性已确认；
- [ ] 全部医疗和营养内容常量已填写并完成审核；
- [ ] OCR、Dify、S3、Redis 的生产配置与隐私条件已确认；
- [ ] 真实报告和真实菜品验收集已经建立；
- [ ] 准确率、召回率、耗时、并发和安全上线门槛已有明确数值；
- [ ] 所有降级和空态都有产品确认的用户可见表现。

